# B8 Adversarial Review — Codex
SPEC: `docs/B8_BATCH_SCALE_UX_SPEC.md`
TODO: `docs/B8_BATCH_SCALE_UX_TODO.md`
Mode: strict adversarial, reviewed-mode reuse, read-only product code.

## Verdict: 需修補後派工

## Findings
1. [BLOCKING][High] RunManager「刪除整批」target 未與既有 selectedRuns 確認流程隔離。
   Evidence: SPEC Task2.2「group.runs 過濾 !active→確認對話→reuse bulkDeleteRuns」；現碼確認/送出全讀 `selectedRuns` (`RunManagerPanel.tsx:268-280,335-345,666-727`)。
   Failure: 若直接重用 `openBulkConfirm/executeBulkDelete`，按 batch header 可能刪目前手動勾選項、或確認框顯示非該 batch；也可能為了實作而改 `selectedKeys`，破壞「批次刪除(全選)」既有選取狀態。
   Fix: SPEC/TODO 明定獨立 `bulkDeleteTarget`/mode (`selection` vs `batch`)；確認框與 execute payload 都讀 target，不污染 selection；測試「先勾 A batch，再按 B batch 刪除」。

2. [BLOCKING][High] `already-terminal→skipped` blanket rule 與單項 FSM 語義衝突。
   Evidence: SPEC/TODO Task1.1「already-terminal→skipped」；現碼同決策 terminal 冪等成功，但相反 terminal raise conflict (`apply_retention_decision`:1768-1775)；既有測試 retain→discard/discard→retain 409 (`test_batch_retention.py:568-629`)。
   Failure: bulk retain 與單項 discard 競態時，若 terminal discarded 被吞成 skipped，UI 可能報「全部保留」完成但該 run 已丟棄；破壞「全部保留==逐個 retain」。
   Fix: 分類規則改為 same-terminal success/skipped 可接受；opposite-terminal 必須 failed/conflict 並帶 state/error；新增 bulk+single mixed-decision concurrency test。

3. [MAJOR][High] B8c 測試要求仍允許 mock store 假綠。
   Evidence: TODO Task2.2 只寫「vitest 2案例」；現有 `RunManagerPanel.bulkDelete.test.tsx` mock `useFeatureFactoryStore` action (`:23-35`)，不碰 fetch；B4c 教訓要求 mock fetch 非 mock store。
   Failure: 刪除整批 UI 可綠，但 store payload/endpoint/refresh 斷掉不會被抓；尤其新 batch target 若繞過 `bulkDeleteRuns` store 真路徑會假綠。
   Fix: Task2.2 gate 加至少一個 real Zustand store + mock fetch test，驗證 `/runs/bulk-delete` body 是該 batch idle runs，且 refresh 行為保留。

4. [MAJOR][Medium] bulk retention aggregate schema 不夠可測。
   Evidence: SPEC Task1.1 只寫 `{succeeded, failed, skipped}`；未定每筆是否含 final state/error/code，也未定 failed vs skipped 的 terminal/conflict 分界。
   Failure: 前端無法可靠清 pending 或呈現部分失敗；不同實作者可能把 conflict 放 skipped/failed，驗收不可比。
   Fix: 定義 `BatchRetentionBulkResponse`/TS type：每筆含 run identity、state、error/code；明定 HTTP 200 + per-item status。

## 被當成事實的未驗證假設
- `apply_retention_decision`「可 loop 成 bulk」成立但不完整：per-item lock/FSM 成立，bulk aggregate 對 terminal/conflict 的語義未驗證。
- `bulkDeleteRuns`「可直接重用」成立於 store action；不成立於現有 RunManager confirm state，需新增 batch target mode。

STATUS: DONE
