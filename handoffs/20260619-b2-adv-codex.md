# B2 adversarial review — Codex

SPEC: `docs/B2_PROGRESS_UNIFY_SPEC.md`
TODO: `docs/B2_PROGRESS_UNIFY_TODO.md`
Focus: normalize single出口 / RSS互斥分欄 / parity / single RSS語意 / byte不變 / WS schema_version

## Verdict
需修補後派工。

## Blocking / Major
1. BLOCKING: `current_rss_mb` deprecate 相容策略不夠精確。
   Evidence: SPEC Task3.1「deprecate current_rss_mb(過渡保留 legacy display)」但未指定 REST/WS 是否雙寫、前端是否讀 legacy alias。
   Code: WS mapper 只送 `current_rss_mb`; TS/store/UI/tests 均消費此欄。
   Failure: agent 可能直接移除舊欄，破既有 batch REST/WS/前端/tests。
   Fix: 明定一版內雙寫或 alias 規則、舊欄生命週期、legacy tests。

2. MAJOR: normalize「單一出口」不夠可執行。
   Evidence: SPEC/TODO 要兩路徑都經 normalize，但 batch child 目前先寫 jsonl row，父端再映射 task。
   Failure: child row、parent task、single notify 仍可能各自手組，形成 2-3 套 schema。
   Fix: 指定 normalize 的唯一邊界：raw event→normalized event；jsonl/REST/WS 都只搬 normalized event。

3. MAJOR: `schema_version` 演進未定義 payload 層級與型別。
   Evidence: TODO 只寫 `schema_version:1`; 現有 WS batch mapper不送 schema_version，TS 另有 string optional。
   Failure: 新舊前端無法依版本分支；數字/字串不一致造成弱型別漂移。
   Fix: 定義 progress schema version 欄位位置、型別、初始值、legacy absent 行為、測試。

4. MAJOR: parity 5 條不足且第③空洞。
   Evidence: SPEC §V「③(retention 留 B3,本批 progress 適用前 4)」但仍稱 parity 5。
   Failure: 無法證偽 REST/WS/前端新舊欄相容、single status RSS、schema_version 演進。
   Fix: 改成明確 5 條：single REST/WS、batch REST/WS、legacy alias、互斥、concurrent>1 coarse。

5. MAJOR: single `process_rss_mb` 語意含 API 進程噪音，但後端契約未要求 metadata/label。
   Evidence: SPEC 僅 UI 標「(單)行程 RSS」，API 欄名只 `process_rss_mb`。
   Failure: API/前端消費者可能誤讀為該 symbol 獨佔記憶體。
   Fix: 文件/API/UI 明定 single=API process RSS observational, not per-symbol exclusive。

## Minor / No issue
- Byte不變方向合理，但 gate 應加 diff scope 或 import-only 限制，避免 normalize 觸碰 generation params/cache。
- §RISK 說不命中(a)/(d)合理；命中(b)。
- OOM/cache/quant計算無直接新風險，前提是只改 progress payload。

STATUS: DONE
