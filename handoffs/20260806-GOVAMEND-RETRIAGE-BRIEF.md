
# 治理修正 epic — 全票重新裁決（依使用者新判準）

brief-kind: consult

**受審**：`handoffs/20260801-GOV-AMEND-BACKLOG.md` 全部 38 張票 ＋ 第 0 批批次規劃

## 委員範本（**全文照做**）

`templates/SPEC_TODO_ADVERSARIAL_REVIEW_PROMPT.md` — 完整讀取並照做。
finding heading 逐字格式 `^[A-Z]+-R[0-9]+-P[0-3]-[0-9]{2,}$`，本輪用 `R21`。
零 findings 請明寫 `FINDINGS_COUNT: 0`。

## 🔴 標題限制（**本輪最重要的操作指示，違反即整份作廢**）

**唯一允許的 `##` 標題**：`## Verdict`／`## §0 前提宣告`／`## 逐項核對表`／
`## 出場判準核算`／`## <canonical finding ID>`。

🔴 **禁用任何 `###`／`####` 子標題。**
原因（主委實測）：`scripts/completeness_check.sh:60` 的 `HEADING_LINE_RE='^[[:space:]]*#{2,6}'`
把 **`##` 到 `######` 全部**當成 finding ID 行驗證
⇒ 任何子標題都會被判 `invalid finding ID`，**整份報告作廢**。
本 epic 今日已因此連續作廢 2 輪（三家一輪、composer 一輪）。
**分項請改用表格或條列（`-` / `1.`）。**

## §0 前提宣告

**已查證**（主委實跑）：

- fact-verified: `HEADING_LINE_RE`（ID 抽取，`:60`）用 `#{2,6}`，
  但同檔 `_check_body_hashes` 的 `H2_LINE_RE`（`:913`）用 `##(?!#)`
  ⇒ **同一支檢查器內兩處對「標題」定義不一致**。
- fact-verified: 既有文件裡裸的 `B<數字>` 共 **10,557 處**、涉及 **1,414 個檔案**。
- fact-verified: `docs/GOVERNANCE_ID_NAMESPACES.md` 已登記 12 個 ID 空間，
  §4 另列 2 個未登記；**「群集 ID」不在其中**（該檔「群集」零命中）。
- fact-verified: 主委本 session 的收斂檔用了 `C1-C6`／`D-1~D-4`／`E-1~E-3`／`F-1~F-4` 作群集 ID，
  其中 `D`／`E`／`F` **皆撞既有登記或具名空間**（`E-<n>` 該檔明文「不得當作 ID 空間配置」）。
- fact-verified: 主委本 session **ID 歸錯群 9 次**；`completeness_check` 對「歸錯群」無感
  （群集段刻意排除在 body-hash 之外）。

**假設**（請攻）：

- assumed: 38 張票**全部**都有真實的 agent 失誤可對應。**主委未逐張查證**，
  懷疑其中若干只是「整齊感」或「文件一致性」而無實測失誤。

---

## 🔴 使用者定的新判準（2026-08-06，逐字）

> 「我們現在要解的每一票，都是要讓你們 LLM Agent **如何在最低摩擦和成本下，
> 能精確且順暢的重複做同樣的事不出錯**……**所有票都是在解你們 LLM Agent 運作上的問題**」

> 「以前和現在有的就**釘死不要動**，以後產生的一律用定義好的規則」

**⇒ 取捨公式**：

```
價值 = (擋掉的 agent 失誤類別 × repo 內實證次數) ÷ 新增摩擦
```

🔴 **可讀性不是驗收標準**。只讓人「看起來清楚」而不減少 agent 實際失誤者 ⇒ **關閉**。
🔴 **溯及既往者預設關閉**。修正只針對往後新產生的東西。

## 你要做的

逐張（38 張）填下表，**不得略過任何一張**：

| 欄 | 內容 |
|---|---|
| 票號 | `B-nn` |
| **擋哪類 agent 失誤** | 具名（例：歸錯群／找錯 ID／整輪作廢／誤擋唯讀指令）。**填不出來 ⇒ 建議關閉** |
| **實證次數** | 在 repo 可查到幾次（`audit.log`／收斂檔／事故清單／runlog）。**請貼查法**。查不到寫 0 |
| **新增摩擦** | 每次派工／每次寫文件／每次收斂會多做什麼 |
| **是否溯及既往** | 是／否。是 ⇒ 建議關閉或收窄為 forward-only |
| **裁定** | 做（排序）／降級／**關閉** |

### 另外要回答的

1. **38 張裡有幾張應該關閉？** 請具名。主委預期不只一張，但沒有證據，請你查。
2. **第 0 批目前的批次規劃（B0–B7＋B3R）依新判準是否仍成立？**
   有沒有哪一批的價值其實很低、該砍或該延後？
3. **`###` 誤判**（見 §0 第一條）依公式算出來的優先序是多少？它現在擋著所有委員輪。
4. **群集 ID 未登記**（§0 第四條）該怎麼處置？它是**每一輪收斂都要配號**的高頻空間。
5. **有沒有票是「該開而未開」的**？依新判準，repo 內有實證失誤但無票對應者。

## 🔴 不受理範圍（標在 `## 逐項核對表` 內，**不要開 `##` 標題**）

1. 重寫既有 38 張票的**票號**——「釘死不動」。
2. B3R 的技術設計——另輪處理。
3. latency 門檻——已三家裁定維持現狀。
4. 措辭／命名美觀——**依新判準本身即不受理**。

## 出場判準

> **38 張全部有裁定 ＋ 每張「實證次數」都附可重跑的查法 ⇒ 本輪完成。**
> 缺任何一張、或有裁定但無證據者，視為未完成。

## 硬性要求

1. **禁改碼、禁改票、禁改 backlog**。只交報告。
2. **rc 一律直接取，禁經 pipe**。
3. 🔴 **禁 `git checkout`／`git restore`／`git clean` 任何 tracked 檔**；誤動請回報。
4. 不要 commit、不要 push；**禁碰 `data_cache/`**。
5. ⚠️ 工作區有未 commit 的 B3 修補且含已知卡死路徑；測大輸入請自行加 `timeout`。

## 產出

38 張的完整裁決表、五個問題的回答、你**建議的執行順序**（含理由）。
收尾清 /tmp workdir（保留 claude-501）。
