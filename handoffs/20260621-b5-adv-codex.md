# B5 adversarial review — Codex — 2026-06-21

## Verdict
需修補後派工。主因不是只有 `_compute_single` threading：BatchGenerateRequest/API/TS/UI 目前也沒有 batch date 欄位，SPEC 把「已從前端收到」當成已驗證事實但程式碼不支持。

## Findings
- BLOCKING High — SPEC §A「BatchGenerateRequest ... 有 start_date/end_date」錯。實碼 `api/models/feature_factory_models.py:176-184` 只有 symbols/timeframe/config/force/max_workers；`frontend/src/lib/types.ts:405-411` 也無 date；UI batch 分支 `frontend/src/app/feature-factory/page.tsx:262-268` 未送 start/end。只修 3 處後端 threading，UI 實測仍全史。
- BLOCKING High — TODO scope 漏 API/TS/UI：需新增 batch request start_date/end_date、前端 batch payload 傳 `startDate||undefined/endDate||undefined`、測 API request model/checkpoint 保存日期；否則 resume 也無日期可重建。證據：checkpoint 用 `request.model_dump()` 保存，`execute_resume` 從 `request_payload` 重建。
- MAJOR High — resume 路徑需明確測，但不一定要額外 runtime 改動。`resume_batch→execute_resume→_run_batch` 會走同一 `_process_item_wave`；若 request_payload 有 date 即可傳遞。TODO 應加 resume date regression，防 checkpoint 仍無 date。
- MAJOR High — 「167天→4009列」表述混淆。4009 對 167d 的 1h native rows 合理；若驗 primary 12h row_count 應約 335。SPEC §V 寫「167天→~4009 1h列」但問題描述又是 primary 12h，整合測若 assert 錯層會假紅/假綠。
- MAJOR Medium — 「列數=選定範圍」不足以證偽 correctness。`_layer0_data_ingestion` 在計算前切窗，會讓 rolling/lag/FracDiff 缺少 window warmup history；日期選擇是「輸出日期」還是「輸入計算窗口」需明確。若使用者只要輸出範圍，保守修法需多取 warmup 前史再裁輸出；若要嚴格只用區間內資料，需文件化首段 NaN/品質差異。
- MAJOR High — mock 同步清單不完整。除 TODO 點名檔，現有 patch/direct 呼叫還有 `test_feature_factory_batch_step4.py`、`test_feature_factory_batch_resume.py`、`test_batch_layer_metrics.py`、`test_batch_progress_normalize.py`、`test_worker_logging.py`、多處 `test_batch_retention.py`。用 `rg "_compute_single"` 全掃並新增 one spy test 驗參數順序。
- MINOR High — config_hash 含 date 屬實：`feature_factory.py:3547-3581` 放 `_start_date/_end_date`；date run 不會污染 no-date cache。既有無-date cache 不會因為修 batch 傳 `None` 失效；有-date run 會生成新 hash，這是預期。

## Assumptions Checked
無 date 行為：若新增欄位 default None 且傳 None，hash 仍 `_start_date=None/_end_date=None`，應與現行 no-date 一致。cache_dir 計算與 date 無關。未跑測試，純 read-only review。

STATUS: DONE
