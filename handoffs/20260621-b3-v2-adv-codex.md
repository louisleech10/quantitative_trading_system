# B3 v2 Adversarial Confirmation — Codex

## Verdict
需小修補後 PASS。v2 的 post-hoc mark + discard 即刪 + real free-space gate 已解 v1 的 4 個 BLOCKING 主方向；殘留是執行語義需再釘死，避免實作者誤把既有 route/前端 action 當可直接複用。

## Findings
1. [FIX][High] BLOCKING#1 已解：v2 不延後 FeatureRegistry.add、browse register、artifact 寫盤；retention 只是 checkpoint/UI 疊加 mark，completed/results/quality/RunManager 今日語義可保持一致。需保留「不改 :606 register 時機」和 flag-off spy。
2. [FIX][High] BLOCKING#2 主體已解：discard 真刪單 run + `shutil.disk_usage` 重評，背壓不再靠邏輯記帳。需補明：decision wakeup 後一定重新讀真實 free bytes；低磁碟+無可 discard pending 時進 hard-pause terminal/observable state。
3. [FIX][Medium] BLOCKING#3 大致已解：crash matrix abc 覆蓋 register→pending、delete→discarded、checkpoint write fail；idempotency key=(symbol,tf,config_hash)合理。需補「reconcile 掃描」演算法：已 registered 未 pending 如何被發現且不重 register。
4. [FIX][High] BLOCKING#4 已解方向：per-item lock/CAS + pending→deciding 占位足以防 retain/discard race。需補 lock scope/key 與 409/no-op 規則，並要求 checkpoint mutation 在同一 critical section。
5. [BLOCKING_TO_FIX][High] `feature_factory_service.delete_run` 目前 registry/artifact 都不存在會 raise KeyError；v2 寫「discard 冪等/delete_run 對已刪 no-op success」不是真實既有語義。修法：batch decision 包裝 delete_run，對同 item terminal discarded 或 KeyError-after-prior-discard 回 success/no-op；不可改公共 DELETE route 語義除非另列 scope。
6. [FIX][Medium] flag-off spy 足夠：byte baseline + spy call count/timing + checkpoint 無 retention 欄 + batch status/browse_task_ids 舊時機，能補 golden 不證時機的洞。
7. [FIX][Medium] retention 5 不變量比 v1 可證偽；仍建議把「retain==今日」明確拆成 registry/browse_task_id/quality summary 三項同一 identity 比對。
8. [FIX][Medium] discard≠deleteRun 前端測試足夠防 UI 誤路由；但後端仍是重用 service.delete_run，文案需明確區分「不呼前端/store deleteRun action」vs「後端可呼 service delete_run」。

## Checks
Read: HANDOFF.md, CLAUDE.md, docs/B3_BATCH_RETENTION_SPEC.md, docs/B3_BATCH_RETENTION_TODO.md, handoffs/20260619-b3-adv-codex.md.
Code spot-checks: api/routes/feature_factory.py:103, api/services/feature_factory_batch_service.py:586/889/1342/1409, api/services/feature_factory_service.py:863, momentum/FeatureEngineering/feature_factory.py:3227.

STATUS: DONE
