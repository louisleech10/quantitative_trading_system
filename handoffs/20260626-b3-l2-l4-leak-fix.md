# B3 L2/L4 leak fix — Codex handoff

## 正在做
- B3 洩漏紅線 round-2：修補 L2 pair-level purge/embargo 與 L4 string sentinel symbol。

## 已完成
- `validate_split_pair_integrity` 改用同 symbol local ordinal 計算 test purge/embargo 禁區。
- `_normalize_symbol_value` 加 `strip()`，拒絕空白與 `nan/null/none/na/n/a` sentinel（大小寫不敏感）。
- 新增反例測試：`test_l2_interleaved_multisymbol_local_ordinal`、`test_l4_string_sentinel_symbols_fail_closed`。
- 新增正例測試：`test_l2_interleaved_multisymbol_far_train_passes`、`test_l4_normal_symbol_with_whitespace_normalizes`。

## 待辦
- 重新三方簽核 B3 後再進 B4。

## 阻塞
- 無。

## 本次決策
- L2 禁區以該 symbol 在 base universe 中的 local ordinal 計算，不再使用全域 row position。
- L4 sentinel normalization 在 contract 層 fail-closed，讓 adapter/split_per_symbol 共用同一規則。

## 踩坑提醒
- `grep -rE 'from api\.' momentum/` 無輸出時 exit code 是 1，代表 0 match。
- 根 `HANDOFF.md` 依執行端合約不覆蓋；本交接寫入 append-only handoff 檔。
