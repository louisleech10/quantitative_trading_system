# R2 Stateful Param Audit — Codex cross-review
Scope: read-only code audit; no code changes. Reviewed merged R1 + Codex/Composer originals.

1) B2 recursive family: classify as D-with-burn-in, not live state, once burn-in is sufficient. Evidence: warmup_table marks EMA/Hilbert/KAMA/MACD all `all_periods_converged: true` with finite factors (`atomic/warmup_table.yaml`:106-177). Wilder examples also converge (RSI lines 277-285; NATR/PLUS_DI lines 232-249). Klinger is EMA(fast)-EMA(slow) over VF (`atomic/volume_indicators.py`:231-241), same convergence logic. Need pin burn-in rule/factor, not carry state.
2) B1 OBV/AD: agree true B/non-convergent absolute level. Evidence: `cumulative_special_cases` says OBV/AD pure cumulative and dataset-start reset (`atomic/warmup_table.yaml`:367-376). ADOSC partly converges only after AD level baseline + EMA settle; safest classification B unless input AD reset is pinned (`atomic/warmup_table.yaml`:377-381).
3) A4 safe_denominator: agree A but low-to-medium severity. Evidence: pandas uses full-column nonzero median then `threshold=scale*rel_eps` (`utils/numeric_guards.py`:41-58); polars same (`polars_adapter.py`:41-61, 340-352). Intended to kill ~1e-14 TA-Lib noise, so serving impact usually near boundary only, but train/serve can differ when median scale shifts. Recommendation: change to causal/expanding/rolling robust scale; do not persist median unless exact historical parity is required.
4) S1/S2 schema/drop gates: agree real risk, best as independent A-schema (not value calibration). Evidence: L3 variance filter uses whole-output nan rate/effective_n/inf/std to omit cols (`operators/rolling_aggregator.py`:799-827, 830-850); L7 dead drop uses full-run `nunique` and valid count (`utils/dead_feature_filter.py`:44-92) applied in frame path (`feature_factory.py`:2691-2728) and CGSA stream path (`feature_factory.py`:3195-3221). S2 low-card skip uses full input `nunique` to skip skew/kurt generation (`operators/rolling_aggregator.py`:128-130, 773-789; emit skip lines 518-533). Missing pin => training feature list and serving feature list drift; model sees missing/extra columns.
5) Exhaustive FF scan: besides A4/A5, found polars L6.5 non-causal fallback full-column winsor sigma/quantile (`polars_adapter.py`:426-443). Main FeaturePreprocessor forces causal True (`preprocessing/feature_preprocessor.py`:148-157; config comment `feature_config.py`:231-235), and normal winsor/rank/zscore/gaussian use rolling paths (`feature_preprocessor.py`:2675-2733, 3371-3410, 3433-3468, 3507-3555). If polars fallback ever runs with `causal_preprocessing=False`, classify A.
6) Exhaustive FF scan also found `_numba_transforms.winsorize_array` full-column quantile (`preprocessing/_numba_transforms.py`:340-369) and non-causal fast sigma mean/std (`_numba_transforms.py`:428-452). These are currently guarded by `causal_preprocessing=True`; keep them unreachable or classify A if re-enabled.
7) No additional `.fit()` in FeatureEngineering production layers. ADF/fracdiff search already covered: `chosen_diff` loop (`feature_preprocessor.py`:3301-3368), non-stationary ADF set (`feature_preprocessor.py`:3582-3615), d* bisection/prior search (`feature_preprocessor.py`:3687-3772; `_hurst_prior.py`:173-323).
8) Analysis/IC/ML layer outside FF has separate stateful preprocessing not in merged doc: `momentum/Analysis/data_preprocessor.py` full-sample time-series zscore/winsor/drop gates (`data_preprocessor.py`:114-138, 151-172) and probability calibrator/model fits are training artifacts. If R3 scope includes IC/ML serving, add separate A/training-artifact section; if FF-only, keep out.
9) A2 IC-First default: dispute merged wording. Base model and scan_config default `adf_differencing.enabled=False` (`feature_config.py`:174-180; `config/scan_config.yaml`:471-477). `professional_full` preset turns ADF+fracdiff on (`config_manager.py`:1047-1096), but service default `get_config/_resolve_config` uses merged config unless explicit `preset` override (`feature_factory_service.py`:3625-3628, 4163-4176), and IC-First consumes passed config (`feature_factory.py`:2043-2063, 2138-2161). Conclusion: IC-First default path does NOT open do_adf; professional_full opens it.
10) A5 labels winsor: agree look-ahead leakage for labels. Evidence: return uses future `close.shift(-horizon)` then clips by full-label-sample quantiles (`labels/label_generator.py`:71-80). It does not leak into online feature serving, but it changes target distribution using future/out-of-sample label tails, so it biases IC/ML evaluation and model selection; it hits ML/backtest-truth risk if used for training/evaluation targets.
11) C1: agree peer dependency, not fit. Evidence: relative price divides by reference close; beta/idiosyncratic momentum use rolling window vs reference returns (`cross_sectional/relative_strength.py`:16-38). Need same reference symbol/time alignment online.
12) D confirmations: feature validator fallback winsor is rolling (`feature_validator.py`:189-214); correlation/coverage metadata do not alter values except optional mean fill path (`feature_validator.py`:285-414`) which should remain non-production or be pinned if used.
13) New items absent from merged doc: E1 polars L6.5 non-causal full-column winsor fallback; E2 numba non-causal/full-column winsor fallback; E3 optional Analysis/data_preprocessor full-sample preprocessing if audit scope expands beyond FF.

ASSUMPTIONS_VERIFIED: checked merged/original audits and grep/read code in atomic/operators/preprocessing/cross_sectional/labels/validation plus Analysis boundary.
TESTS_RUN: read-only audit commands (`rg`, `nl`, `sed`) only; no pytest needed.
FAILURES_SEEN: none.
SCOPE_CHANGES: none; no code edits.
NUMERIC_OR_SCHEMA_IMPACT: none from this audit; findings identify existing A/A-schema risks only.
STATUS: DONE
