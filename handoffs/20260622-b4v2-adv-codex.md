# B4 v2 adversarial confirmation — Codex — 2026-06-22

## Verdict
需修補後派工。v2 已正確收斂為「選項1 bulk reuse delete_run + best-effort report + 孤兒安全網」；v1 多數 blocking 已閉合，但仍有 2 個會讓實作者落錯路徑的規格缺口。

## Confirmed closed
1. v1 #1 閉合方向正確：§A 誠實修正 `deleting` flag 現況；`delete_run` 未 mark、`get/list_all` 不檢是正確事實。v2 要求 delete path mark-deleting + reader 隱藏/標，是必要補強。
2. v1 #2 閉合：不再承諾「交易式」；改成逐 run best-effort、partial 明列 failed、不中斷整批，再用 orphan cleanup 防累積，與現有 `delete_run` 行為相符。
3. v1 #3/#4 下游範圍比例合理：記憶體 cache / 前端 stale 屬重啟或重整可清，不應膨脹成全矩陣；磁碟累積以 orphan cleanup 兜底即可。
4. B3/B4 並發方向正確：retention discard 已 reuse `delete_run`；v2 要求 bulk vs retention discard 走 RunBusyError 或 FSM 更新、同 run 冪等，測試點足夠。
5. HTTP 200 + per-run status 正確：避免 207 client 分歧，前端只需依 body 顯示 deleted/failed/skipped。
6. `d_star` 不清正確：它按 symbol/tf/fracdiff fingerprint 跨 run 共享，列入 §N 是必要且合理。

## Findings
1. [BLOCKING][High] mark-deleting 不可直接沿用現有 `FeatureRegistry.mark_deleting()` 語義。實碼 `feature_registry.py:217-231` 會因 `alias` 或 `batch_alias` 直接 return false，這是 auto-cleanup 保護命名 run 的語義；但 RunManager bulk 是使用者顯式確認刪除，應允許 alias/batch_alias run。SPEC/TODO 目前只說「用既有欄 mark_deleting」，容易導致 aliased/batch runs 被 skipped。修補：新增/指定 delete-path mark helper（例如 allow_named=True 或 mark_deleting_for_delete），並加 alias 與 batch_alias bulk delete 測試。
2. [BLOCKING][High] 孤兒掃描只寫 `features_run_dir` 不足。實碼 `run_lifecycle.py:90-115` 刪 features leaf 後還刪獨立 `cgsa_work_dir`；partial error 最可能留下 features/registry/cgsa 任一不一致。若 orphan cleanup 只比對 registry ↔ features run dir，CGSA-only orphan 或 registry 有但 features 無、CGSA 仍在的狀態可能繼續累積。修補：§A/Task2/§V 明確孤兒 artifact 包含 features leaf + CGSA leaf（以 manifest ownership/config_hash 驗證），兩類孤兒測試需涵蓋 CGSA。

## Residual notes
- active run 排除方向 OK，但實作要以 run lease / active task 判斷；dir 有但 registry 無的 active generation 不可被 b 類清掉。
- get/list hiding deleting 建議區分 public reader 與 internal scan；orphan scan 需要能看到 deleting/partial 狀態。

STATUS: DONE
