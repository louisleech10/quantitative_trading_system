# IC 1e+1b Golden baseline v2 — Codex R2 複驗

**範圍/判準**：逐項重打 `IC1EB-BASELINE-REVIEW-codex.md` 的 BLOCKING/MAJOR 反例，並抽驗 F1–F17 ACCEPT 落地；baseline/`data_cache` 全程唯讀。上游 SPEC stamp：`bash scripts/reconcile_stamps_check.sh handoffs/IC1EB-RECONCILE.md codex,composer` 所對應 sha=`b77932d8…`（SPEC/HANDOFF 已載明）；baseline reconcile 是本次受審標的，非先行採信依據。
**F1/F2 xsec 實際資料集/500 欄 — CLOSED**：`python -c '<reader explicit e53e... + H5/report set replay>'` → common=218300、selected=500、report rows/input=500，report feature set 等於三 symbol 顯式 `e53e22906c...` 交集之 SHA-500；BTC/ETH/BCH 三份 H5 各 500 欄且各自等於同 full config 選集。
**F3 passed-set 假快照 — CLOSED**：`python -c '<reconstruct_passed on long_BTC...e53e report>'` → top-level `passed_features=False`、重建=39=manifest、hash match；將 `output_features += 1` → `AssertionError`。12 顆 manifest 的 stage5 output 與記錄 count 一致（xsec 無 stage5，語意為全輸出 500）。
**F4 row ordering — CLOSED**：`python -c '<two-row five_hash/order mutation>'` → reverse rows 時 five-hash equal=True、`summary_feature_order_sha256` changed=True，原 ordering mutation 已轉紅。
**F4 raw column/schema identity — STILL-OPEN (BLOCKING)**：同命令反轉 raw G1 key order → hash 仍 equal=True；刪掉 `coverage` vs 明列 `coverage:null` → hash 仍 equal=True；manifest 無 raw G1 column-order hash。固定 `reindex(G1_COLUMNS)` 仍會掩蔽欄順序/缺欄，未關閉我 R1 的 column-order/schema 反例。
**F5 strict numeric — STILL-OPEN (BLOCKING)**：`python -c '<coverage mutations through five_hash>'` → `corrupt-A/B` 均 raise `TypeError`（此半閉合），但 `+inf/-inf` 均 ACCEPTED 並產 hash；原要求是 finite-or-NaN gate，非法 infinity 仍可進 Golden。
**F8 NaN canonical — STILL-OPEN (MAJOR)**：同命令造兩個皆為 NaN、bit payload=`...001/...002` → `values_sha256` 分別 `d1fbfea2eb7c.../223d32bc44d0...`、equal=False；缺值未真正 canonical 成單一 NaN bits，語意相同可假紅。
**F6 data_cache 唯讀 gate — STILL-OPEN (BLOCKING)**：現場 `python -c 'data_cache_fingerprint() == manifest.after'` → True，manifest before=after；但 tmp 原反例（同 path/size/mtime，內容 `AAAA→BBBB` 並還原 mtime）→ fingerprint_equal=True。函式只 hash path/size/mtime，未依 R1 方法 hash content，零變化 gate 仍可假綠。
**F7 report/provenance/atomic 子項 — CLOSED**：shell 逐檔 `shasum -a 256`+`stat` 對 manifest → `REPORT_INTEGRITY checked=12 bad=0`；generator hash match=`1a63444d...`，HEAD match=`ce667ba...`，env 版本具備；直接重跑 generator → exit=1 拒覆寫且 staging absent，`os.rename(staging, final)` 路徑成立。
**F7 input/effective provenance — STILL-OPEN (BLOCKING)**：`find .../inputs -type f | wc -l` → 19；`jq/rg input_sha|input_files|effective_config` → 0。任何 premat H5/meta/labels 被換 bytes，manifest 不會轉紅；`request` 還混入 harness-only `expect_scope/expect_rows`，且沒有完整 effective split/feature-filter/predecessor config，未落實 ACCEPT 的 input integrity + complete effective request。
**F9 series hashes — CLOSED**：`jq` 檢查 12/12 entries 都含 `rolling_ic_series/ic_decay/grouped_ic` hash；12/12 亦有 report sha/size 與 removal-reason hash。
**F10 deterministic/family sampling — STILL-OPEN (MAJOR, ACCEPT 走樣)**：程式確為 SHA-256(name)[:500]，但 `jq` → `selected_names=false / selected_names_sha256=false / family_distribution=false`；`materialize_subset()` 回傳 selected 後被 caller 丟棄。裁決要求的完整清單/hash/family histogram 未入 manifest。
**F12 full split-off 真路徑 — CLOSED（truth-preserving）**：`jq` → request 明列 `ic_train_test_split:false`、stage0=500、report `scope=null`/無 split block。reconcile 寫「assert scope=full」未字面落地，但硬塞 full 會竄改舊輸出；request+raw null 足以讓 B5 重放並辨識 full，故此 wording drift 不單獨 BLOCK。
**F13 event 真路徑/α 變更面 — STILL-OPEN (BLOCKING)**：`python -c '<real kline volume quantiles + report replay>'` → artifact q75 精確匹配、n=424、tier=`sufficient`；同一真 kline q95 可得 85 events（落我 R1 30–99 反例帶）。因此雖是真資料，仍未命中 low-confidence α/pass/reason 變更面。
**event `scope=None` 對 G-2 的判斷**：report 內真相為 requested=True、applied=False、reason=`rolling_warmup_insufficient`、test_rows=37<min_test_rows=131、`oos_guarantees=false`、nested scope=`full_sample_legacy`，top-level scope 因舊 fallback 被 pop 才是 None。它可用於「同一 full-sample event universe」的舊新 G-2 診斷，不可宣稱 OOS；B5 必讀 nested split metadata，且目前 sufficient tier 不能替代 low-confidence G-2。
**F14 labels_path return_5 — STILL-OPEN (BLOCKING/ACCEPT 走樣)**：`python -c '<inspect labels H5 + real kline replay>'` → values 確為真 BTC return_5，但 groups 僅 `['BTCUSDT']`（非裁決的 per-symbol/MultiIndex 3sym）；產物只有 `InvalidInputError` expected-raise receipt，沒有實際 xsec report，故不能 assert h=5/maxlags≥4/ICIR order hash。這可作舊 fail-closed receipt，不能充 F14 Golden run。
**F15/F16/F17 抽驗**：generated_at/env/full hashes 均有且 short-hash resolver 已由 full constants 避免；12h pass 現為 22–40，F17「全 0」前提已消失、G-2 pass diff 診斷性反而較好，但 reconcile 要求的 manifest 註記不存在。
**新問題總結**：輸入產物無完整性鏈、manifest `request` 非真 API request、schema missing/null 碰撞、NaN payload 假紅；均由上述實跑 mutation/field absence 證實，非採信文案。
**DATA_CACHE_WRITE**：none；`data_cache_fingerprint()` 現值仍等於 capture-after（但該 fingerprint 的 content-blind 缺陷如上）。
**產出**：`handoffs/IC1EB-BASELINE-REVERIFY-codex.md`（本檔）；未改 baseline、script、SPEC/TODO、根 `HANDOFF.md`。
VERDICT: BLOCK(F4 raw column/schema 仍可碰撞；F5 接受 ±inf；F6 指紋不 hash content；F7 inputs/effective config 無完整性；F13 未覆蓋 low-confidence α；F14 非 3sym labels_path run、僅 expected raise)
