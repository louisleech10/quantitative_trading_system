# B5 v2 adversarial review — Codex — 2026-06-22

## Verdict
需修補後派工。v2 已納入 v1 核心 findings（BatchGenerateRequest 無 date、8 mock、resume、warmup=B6），但 row-count 驗收與 mock 清單仍有會造成假紅/假綠的問題。

## v1 findings 收斂確認
- BatchGenerateRequest 無 date：已納入 SPEC §A/Phase1。
- 8 mock：已納入，但清單含非 batch `_compute_single`，需修。
- resume：已納入 request.model_dump → BatchGenerateRequest 重建。
- warmup 邊界：已明確 B5 strict-window、B6 warmup。

## Findings
- BLOCKING High — row-count 驗收層級仍混淆。SPEC §A 寫「1sym primary 12h native_rows=4009」，§V/Task2 又寫「167天→~4009列 1h」。4009 是 1h strict rows，不是 12h primary output；若測錯 artifact 會假紅/假綠。修：明確 assert 對象（primary 12h output 約 335；native 1h ctx 約 4009；全史 20352 是 1h）。
- MAJOR High — 8 mock 清單不實。`rg _compute_single` 顯示實際 batch service affected files 為 7 個：step4/resume/retention/layer_metrics/progress_normalize/worker_logging/multi_symbol_ic_first；`tests/test_multi_window_rolling.py` 只是 `_compute_single_window_reference`，非 service mock。修：改成 rg-derived 清單，不要硬寫 8。
- MAJOR Medium — 前端有兩套 `/batch` API surface。page.tsx 用 `useFeatureFactory.startBatchGeneration(request)`；store `featureFactoryStore.startBatchGeneration(symbols,timeframe,config,options)` 也會 POST `/batch` 但 options 無 date。SPEC 只寫「相關 store/api」，vitest 2 案例未指定兩者都測。修：明確改/測 hook request interface + store positional API，或證明 store path dead code 並移出 scope。
- MINOR Medium — resume 測試只點名 resume_batch，實碼另有 disk-backpressure wakeup 也從 checkpoint request_payload 重建。共享 request_payload 理論可過，但應在 TODO 註明此路徑 covered by same invariant 或加薄測。

## Notes
- config_hash 含 `_start_date/_end_date` 屬實；None 不污染舊 feature cache。
- B5 不做 warmup 的邊界清楚，未發現偷做 B6。
- 舊 checkpoint 無 date → Pydantic Optional default None 的相容方向正確。

STATUS: DONE
