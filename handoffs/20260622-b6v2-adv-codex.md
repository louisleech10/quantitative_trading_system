# B6 v2 adversarial confirmation — Codex
Date: 2026-06-22 | Mode: read-only docs/spec review.

Verdict: 需修補後派工。v2 已採 Option1, 不承諾 byte parity, 但仍有 2 個需補洞風險。

Closed from v1:
1. 多 TF per-TF warmup: CLOSED. SPEC/TODO 明定次 TF 用 primary ingest_start 反推 source 時間跨度, 不共用 primary bar 數; 對齊後 trim。
2. 多 persist trim choke: CLOSED. 覆蓋 normal L7, CGSA _layer7_raw/_validate, multi-TF primary_raw, IC-first raw+processed, manifest/registry/browse/checkpoint。
3. parity 例外表: CLOSED. cumulative, fracdiff d*, ADF order, post-IC/API rank-zscore, labels horizon 已列為排除。
4. 驗證契約: MOSTLY CLOSED. 主驗收改品質增益, allclose 降為輔助非阻塞, 加因果/自洽/不外露/flag-off golden, 用真實 kline 防假綠。
5. labels end-scope: CLOSED. end+horizon 明確 out-of-scope, 尾 NaN metadata, labels 不外露。
6. flag 關 strict: FUNCTIONALLY CLOSED. 預設關=B5 strict, golden check; 但 env flag 名/是否入 hash 仍未具體。

Findings:
1. [MAJOR] max_warmup 清單漏 L5 cross-sectional。現有 L5 `RelativeStrengthProcessor.compute_beta(... window=60)` rolling 會影響 start 前段; SPEC Task1.1 只列 L1/L2/L3/L4/L6.5/native/validator, 未列 L5/reference symbol warmup 或明確排除證據。
2. [MAJOR] L6/meta 與 advanced atomic windows 邊界需更精確。SPEC 未列 L6; code 中 L6 本身多為 L1/raw 派生, 但 L1 advanced microstructure/entropy/tail_risk 有獨立 windows/hurst/perm/mdd 等來源, Task1.1 的「L1 warmup_lookup」可能無法覆蓋 config-driven advanced windows。
3. [MINOR] flag contract 仍欠機械細節: env var 名、default、config_hash/cache 行為、strict checkpoint 與 warmup checkpoint 相容策略測試。
4. [MINOR] warmup_insufficient API/UI 欄位仍只寫「明定欄位名」, 尚未在 SPEC/TODO 列 exact schema keys/test selectors; 可在派工前補一行固定契約。

Recommendation: 補 Task1.1 的 L5/L6/advanced atomic source checklist + flag/schema exact fields 後可派工; 不需重寫 Option1 驗證框架。
Tests run: static read-only review only (`sed`, `nl`, `rg`); no pytest.
STATUS: DONE
