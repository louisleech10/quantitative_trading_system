# B1 Worker Logging SPEC/TODO Adversarial Review — Codex
SPEC: `docs/B1_WORKER_LOGGING_SPEC.md`; TODO: `docs/B1_WORKER_LOGGING_TODO.md`; sources read: `api/core/logging.py`, `api/services/feature_factory_batch_service.py`.

## Verdict
需修補後派工。方向合理,但 root handler 去重/衝突、append 原子性驗證、fail-open/byte/env 邊界需更精確,否則易假綠。

## Findings
1. MAJOR High — 未驗證假設:「worker 掛 root FileHandler 不會重複」。SPEC Task2.1 寫「掛 root」,但未要求 idempotent marker/同 path 去重。若 `_compute_single` 同 worker process 多次呼叫或 future reuse,root 會累加 handler,造成重複 log。需要求 helper 檢查既有 worker handler(path+pid/sym/tf 或 marker)並測連續 init 兩次只寫一行。
2. MAJOR High — root handler 衝突邊界不足。`setup_logging` 會清 root handlers並掛 console+TimedRotating file(`api/core/logging.py:51-100`),TODO 只說掛 root,未說 worker helper 不得清除既有 handler、不得改 root level、不得影響 caplog/第三方 handler。需測:已有 StreamHandler/pytest caplog/父 handler 存在時新增 worker handler不移除、不重複、不改其他 handler level。
3. MAJOR Medium — non-rotating FileHandler 避免 rotate 競態成立但不等於多進程行原子。SPEC 原文「FileHandler non-rotating」+ smoke「短行」不足以證明 logging handler 實際每 record 單次 append write、encoding/flush 不拆行。需 smoke 用真正 `logging.FileHandler` 從 1-4 process 並發各寫唯一 JSON line,每行可 json parse、id set 完整、無 duplicate/partial;行長至少覆蓋實際 formatter+context長度。
4. MAJOR High — fail-open 驗收不夠可證偽。TODO 只寫「全包 try/except」,未要求注入 `FileHandler`/formatter/path mkdir 失敗並確認 `_compute_single` 仍呼叫 `factory.generate_features` 且生成例外仍照拋。需新增兩測:logging setup raise→成功結果照返;generate_features raise→仍回原 compute failure,不被 logging except 吞掉。
5. MAJOR Medium — byte 不變 gate 有方向但缺局部防護。SPEC/TODO 要跑 golden `--check`,但未要求比較實作 diff 不碰 generation call args/cache path/config_override 或禁改 `factory.generate_features` 區塊。因本批聲稱純 logging,TODO 應限制只改 env 設置、helper、worker入口呼叫、測試;若動 `_compute_single` 生成參數需 BLOCK。
6. MAJOR High — env 未設/還原邊界少一個新 env 的明確測試。現有程式在 wave 前保存/還原 `FFACT_CHILD_METRICS_PATH`/`FFACT_LAYER_METRICS_PATH`(`feature_factory_batch_service.py:455-491`);TODO 要仿照但驗證只寫 `worker_log_env`。需明列 `FFACT_API_LOG_PATH` previous None/value 兩種 restore,ProcessPool 建構/submit 失敗也還原。
7. MINOR Medium — TODO 路徑行號有漂移/不精確。SPEC 指 `_compute_single` :1129,實際為 `api/services/feature_factory_batch_service.py:1146`;不阻塞,但派工 prompt 應用完整路徑避免執行端找錯檔。

## 被當成事實的未驗證假設
- 「子進程不繼承父 root file handler」是 SPEC 已宣稱實測,本次未重跑,只以文件/程式碼採信;若作 gate,需附實測命令輸出。
- 「non-rotating FileHandler 足以支撐同檔 append 無破行」目前是 assumption;需用真 logging 多進程 smoke 轉成 fact。

STATUS: DONE
