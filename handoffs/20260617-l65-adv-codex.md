## Verdict：需修補後派工

## Findings
- [BLOCKING][High] §0 前提錯置：causal「讀取端強制」只指定 `FeaturePreprocessor.__init__`。證據：SPEC Task1.1「feature_preprocessor.py:147」；但程式仍有 `transform_context.get("causal_preprocessing", True)` 傳入 fast/gaussian 路徑(1759/1799/1811/1885/1894/2048/2057)。失敗：外部 False 可繞過建構子釘死，fast path/registry/shard 仍非 causal。修法：SPEC/TODO 明列所有 transform_context 讀取端改為 self.causal_preprocessing/True，並加測 fast+registry/shard False。
- [BLOCKING][High] §0/§8 metadata 與 grep 驗收互斥。證據：SPEC Task2.1「metadata `ic_first_pipeline` 欄保留為 True 常數」；Task2.2「grep `ic_first_pipeline...` → 0」；TODO 同時要求保留 :2167/:2420 與 grep 0。失敗：實作者無法同時滿足，可能刪 metadata 破 batch/API，或保留後 gate 永遠 fail。修法：定一個策略：保留 metadata 則 grep 排除 metadata schema；或改名/移除並同步 batch/API/TS contract。
- [MAJOR][High] §2 漏端到端 API/schema contract。證據：TODO Task2.3 只列「frontend 3 檔」；實際 grep 有 `api/services/feature_factory_batch_service.py:660` 與 `frontend/src/lib/types.ts:277`。失敗：前端不送欄位但 API/batch 還讀舊欄位，或舊 request/localStorage 進 API 後行為不一致。修法：TODO 加 API request/config contract 測試與舊 payload ignore 測試。
- [MAJOR][High] §9 防假綠不足：大量既有測試直接斷言 legacy/env/False 行為，但 TODO 只籠統說「改寫/移除」。證據：rg 命中 `test_ic_first_pipeline.py`、`test_multi_symbol_ic_first.py`、`test_failopen_matrix.py`、`tests/momentum/test_feature_preprocessor.py`、`test_l65_v2_transforms.py`。失敗：執行端可刪整批測試換綠，或只改 smoke。修法：列測試清單與每類新斷言；禁止刪除除非附等價新測試。
- [MAJOR][Medium] §G Golden 來源不可復現。證據：SPEC「baseline 存 `tests/golden/l65_hardening/...`」但未指定 builder script、命令、config freeze、row index 抽樣規則。失敗：byte 一致 gate 由實作者臨時產 baseline，自我比對假綠。修法：先建 read-only baseline builder/manifest；記錄 git SHA/config hash/symbol/tf/row ids/feature ids。
- [MAJOR][Medium] §3/§7 cache 正確性漏 causal 釘死後舊 False cache。證據：SPEC 只說 d_star_cache fingerprint 含 `calibration_bars`，TODO 未要求 invalidation；現有 cache key 含 `causal_preprocessing`。失敗：舊 False cache/metadata 被讀或 golden 混入，造成不可解 diff。修法：明列 cache version/key 期望：False 外部輸入也必 canonicalize 為 True，並測舊 False cache 不命中。
- [MAJOR][Medium] §6 OOM/並行驗證薄弱。證據：L6.5 是 CGSA/batch/shared hot path；TODO B2 驗證只有 grep/npm/G-1，未含 CGSA/sharded/registry 路徑。失敗：legacy 移除後單 symbol 小 baseline 綠，但 batch2d/CGSA/native/shard 路徑壞。修法：加最小 CGSA + batch_service + multi-symbol 真 kline smoke，含 memory/error classification。
- [MINOR][High] TODO 內部殘留錯誤 task 編號。證據：Task1.1「留 Task 4.1」但實際是 Task3.1。失敗：派工閱讀混亂。修法：改為 Task3.1。
- [MINOR][Medium] §4 quant 假設：IC-First 行為不變只比 3 symbols×2 tf。證據：§G symbol 集合 BTC/ETH/ADA, tf 1h/4h；HANDOFF 實測 cache 有 10 symbols×3 tf。失敗：12h 或非抽樣 symbol 的 native alignment/leakage 漂移漏掉。修法：至少三方簽核用 10×3；fast golden 可 3×2 但不可代表資料正確。

## 十類必查
1. 矛盾/互斥：有，metadata 保留 vs grep 0；Task4.1 殘留。
2. 漏項/端到端：有，API/schema/batch/CGSA/shard 未完整落點。
3. 不可測驗收：有，baseline builder/抽樣規則缺失。
4. 可疑 quant 假設：有，3×2 golden 代表性不足。
5. 過度工程：無，移 legacy/causal 釘死符合風險。
6. OOM/並行：有，batch/CGSA/shard gate 不足。
7. Cache 正確性：有，舊 False cache canonicalization 未定。
8. API/型別/相容：有，metadata/config/TS 策略未定。
9. 測試品質：有，既有測試改寫規格不足。
10. Agent 可執行性：部分不足，矛盾驗收會卡派工。

## 被當成事實的未驗證假設
- 「讀取端強制 causal=True」等於只改 __init__：程式證據顯示多個 transform_context 讀取端仍可接 False。
- 「舊 config dict 帶 ic_first_pipeline extra=allow 忽略不報錯」：PreprocessingConfig 本身未見 model_config extra=allow，只有 FactoryConfig extra=allow；需實測 nested extra。
- 「保留 metadata ic_first_pipeline 不影響移除欄位/env」：未驗證下游 batch/API/前端是否把 metadata 與 config 欄位分清。
- 「test_l65_parallel clean tree 已 FAIL」：TODO 當事實使用，但本 review 未見附當次命令輸出；需在派工前重跑標記。

STATUS: DONE
