# 20260711-IC1EB-B3-FIX1

**Agent**: Grok | **Task**: IC1EB-B3 FIX1 (CODEX FINDING-1 in-frame return_N)

## 正在做 / 完成
- 修 in-frame 候選：`return_1` 硬編 → 一般 `return_N`（regex `return_(\d+)`）
- 優先序：label > return_N > future_return > target > y（維持既有，return_1 位泛化）
- 多欄：N 最小；同 N 字典序第一
- 測試 +3（return_5 反例、多 return_N、label 優先）

## 待辦
- 編排端接回 code review（非作者）確認 FINDING-1 CLOSED

## 阻塞
- none

## 本次決策
- 最小改動：不重排候選優先序，只把 return_1 槽泛化為 return_N
- 多 return_N 取 N 最小（相容舊 return_1 行為）

## 踩坑提醒
- labels_path 分支已正確，勿動；in-frame 才是缺口
- 多 return_N 時未選中的 return_* 會當 feature 進 IC（既有「非 label 欄=feature」語意）

## 產出
- `handoffs/IC1EB-B3-IMPL-RESULT-FIX1.md`
- receipts: VERIFY:ic1eb-b3-fix1-full-gate, VERIFY:ic1eb-b3-fix1-t31-unit

STATUS: DONE
