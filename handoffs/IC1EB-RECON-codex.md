# IC1EB-RECON-codex — 2026-07-09
1. p-value 鏈路：`compute_ic_statistics` 生產 `t_stat/p_value/ci_lower/ci_upper/n_observations`，但 stage5 summary 只消費 `p_value`；receipt: `nl -ba momentum/Analysis/statistical_validator.py | sed -n '24,138p'` => 24-32/95-128；`nl -ba momentum/Analysis/ic_filter_orchestrator.py | sed -n '2236,2294p'` => 2254/2268-2282；`sed -n '2521,2605p'` => 2546/2590-2593。
2. caller/map：生產 caller 只有 `_stage5_statistical_validation`，測試 caller 另有 validator tests；`apply_significance_filter` 無生產 caller；`adjust_multiple_comparisons` 無生產 caller；receipt: `rg -n "compute_ic_statistics\\(|apply_significance_filter\\(|adjust_multiple_comparisons\\(" momentum api frontend/src tests` => orchestrator 2254 + tests only for latter two.
3. rolling_ic 結構：`compute_rolling_ic` 回 `feature -> window_N -> list[IC]`，一 feature 多窗，stride=1 預設造成相鄰 rolling window 高度重疊；`_collect_values` 對 dict windows 依 key 排序後 extend 成單一樣本列再 t-test；receipt: `nl -ba momentum/Analysis/ic_engine.py | sed -n '269,303p'` => 298-302；`nl -ba momentum/Analysis/statistical_validator.py | sed -n '75,83p'`; python 實測 `compute_rolling_ic(...,[3,5],1)` => `{'f1': {'window_3': 8, 'window_5': 6}}`; `_collect_values({'window_3':[1,2],'window_5':[3,4]})` => `[1.0,2.0,3.0,4.0]`。
4. report/frontend schema：JSON report 直接放 `summary_table`；longitudinal summary 無 `t_stat/ci_*`，cross-sectional summary 有 `t_stat` 且 `p_value=None`；frontend type 讀 `p_value`、optional `t_stat`、無 `ci_lower/ci_upper`，UI longitudinal 顯示 p-value，cross-sectional 由 `ic_mean/icir/sample_size` 推導 t-stat/CI；receipt: `nl -ba momentum/Analysis/ic_reporter.py | sed -n '29,54p'`; `nl -ba momentum/Analysis/ic_filter_orchestrator.py | sed -n '1080,1150p'`; `nl -ba frontend/src/lib/types.ts | sed -n '1994,2006p'`; `nl -ba frontend/src/components/ic-analysis/ICSummaryTable.tsx | sed -n '75,137p'` and `360,444`。
5. rolling IC 參數/算法：schema 與 ICEngine defaults 都是 windows `[21,63,126]`, stride `1`, icir window `63`, reference_tf `12h`; `_rolling_corr_matrix` 用 cumulative sums 對每個 start/end rolling 相關，NaN for invalid；`_select_icir_series` 優先 `window_63` 否則第一個 window；receipt: `nl -ba momentum/Analysis/ic_config_schema.py | sed -n '64,75p'`; `nl -ba momentum/Analysis/ic_engine.py | sed -n '59,70p'` and `1006,1014` and `1287,1327`; python `load_ic_config()` => rolling_windows `[21,63,126]`, rolling_stride `1`, icir_window `63`, reference_tf `12h`。
6. autocorr：`compute_ic_autocorrelation` 只在 stage4 放進 `ic_results["ic_autocorr"]`，stage7 report 不輸出它，feature tier 有 UI toggle 但無 config plumbing；現況是診斷/快取內資料，不修正 p-value；receipt: `nl -ba momentum/Analysis/ic_engine.py | sed -n '442,464p'`; `nl -ba momentum/Analysis/ic_filter_orchestrator.py | sed -n '2180,2232p'`; `rg -n "ic_autocorr|ic_autocorrelation" momentum api frontend/src tests` => report only `rolling_ic_series`, no `ic_autocorr` report field。
7. n_tests 現實/FDR 互動：feature manifest 實測單次 run feature 數 161k-437k；stage5 對 `features_df.columns` 全列檢定，`FeatureFilterSchema.max_features` default None；event low_confidence 產生 `adjusted_p_threshold=0.10` 並由 stage5 覆蓋 `p_value_max`；FDR 應在同一 evaluated feature universe 上產生 adjusted p/q 後再與有效 alpha 比較，low_confidence alpha policy 需明確定義先後；receipt: python manifest scan => BTCUSDT 1h `437066`, 12h `218369/161031`; `nl -ba momentum/Analysis/ic_config_schema.py | sed -n '161,169p'`; `nl -ba momentum/Analysis/event_filter.py | sed -n '128,144p'`; `nl -ba momentum/Analysis/ic_filter_orchestrator.py | sed -n '2254,2261p'`。
8. SelectionScope/FDR：`SelectionScope` 欄位為 `scope_id/universe_features/split_label/evaluated_features/n_tests/method/base_universe_hash`，驗證 split label、subset、n_tests；既有測試鎖 fields/mismatch；手刻 BH 與 statsmodels 0.14.6 對照 allclose True；receipt: `nl -ba momentum/core/contracts.py | sed -n '723,743p'`; `nl -ba tests/momentum/core/test_scope_contract.py | sed -n '10,64p'`; python `multipletests(..., method='fdr_bh')` => custom `{'a':0.04,'b':0.04,'d':0.05333333333333334,'c':0.5}`, statsmodels same, `allclose True`。
9. FDR 幽靈開關：backend `ICConfig` 無 fdr/multiple comparison 欄；frontend store presets 有 `fdr_correction`，FeatureTierPanel 顯示 L3 toggle，但 `getEffectiveConfig` 只送 stage/module overrides，不送 fdr；`buildConfigOverride` 只送 thresholds/redundancy/labels；receipt: `rg -n "fdr|multiple_comparison|adjust_multiple_comparisons" momentum/Analysis/ic_config_schema.py momentum api frontend/src tests`; `nl -ba frontend/src/store/icAnalysisStore.ts | sed -n '57,135p'` and `290,325`; `nl -ba frontend/src/hooks/useICAnalysis.ts | sed -n '32,52p'` and `269,281`; `nl -ba frontend/src/components/ic-analysis/FeatureTierPanel.tsx | sed -n '18,43p'`。
10. HAC 工具/lag 來源：venv 有 statsmodels 0.14.6；1-align resolver `_resolve_effective_label_horizon(config, labels_df)` 與 `_resolve_label_horizon_from_column(name, config)` 已存在，labels 欄名優先；overlapping label horizon h 可作 Newey-West lag 下限/來源（需 SPEC 定公式）；receipt: python `import statsmodels; print(statsmodels.__version__)` => `0.14.6`; `nl -ba momentum/Analysis/ic_filter_orchestrator.py | sed -n '188,243p'`。
11. bootstrap 工具：現有 `BootstrapEstimator.bootstrap_confidence_interval(y_true,y_pred_proba,metric,n_bootstrap,confidence,random_state)` 是 i.i.d. resample classification metrics，無 block bootstrap/IC series 介面；可重用 dataclass/CI pattern，不宜直接套用 rolling IC block bootstrap；receipt: `nl -ba momentum/Analysis/bootstrap_estimator.py | sed -n '19,109p'`。
12. deep path：`FactorReturnAnalyzer.compute_factor_returns` hardcode `"newey_west_adjusted": False`; caller 是 deep module `_run_factor_return`，受 factor_return tier/module enable 控制，frontend `FactorReturnChart` 只畫 `quantile_returns_summary`，不讀 NW flag；此屬 deep analysis module，是否入本刀 scope 取決於 SPEC 是否把 IC 顯著性正確化擴到 factor return 顯著性標記；receipt: `nl -ba momentum/Analysis/factor_return_analyzer.py | sed -n '25,104p'`; `nl -ba momentum/Analysis/ic_filter_orchestrator.py | sed -n '1431,1450p'` and `1572,1578`; `nl -ba frontend/src/components/ic-analysis/FactorReturnChart.tsx | sed -n '13,21p'`。
13. 施工面預估：後端需動 `statistical_validator.py`, `ic_filter_orchestrator.py`, `ic_config_schema.py`, `momentum/core/contracts.py`/artifact schema 視 schema 決策，相關 tests；前端需動 `types.ts`, `icAnalysisStore.ts`, `useICAnalysis.ts`, `FeatureTierPanel.tsx`, `ICSummaryTable.tsx` 視 UI 顯示 q/HAC CI；風險為多窗 pooling 行為改變、SelectionScope/n_tests 定義、low_confidence alpha 與 FDR 先後、report/Parquet schema 相容、cross-sectional p/t-stat 分支、deep NW flag scope。
ASSUMPTIONS_VERIFIED: current HEAD only; no production/test/doc edits; true caller graph checked by rg; statsmodels import/version checked in venv; true feature magnitude checked from data_cache manifests; FDR numeric correctness checked against statsmodels.
TESTS_RUN: `sed -n '1,220p' HANDOFF.md`; `sed -n '1,260p' CLAUDE.md`; multiple `rg`/`nl -ba ... | sed ...` receipts above; python snippets for statsmodels/FDR/config/rolling_ic/_collect_values/manifest counts all completed; no pytest run because recon-only.
FAILURES_SEEN: `rg ... pyproject.toml setup.cfg` returned missing-file warnings for absent optional files; HDFStore probes on three `.h5` paths returned empty keys; neither affected conclusions because manifest counts and code receipts were available.
SCOPE_CHANGES: none; only wrote `handoffs/IC1EB-RECON-codex.md`; did not modify root `HANDOFF.md`, production code, tests, docs, git history, or data_cache.
NUMERIC_OR_SCHEMA_IMPACT: none from this recon; proposed implementation would affect p-value semantics and likely add adjusted p/q/HAC fields unless SPEC chooses internal-only behavior.
STATUS: DONE
