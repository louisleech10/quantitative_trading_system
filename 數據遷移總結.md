# 數據遷移總結報告

生成時間：2025-11-09

---

## 執行摘要

已完成數據遷移驗證流程，發現舊系統HDF5檔案存在**numpy版本兼容性問題**，無法直接遷移。

**建議方案：放棄遷移舊檔案，直接使用批量下載功能從Binance重新下載所需數據**

---

## 遷移結果統計

### 檔案掃描
- **總檔案數**: 431個舊HDF5檔案
- **位置**: `data_cache/hdf5_cache/*.h5`
- **總大小**: 約49 MB

### 遷移狀態
| 狀態 | 數量 | 說明 |
|------|------|------|
| ✓ 已存在於新系統 | 3 | BTCUSDT 12h (×2), ETHUSDT 12h |
| ✗ numpy兼容性錯誤 | 428 | 無法讀取，需重新下載 |
| ✓ 導入成功 | 0 | - |
| ✗ 連續性失敗 | 0 | - |

---

## 核心問題：numpy 版本兼容性

###  錯誤信息
```
Failed to read legacy cache: numpy.dtype size changed, may indicate binary incompatibility.
Expected 96 from C header, got 88 from PyObject
```

### 原因分析
舊HDF5檔案是用**不同版本的numpy**創建的：
- **舊版numpy**: dtype size = 96 bytes（當時創建檔案）
- **當前numpy**: dtype size = 88 bytes（當前環境）
- **結果**: pandas.read_hdf() 無法讀取舊檔案

### 為什麼不能簡單修復？
1. **降級numpy風險**: 可能破壞其他依賴（pandas, TA-Lib等）
2. **轉檔複雜**: 需要建立舊環境 → 讀取 → 匯出 → 新環境匯入
3. **數據品質未知**: 舊檔案是否包含缺口（如2023-03-24 Binance維護）無法確認
4. **時間成本**: 處理431個檔案的兼容性問題 vs 批量下載

---

## 已完成的改進

### 1. 增強連續性驗證 ✅
**檔案**: `momentum/DataExtraction/kline_storage.py` (lines 501-511)

**修改內容**:
```python
# 在 _import_from_legacy_cache() 方法中加入連續性驗證
try:
    self._validate_continuity(df_formatted, symbol, timeframe)
    logger.info(f"Legacy data continuity validation passed for {symbol}/{timeframe}")
except ValueError as e:
    logger.error(
        f"Legacy data {legacy_file} has continuity issues and cannot be imported.\n"
        f"Error: {e}\n"
        f"Recommendation: Use batch download to fetch fresh data from Binance instead."
    )
    return False
```

**效果**:
- 導入前先檢查數據連續性
- 有缺口的數據拒絕導入
- 提示使用批量下載替代

### 2. 創建遷移驗證腳本 ✅
**檔案**: `數據遷移驗證.py`

**功能**:
- 掃描所有舊版HDF5檔案
- 嘗試導入並驗證
- 生成詳細報告

### 3. 生成遷移報告 ✅
**檔案**: `遷移報告_20251109_140915.txt`

**內容**:
- 導入成功/失敗統計
- 詳細錯誤列表
- 下一步建議

---

## 建議解決方案

### 🎯 推薦方案：使用批量下載重建數據

#### 為什麼推薦？
1. ✅ **數據品質保證**: 從Binance API直接獲取，確保準確性
2. ✅ **自動缺口處理**: 已實現的 `KNOWN_DATA_GAPS` 機制會跳過2023-03-24維護期
3. ✅ **連續性保證**: 批量下載包含自動連續性驗證
4. ✅ **格式統一**: 所有數據都使用新HDF5格式，避免兼容性問題
5. ✅ **簡單高效**: 一次性批量下載，無需逐個檔案處理

#### 執行步驟

**Step 1: 確認需要的交易對和時間框架**
```python
# 從舊檔案列表提取symbol
# 431個檔案 → 約431個 symbol/timeframe 組合
# 主要是 12h 時間框架
```

**Step 2: 使用批量下載API**
- 通過前端批量下載介面
- 或使用 `api/services/batch_download_service.py`
- 選擇需要的symbol和時間範圍

**Step 3: 驗證下載結果**
```bash
python3 "數據遷移驗證.py"
# 重新運行驗證腳本檢查新數據
```

#### 預估時間
- **批量下載**: 約2-4小時（視symbol數量和網速）
- **驗證**: 約10-15分鐘
- **總計**: < 1工作日

---

### ⚙️ 備選方案：修復numpy兼容性（不推薦）

#### 方法
1. 建立舊環境（舊版numpy）
2. 讀取舊HDF5 → 匯出CSV
3. 新環境讀取CSV → 寫入新HDF5
4. 驗證連續性

#### 缺點
- ⏰ 時間成本高（431個檔案）
- ⚠️ 數據品質無法保證（可能含缺口）
- 🔧 環境管理複雜
- 💾 需要臨時存儲空間（CSV中間檔案）

**不推薦原因**: 投入產出比低

---

## 當前系統狀態

### 新HDF5存儲系統 ✅
**檔案**: `data/kline_storage/kline_cache.h5`

**已包含的數據集** (3個):
- BTCUSDT 12h (legacy_cache)
- ETHUSDT 12h (legacy_cache)

**功能特性**:
- ✅ 統一HDF5格式（單一檔案，多symbol/timeframe）
- ✅ 連續性驗證（零容忍政策）
- ✅ ACID事務保證
- ✅ 自動缺口偵測
- ✅ 已知Binance事件排除(2023-03-24)

### 舊系統檔案
**位置**: `data_cache/hdf5_cache/`
**狀態**: 428個檔案無法讀取（numpy兼容性問題）
**建議**: 保留作為備份，但不進行遷移

---

## EMA計算驗證 ✅

### 業界標準確認
經過深入研究，確認：
- ✅ 當前實現使用 **TA-Lib** (SMA初始化)
- ✅ 符合業界標準（Binance, QuantConnect, Backtrader）
- ✅ 已通過PHASE3驗證（誤差 < 1e-8）

**結論**: EMA計算無需任何修改

---

## 下一步行動計劃

### ✅ 已完成
1. [x] 增強 `_import_from_legacy_cache()` 連續性驗證
2. [x] 創建遷移驗證腳本
3. [x] 運行驗證並生成報告
4. [x] 分析結果並提出建議

### 📋 待執行（根據您的決定）

#### 選項A：使用批量下載（推薦）
1. [ ] 準備需要下載的symbol列表（從431個舊檔案提取）
2. [ ] 使用批量下載功能從Binance重新下載
3. [ ] 驗證下載結果（運行驗證腳本）
4. [ ] 創建舊檔案備份
5. [ ] 刪除舊 `data_cache/` 目錄
6. [ ] 移除 `_import_from_legacy_cache()` 相關代碼
7. [ ] 更新文檔標記遷移完成

#### 選項B：修復numpy兼容性（不推薦）
1. [ ] 建立舊環境（匹配舊numpy版本）
2. [ ] 批量轉檔腳本（HDF5 → CSV → 新HDF5）
3. [ ] 驗證所有轉檔數據
4. [ ] 清理中間檔案

---

## 附件

- `數據遷移驗證.py` - 自動化驗證腳本
- `遷移報告_20251109_140915.txt` - 詳細驗證結果
- `momentum/DataExtraction/kline_storage.py` - 已增強連續性驗證

---

## 結論

**建議採用選項A（批量下載）**，原因：
1. ✅ 簡單、可靠、高效
2. ✅ 數據品質有保證
3. ✅ 避免兼容性問題
4. ✅ 已有完善的批量下載基礎設施

**舊檔案處理**:
- 保留作為歷史備份（tar.gz壓縮）
- 不強制遷移
- 新系統從Binance獲取所有數據

---

**報告生成時間**: 2025-11-09
**執行人員**: Claude Code
**狀態**: ✅ 驗證完成，等待決策
