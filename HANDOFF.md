# HANDOFF

**當前任務**：GAP-3 事件型 UAT 缺口修補 SPEC（`docs/GAP3_EVENT_UX_SPEC.md`）審到 **完整 FROZEN**。
**工作方法**：先讀 `docs/GAP3_EVENT_UX_ROLE_CARD.md`。
🔴 **開工前必讀角色卡兩條最高位階條款**：①R20「主委不得再新建任何驗收機制」②R21「條件②′」。

## 狀態（R21 已全數落地並收尾）

- SPEC **3,246 行／42 Task**（二十二輪未增未減）；版本行 = `R21-landing`，狀態 **未 FROZEN，待 R22**。
- R21：14 findings（codex 7／composer 3／grok 4）→ 7 群集，**全數落地**；三家 Verdict 一致「需修訂後定版」。
  **P0＝0（連七輪）**、**(N)＝0（連七輪）**。主委自傷 R19 5 → R20 6 → R21 **7 群**。
- 🔴🔴 **裁定一：條件② 不可達 ⇒ 替換為 ②′**（兩家；主委於 brief 主動提出）。
  原條件「本輪自傷絕對數＝0」自 R4 至今一次未達，composer 判「不可達＝永久否決權」、
  grok 判「近永久否決」。**②′＝三款同時**：(1) 自傷 P0＝0 (2) 自傷群集數連兩輪不增
  (3) 不含「新建驗收機制」類。**條件① 未動。** ⚠️ **流程門檻變更，已向使用者明示可否決。**
- 🔴 **裁定二：R20 新條款生效後首次覆核，主委兩處自我歸類皆被推翻**——
  AST 斷言不屬 (b)（降級 `待裁定`）／manifest `is_digest_entry` 不屬 (c)（刪除該欄）；
  Rule 3 適用，未實跑之驗收已標「待裁定」。
- 🔴 **purge 綁定第五次被打破**：R20 之「沒有任何宣稱可錯」為誇大——`TypeError` 只擋缺參，
  傳齊 kwargs 卻傳 module 全表 ⇒ purge 靜默翻倍。已撤回並補常設 mutation ⑤。
- 🔴 **主委在寫下「撤回要 grep 所有同義字面」之下一輪又犯同一個錯**（兩處 active 殘留）。

## FROZEN 條件現況

①⬜ P0=0／**P1=14**　②′⬜ 自傷 7 群（連兩輪未下降，(3) 本輪成立）　③✅　④當輪 locus 待清

## 下一步（依序）

1. **派 R22**：由 r21 三份改號生成 brief/facts/locus。
   🔴 **改號時必查 `locus.sh` 之 PATCHES glob 有沒有跟著換**（R17 踩過）。
   🔴 **R22 brief 必含**：①**裁定角色卡 (a)(b)(c) 封閉集合之缺口**（CODEX-R21-P2-01；
   純刪除／定義澄清／字面更正／撤回四類皆不在集合內，主委不得自行升格——**這是 R22 待裁項**）
   ②覆核本輪之降級與刪除是否徹底 ③再攻擊 purge 錯 map 之 (d-3) 防護
   ④各家重寫自家 R21 SYNC-LOCI。
   `--task-id` **須為 session 之大寫形式**（`20260824-GAP3UX-X-REVIEW-R22`）。
2. R22 落地 → 若條件齊備 ⇒ **FROZEN，然後停下來等使用者**（不得自行往 TODO 或實作走）。

## 查法（不寫聲稱，只寫命令）

- 常駐閘：`bash scripts/gap3ux_pre_review.sh`；失敗全量輸出落 `.claude/gate/gap3ux_pre_review.last.log`
- locus：`python3 scripts/patch_locus_check.py handoffs/patches/*r21*.md`
- 債務：`bash scripts/debt_ledger.sh --has-open`　治理票：`bash scripts/gen_fact_key_blocks.sh --check`
- 白話看板同步：`bash scripts/plain_docs_sync_check.sh`（git-commit 基準，未 commit 前會紅）
- 🔴 **每輪派工前先跑 `bash scripts/agent_preflight.sh`**——R21 postflight 曾因
  `/tmp/agent_dc_snapshot.txt` 被委員清掉而 rc=1（非真失敗，但無法比對 data_cache）。

## 定案，不要重新討論

輪次上限**已解除**／42 Task 一個不砍／條件 IC 答案窗屬 IC 分析層／A-6＝使用者原話（③已滿足）／
凍前**不拆 SPEC**／條件④＝(甲)（R17）／**主委不得新建驗收機制（R20）**／**條件②′（R21）**。

## 流程紀律（累積）

- 批次套補丁之腳本**任一條沒命中須非零退出**；落地後**當場跑補丁包 VERIFY grep**（R16）。
- **不要用 `git stash` 驗「檔案乾淨時的行為」**（R17）。工作區另有他人之 `stash@{0}: review-temp`，**勿誤刪**。
- **結構斷言須附行號碼證**；**「請這樣跑」之驗收須先確認在本專案環境跑得起來**（R18）。
- **機械閘只能比對位元組／集合成員／物件參考**（R19）。**「這是常數」需碼證**（R19）。
  **rc=0 不等於沒問題**，工具警告區要讀完（R19）。
- **保證若需要「某人記得維護某份東西」，它就不是保證**——改成「漏了就跑不起來」（R20）。
- 🔴 **撤回一個做法時要 grep 所有同義字面，並把命中數貼進收斂檔**
  （R20 寫下、R21 立刻又犯 ⇒ 改為落地流程之一步，不只是教訓）。
- 🔴 **「已解決」要標明解決的是哪一面**；「沒有任何 X 可錯」等全稱句在規格裡＝邀請反例（R21）。
- 🔴 **自我歸類天生不可信**——聲明的價值在「讓別人可逐條打」，不在「我說了算」（R21）。
- 🔴 **驗收條件長期為假時先問「它是否可達」**；但若放寬對自己有利，**必須交出去裁**（R21）。
