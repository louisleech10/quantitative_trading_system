# HANDOFF

**當前任務**：GAP-3 事件型 UAT 缺口修補 SPEC（`docs/GAP3_EVENT_UX_SPEC.md`）審到 **完整 FROZEN**。
**工作方法**：先讀 `docs/GAP3_EVENT_UX_ROLE_CARD.md`。
🔴 **角色卡新增最高位階條款（R20 三家裁定）：主委不得再新建任何驗收機制。開工前必讀該節。**

## 狀態（R20 已全數落地並收尾）

- SPEC **3,192 行／42 Task**（二十一輪未增未減）；版本行 = `R20-landing`，狀態 **未 FROZEN，待 R21**。
- R20：12 findings（codex 5／composer 3／grok 4）→ 6 群集，**全數落地**；三家 Verdict 一致「需修訂後定版」。
  **P0＝0（連六輪）**；歸因 **(N)=0**，十二條全部指向 R19 落地批。
- 🔴🔴 **三家裁定「主委本輪起停止新建任何驗收機制」**（主委於 brief 主動提問而得）。
  只准三類：**(a) 收斂進單一碼內來源 (b) 位元組／集合／`is` 三類機械斷言 (c) 修既有閘**。
  grok 三條明文禁止＋「每輪落地後逐條聲明屬 (a)(b)(c) 何者，無法歸類者不得落地」已入角色卡。
  同時三家判定**規格本體（42 Task／purge 式／五階段）已趨穩**。
- 🔴 **purge 綁定宣稱被打破第四次**（R16 列舉三／R17 列舉四／R18 解析文件／R19 常數清單）
  ⇒ **採 GROK 補丁包**改 keyword-only 簽章注入（漏傳即 `TypeError`，`inspect.signature` 導出集合）。
  **主委未自創。**
- 🔴 **主委在宣稱消除「第二套並行敘事」之同一次修訂中製造了兩套**（三家全員）；落地時另自查發現
  新寫段落下方仍留整段舊 mutation 清單，一併刪除。

## FROZEN 四條件現況

①⬜ P0=0／**P1=12**　②⬜ 自傷 6 群集　③✅　④當輪補丁包 locus 待清（規則已裁 (甲)）

## 下一步（依序）

1. **派 R21**：由 r20 三份改號生成 brief/facts/locus。
   🔴 **改號時必查 `locus.sh` 之 PATCHES glob 有沒有跟著換**（R17 踩過）。
   🔴 **R21 brief 必含**：①覆核主委本輪之「(a)(b)(c) 自我歸類表」（見 synth 末）
   ②**裁定 AST 掃 Python 是否算「解析文字」**（主委自判不算，需覆核）
   ③依角色卡新條款第 3 條，**裁定「主委未實跑之三項（簽章注入四條 mutation／AST 斷言／
   manifest 比對）是否應改標『待裁定』而非直接落地」** ④各家重寫自家 R20 SYNC-LOCI。
   `--task-id` **須為 session 之大寫形式**（`20260824-GAP3UX-X-REVIEW-R21`）。
2. R21 落地 → 若四條件齊備 ⇒ **FROZEN，然後停下來等使用者**（不得自行往 TODO 或實作走）。

## 查法（不寫聲稱，只寫命令）

- 常駐閘：`bash scripts/gap3ux_pre_review.sh`；失敗全量輸出落 `.claude/gate/gap3ux_pre_review.last.log`
- locus：`python3 scripts/patch_locus_check.py handoffs/patches/*r20*.md`
- 債務：`bash scripts/debt_ledger.sh --has-open`　治理票：`bash scripts/gen_fact_key_blocks.sh --check`
- 白話看板同步：`bash scripts/plain_docs_sync_check.sh`（git-commit 基準，未 commit 前會紅）

## 定案，不要重新討論

輪次上限**已解除**／42 Task 一個不砍／條件 IC 答案窗屬 IC 分析層／A-6 取代裁定＝使用者原話（條件③已滿足）／
凍前**不拆 SPEC**／條件④＝(甲)（R17 裁定）／**主委不得新建驗收機制（R20 裁定）**。

## 流程紀律（累積）

- 批次套補丁之腳本**任一條沒命中須非零退出**；落地後**當場跑補丁包 VERIFY grep**（R16）。
- **不要用 `git stash` 驗「檔案乾淨時的行為」**（R17）。工作區另有他人之 `stash@{0}: review-temp`，**勿誤刪**。
- **結構斷言須附行號碼證**；**「請這樣跑」之驗收須先確認在本專案環境跑得起來**（R18）。
- **機械閘只能比對位元組／集合成員／物件參考**；需「解析文字」或「判斷分類」者不是機械閘（R19）。
- **「這是常數」是需要碼證的宣稱**（R19）。**rc=0 不等於沒問題**，工具警告區要讀完（R19）。
- 🔴 **當一個保證需要「某人記得維護某份東西」才成立，它就不是保證**——改成「漏了就跑不起來」（R20）。
- 🔴 **撤回一個做法時要 grep 它的所有同義字面**，不是只改正在編輯的那一段（R20）。
- 🔴 **在同一件事上反覆修補時，把「我是否該停手」交出去裁**，不要再想更聰明的修法（R20）。
