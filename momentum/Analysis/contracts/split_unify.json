{
  "_doc": "SPLITUNIFY Task 1.2（SPEC C-8）：切分權威值集與 fail-closed reason 之**唯一真相源**。Python 端（momentum/Analysis/event_samples/split_projection.py）與前端各自讀本檔並對證，禁任一端手打第二份。🔴 本檔**刻意不含** assignment_states——三態＝兩容器（EventSplitPlan.assignments 帶 train/test ＋ 獨立的 purged），不是三值枚舉（SPEC C-3）；purge reason 亦不住這裡，沿用 momentum/Analysis/contracts/event_import_contract.json 之 split_purge_reasons（字面 interval_crosses_split_boundary）。",

  "schema_version": 1,

  "_split_authority_doc": "canonical 切分權威之封閉值集。kline_holdout ＝ K 線 chronological holdout（SplitPlan，含 purge/embargo）為唯一邊界來源（SPEC C-1）。新增值須同步 SPEC §C 與前端對證測試。",
  "split_authority_values": [
    "kline_holdout"
  ],

  "_fail_closed_reasons_doc": "投影與其接線端之 fail-closed 原因封閉集合（SPEC C-0/C-2）。字面由 Python 與前端共用，禁散文複列。multi_symbol_projection_unsupported＝per-symbol 投影未支援前的多標的批（SPEC C-2）；missing_train_plan／missing_test_plan＝缺任一側切分計畫（含 test_plan.row_index 為空，SPEC C-4 第一段之先決 fail-closed）；canonical_feature_universe_unavailable＝呼叫端拿不到 post-trim feature universe，須明示 event-study-only 而非按事件數另切（SPEC C-0 決議③）。🔴 B3 自查發現的第五種情形（train/test 兩 plan 之 base_universe_hash 不同）**刻意不在此登記**：登記會動到已戳記之 SPEC 的封閉值集與前端枚舉面，而該守衛的保護力不依賴字面——它在 split_projection.py 以明文 ValueError 擋下，是否要升格為具名 reason 交 B3 code review 裁定。",
  "fail_closed_reasons": [
    "multi_symbol_projection_unsupported",
    "missing_train_plan",
    "missing_test_plan",
    "canonical_feature_universe_unavailable"
  ],

  "_test_segment_count_keys_doc": "SPLITUNIFY Task 4.1／SPEC C-6：**整份 report**（不只 metadata）中鍵名含 `test` 且值為整數之鍵的**封閉登記**，dotted path 自 report 根起算（list 索引與 per-feature 名一律正規化為 `*`）。判準是 **deny-by-default**：掃到的每一個都必須落在下面四個桶之一，否則測試紅——新鍵必須先進本登記才寫得出去。🔴 B4 review R1 三家獨立命中：原版只掃 metadata 且只做正向計數，codex 實測注入 `summary_table[0].verification_test_event_count` 與 `validation.test_event_count` 皆**未被抓到**。🔴 同輪另一發現（grok）：事件路徑上 `oos_downgrade.test_rows` 其實是**事件數**（與 canonical 同值 13），原本被誤登記為列語意 ⇒ 已改列入 must_equal_canonical。C-6 之文字是「恰一個」，本實作採**更強且可機械化**的等價形式：canonical 恰一個，其餘同語意鍵**必須逐值等於** canonical——真正要禁的是「同一個問題兩個**不同**答案」（實測症狀 31 vs 33），而報告中同一個數字在多個 section 出現是既有事實（實測 7 處），逐一刪除會波及本票以外的消費者。此偏離已在 B4 review R2 具名送審。",
  "test_segment_count_keys": {
    "canonical": "metadata.split_unify.n_test",
    "must_equal_canonical_on_event_path": [
      "metadata.ic_train_test_split.test_events",
      "metadata.oos_downgrade.test_events",
      "metadata.oos_downgrade.test_rows",
      "metadata.event_filter.split_mask.test_rows",
      "filter_log.stage3_event_filter.split_mask.test_rows",
      "marginal_ic.n_test"
    ],
    "row_semantics_not_event_count": [
      "metadata.ic_train_test_split.test_rows"
    ],
    "_not_a_segment_count_doc": "鍵名含 test 但**語意完全不同**者：n_tests＝多重檢定的檢定次數（14）；n_used_test＝marginal IC 實際用到的列數；min_test_*＝門檻不是計數。列在這裡是為了讓 deny-by-default 掃描有明確歸屬，不是豁免。",
    "not_a_segment_count": [
      "metadata.selection_scope.n_tests",
      "metadata.significance.n_tests",
      "filter_log.stage5_thresholds.n_tests",
      "metadata.ic_train_test_split.min_test_events",
      "metadata.oos_downgrade.min_test_events",
      "metadata.oos_downgrade.min_test_rows",
      "marginal_ic.n_used_test",
      "marginal_ic.composite.n_used_test",
      "marginal_ic.per_feature.*.n_used_test",
      "marginal_ic.sequential[*].n_used_test"
    ]
  },

  "_split_unify_keys_doc": "metadata.split_unify 之必填鍵（SPEC C-6）。fail-closed 時 n_test 為 null 而非 0；reason 取自 fail_closed_reasons。",
  "split_unify_keys": [
    "n_test",
    "split_authority",
    "boundary_hash",
    "per_symbol_counts",
    "reason"
  ],

  "_estimand_scope_doc": "event-study-only 路徑之估計量範圍標記（SPEC C-0 決議③(b)）。event_forward_return_table 在 split_plan=None 時跑全 manifest 事件（tables.py:211-238 無 test 過濾），必須以本字面標明它不是 OOS。",
  "estimand_scope_values": [
    "full_sample_not_oos"
  ]
}
