# ROADMAP — 量化交易系統戰術路線圖

> **這份只回答一個問題：現在在哪、接下來做什麼。** 敘事與歷史在 `docs/ROADMAP_DETAIL.md`。
> 即時任務狀態看 `HANDOFF.md`；決策理由看 memory。
> 維護：**每次 commit 一併更新本檔**（2026-06-26 使用者定）。日期看 git log，手寫日期欄已廢。

當前階段：**V1.0 工具階段** — crypto 單市場研究管線（探索 → 發現 Pattern → ML 優化 → 回測）。
願景 V1→V2→V3 見 `PRODUCT_VISION.md`。

---

## 🔥 現在在哪（狀態表）

> 🔴 **本表只放狀態與下一步。** 想寫背景、理由、歷史 ⇒ 寫進 `ROADMAP_DETAIL.md`，本表放 pointer。
> 病根（使用者 2026-08-14 第四次指出）：本檔曾 227 行、其中「進行中／下一步」一節佔 213 行
> ⇒ 要回答「我在哪」得讀 213 行敘事。**主委四次說要改、四次講完就放掉。**

| 工作線 | 狀態 | 下一步 | 細節 |
|---|---|---|---|
| **量化主線（IC 分析）** | 🏁 **GAP-1 四批全部 CLOSED（2026-08-18）**：B1–B4 各三家 code review＋三家戳記；280 測試／20 mutation／wiring 閘 rc=0；延伸檔 A1-1..A1-24；殘留 G1-R1..R7／R9／R10／R11 | GAP-2a 亦 CLOSED（2026-08-19）；**GAP-3 之 UAT 缺口修補：SPEC 與 TODO 皆已 FROZEN（2026-08-24）**；**B1 已收斂（2026-08-24，五輪 review 3→2→10→7→0）**，唯一入口 `docs/GAP3UX_IMPL_HANDOFF.md`（42 Task 中 3 個 ✅／1 個 🔧；看板 `白話說明/GAP-3施工看板.md`）。**B2 已落地（2026-08-25）：Task 1.2／1.3／1.4／1.8，mutation 9 條 `closure: CLOSED`，待三家 code review**。**B3 已收斂（2026-08-25）：Task 1.11／1.12／1.9 深度三層防線；三輪 code review findings 6→3→0，三家一致可進 B4；R1／R2 全部反例皆由原提出方確認關閉（章程 §B8），mutation 7→13 條 `closure: CLOSED`**。**B4 已收斂（2026-08-26）：Task 1.5／1.6／1.7 匯入前端；六輪 code review findings 7→4→4→1→1→0；mutation 3→15 條 `closure: CLOSED`；解除 `R-B2-1`、新增 `R-B4-1`**。**B5 已收斂（2026-08-26）：Task 2.1／2.2／2.3 匯出前篩選；四輪 code review findings 6→3→1→0，三家一致可收；mutation 10→19 條 `closure: CLOSED`；三條必辦殘留 `D-002 A-004`／`R-B3-1`／`R-B3-2` 皆已解除**。**B6 已收斂（2026-08-26）：Task 3.1／3.2／3.3 事件批次刪除；六輪 review 5→2→2→1→兩家零→0，三家一致；mutation 12→23 條 `closure: CLOSED`；`RULING-1`／`RULING-2` 皆三家一致定案**。**B7 已開工、卡在 `D-004` 戳記輪 R2（2026-08-26）**：開工偵察查出兩件擋住實作的事並派 consult 輪取得四題裁定——①匯出記錄要帶的 `future_{h}bar_return` 與 `lookahead_bars_declared` 經**實測**皆被契約 validator 以 `unknown_field` 拒收 ⇒ 照 SPEC 字面實作會產出**匯不回去的檔**，裁定＝改契約走 `D-004`；②Task 4.1 移除主答案窗後 B5 下界守衛半數變死碼，裁定＝改形不刪；③交接原稱「4.2 會讓 G-2 golden 合法改變」經實測**確認為錯**（golden 跑 IC 管線、不碰 `analyze_tables`；`--check` rc=0、sha 未變）⇒ **不重凍**；④Task 4.2 後端已具備、前端未接線。已完成 Task 4.2 後端驗收 4 條 `-k horizon_curve`。🔴 `D-004` 戳記歷三輪（R1／R2 皆 composer／grok APPROVED 而 **codex REJECTED 且兩次都正確**）：R1＝`RULING-3(c)` 實為 2 vs 1，主委誤採少數版並標「三家一致」（第**五**次「宣稱大於實作」，形態＝**未逐家交叉核對即宣稱一致**）；R2＝A-020 漏記**三家一致**之三項限制（`future_*` 不進 `ic_feed`／保留 `receipt_schema.batch` 同名鍵／驗批內一致性），而該輪 composer 與 grok 標「一致」時**也沒回讀自己的 consult 原文**（第**六**次）；R3 三家全數 APPROVED（sha `12a8fc74…`／`befd04f7…`）。**B7 已收斂（2026-08-26，三輪 review 7→3→0，三家一致可收）**：契約 A-020 落地（`lookahead_bars_declared` 移出 `derived_fields` 入 `optional_fields`＋`receipt_schema.batch` 保留＋批內一致性；`future_{1..12}bar_return` 逐欄列舉，doc 字面含「不進 `ic_feed`」且由**執行期**斷言 `build_event_ic_inputs()` 回傳不含 `future_` 前綴鍵承載，非驗 doc 字串）、Task 4.1（附帶欄多選預設全選、移除主答案窗與 `label_value`、`window.horizon_bars` 逐列由該列 tf 之宣告深度導出且與 `lookahead_bars_declared` **刻意不等**）、4.1b（四段揭露皆由實際設定導出，逐 tf 各一行；`control_kind` 顯示值 == 匯出檔實際值）、4.1c、4.3（逐附帶 horizon 列缺筆數，不含「主答案窗」字樣、不阻擋匯出）、4.2 前端接線（`EventTablesPanel` 此前恆送 `{}` ⇒ 後端永遠用預設 `[1,2,4]`）。A-021 守衛改形：`withExportLowerBoundGuard(state, {notify, proceed})` 保留 `proceed` 結構保證、職責改 readiness fail-closed，`exportAllowedByLowerBoundState` 刪 scalar 比較，`inexpressible` 改為可匯出，刪 `horizonOptions`。🔴 R1 七條 findings 中**三家同時抓到同一條 P1**——主委在 brief 宣稱「拿不到深度就擋」而實作只擋了「有條件」那一半（第**七**次「宣稱大於實作」）；另有深度回傳完全未驗（空 map／缺 tf ⇒ floor 1 **冒充深度 0**）、`future_*` 放行 NaN／±Inf（違 `CLAUDE.md` 禁弱化 NaN/inf 閘）。R2 codex 再抓三條而另兩家判可收：其一為 **R1 修法自身開的破口**（`timeframes` 為空 ⇒ 靜默可匯出且錯被 `void` 吞掉，**正是主委自己寫進 brief 卻沒先打過的嫌疑點**）、必填欄「只有部分列帶」放行成 `NaN`、**`npm run build` 不涵蓋測試檔**故型別錯漏網（此後前端收案固定加跑 `npx tsc --noEmit`）。R3 三家零 finding 且皆附實跑證據（含主委交攻之三個面：取消誤彈／進頁誤紅／全有全無誤擋 Task 1.8）。三家並裁定 TODO Task 4.2 邊界②與 Phase 4 Gate 之「須重凍」為隨 A-022 失效之**過期副本**，**不另開 D-005**。驗收：`gap3_attached_columns_contract` 16／`eventExportHorizonColumns` 6／`eventExportDisclosureLegacy` 5／`eventExportNoIcDecay` 4／`exportMissingColumnDialog` 5／`eventExportGuardRuntime` 7／`eventTablesHorizonWiring` 4／vitest 全套 54 檔 310 條／build rc=0／`tsc --noEmit` B7 相關 0 錯／`event_samples` 299 passed／golden rc=0 **sha 未變（不重凍）**；**mutation 23→22 條 `closure: CLOSED`**（B7 自成一份 receipt `gap3ux-b7-all-mutations.receipt.json`）。🔴 主委另修掉自己 mutation runner 的一個真 bug（備份清單與實際改檔不同步 ⇒ `page.tsx` 未被還原），已加**備份閘**機械強制。**B8 已收斂（2026-08-27，commit `ebd77b87`）：Task 5.0／5.1／5.2／5.3 訊息與表頭；五輪 review 5→1→4→2→0（🔴 非單調——R3 跳回 4 是首次請三家獨立重掃全部 21 條 definition＝新開攻擊面）；mutation 19 條 `closure: CLOSED`；`event_samples` 313／gap3 api 189／vitest 57 檔 327／build rc=0／golden sha 未變**B9 已收案（2026-08-28，commit `01870c1a`→`fbf2f643`）：Task 6.0／6.1／6.2／6.3／6.4 IC 止血閘；五輪 review findings 10→3→4→5→3（非單調）＋兩輪 consult；mutation 19→29 條 `closure: CLOSED` 逐一等於。🔴 **收案判準經三家 consult 改寫**（`20260828-gap3ux-b9-consult-r1`）：由「三家零 finding」改為**請求形狀雙向矩陣**（每格 under-block／over-block 各一條）＋mutation 閉鎖＋具名殘留＋停輪條件——三家判定「零 finding」對過渡止血 Task **過當且不可停**。否決方案 B（`plan_load` 下沉：codex 實查會動 7 個 production caller 且其測試基線本就有紅）與方案 C（cap 移進 `load`：違反 Task 6.4）。新增殘留 `R-B9-1..5`；耐久解法 `LoadPlan`（一次解析、同一 plan 帶進 task）**明列為 GAP-6 或 D-00N 工作**。上限 80,515 ＝最小超標點 161,031×0.5，**綁本機 8GB**。🔴 **B9 沒有讓大 run 變得可分析**——那需要分塊載入，本就屬 GAP-6。****。🔴 本批九條自傷同一種病＝**glossary 之 definition 在重述公式而我是讀碼推論寫的**（`n_eff` 實為等權恆等於 n／`prevalence_full` 分母是 n_labeled／`horizon` 自進場根起算／`macro_mean` 是保留集×uniqueness 加權／`n_test` 為三者交集且**改了三次**…）；修法是每條補**算式綁定測試**，算式一改先紅。三家修正主委判斷：病根只講對一半，另一半是**審查方法沒跑不對稱反例探針**。🔴 另有工作區事故：`import_contract.py` 未 commit 之實作整段回到 HEAD（composer 複驗時發現），**機制未判定**、主委初判「執行端違約」已撤回；grok 查出 B8 runner 缺 `IsolatedWorktree`（B7 範本已退化而交接沒記，缺陷延續兩批）＝主委自身缺陷，已補隔離並在隔離環境重跑 19 條。新增鐵律：**派 review 前先 commit**、**runner 須同時有隔離＋備份閘＋開跑前刪 receipt**。新增殘留 `R-B8-1`（definition 收窄，blocked-by Task 7.5）／`R-B8-2`（後設欄進 bundle，user-ruling）／`R-B8-3`（事故之兩條無法 post-hoc 證明的假設，needs-research）；更正 `R-B7-2` 計數 6→8。**下一步＝B9＝Phase 6（IC 分析止血閘）** | TODO `docs/GAP1_STRATEGY_OVERFIT_TODO.md`；延伸檔 `docs/GAP1_STRATEGY_OVERFIT_AMENDMENTS.md`；收斂 `handoffs/reconcile/20260818-gap1-b4-review-r18/synth.md` |
| **`/search` 修復（out-of-epic）** | 🏁 **CLOSED（2026-08-22）**：使用者 UAT 現場抓到三個既有 bug（毫秒游標→無窮迴圈＋資料重複、同步下載佔事件迴圈、worker 日誌不落檔）；7 輪三家對抗審＋2 輪蓋章，findings 8→7→2→2→3→3→0；`reconcile_stamps_check.sh` rc=0 三家 APPROVED | —（九項具名殘留各帶三值理由，觸發時再開） | 收斂＋戳記 `handoffs/reconcile/20260822-searchfix-x-review-r7/synth.md`；白話 `白話說明/search搜尋修復.md`；commit `bf0fd48a` |
| 治理 epic | **留現狀、不再擴建**（使用者 2026-08-14） | 無。已掛機制繼續運作。**唯一例外（使用者 2026-08-25 明示授權）**：mutation 併發隔離 `scripts/mutation_worktree.py`＋`verify_mutation.sh` 改薄殼委派 ⇒ 三家可平行跑 mutation，主 repo 零觸碰 | `ROADMAP_DETAIL.md` |
| P1-6 委員債狀態機 | B5 未完工（線 C 未做） | 不排程（治理不再擴建） | `ROADMAP_DETAIL.md` |
| GAP-1 DSR/PBO/MinBTL 策略層防偽 | 🏁 **全票 CLOSED（2026-08-18）**；殘留 registry G1-R1..R7＋R9＋R10 | —（殘留 registry 觸發時再開） | TODO＋延伸檔（見上行）；待補完：registry「GAP-1 待補完」節 |
| PA-CUMSUM 單利權益改正（小票；G1-R8 收回） | 🏁 **CLOSED（2026-08-18）**：`EquityCurveData` 改為單利（cumsum）／複利（cumprod−1）**兩條都算、都標清楚**（後端＋API model＋前端切換，預設複利）；三家 code review 7 條全修（多標的等權組合／proba 缺值 4xx／契約封閉）| — | 出處＝`CODEX-R8-P1-12`／`GROK-R8-P1-03`；使用者 2026-08-18 裁定「兩條都算＋前端切換一起做」；收斂＋三家戳記 `handoffs/reconcile/20260818-pacumsum-x-review-r23/synth.md` |
| **GAP-2a 邊際 IC／多因子組合（純 IC 層）** | 🏁 **CLOSED（2026-08-19）**：SPEC R7 FROZEN＋TODO FROZEN（五輪 20→15→7→1→0）；B1–B5 各三家 code review＋三家戳記；A1-1..A1-11；§V 24 條 mutation 全 RED；§G-1 golden 改前==改後 PASS；殘留 G2-R1／R2／R3／R5／R6／R7／R8 見 registry「GAP-2 待補完」 | — | SPEC `docs/GAP2_MARGINAL_IC_SPEC.md`；registry #2a |
| GAP-2b IC→ML 橋 | 契約 **已落地**（`ic_survivor_contract.json` v1＋`validate_survivor_output`／`build_survivor_output`；B1／B3／B4 三家戳記；倖存者檔 `ic_survivors_{case_id}.json` 每次 analyze 落檔）；橋本體＝殘留 G2-R1（user-ruling blocked-by ML 層） | 本體等 ML 層穩定 | registry #2b／「GAP-2 待補完」G2-R1 |
| GAP-3 事件型分析（使用者重定義：外部標好正反例匯入→PIT 對齊→條件 IC／ML；非 event study） | **SPEC＋TODO 皆 FROZEN（2026-08-20 白話閘核准）**：SPEC 六輪對抗審 15→0＋三家戳記；TODO 兩輪 14→0（R7 12 群集寫回、R8 原提出方全 CLOSED）＋三家 RECONCILE-STAMP 蓋 r8 synth rc=0；殘留 G3-R1..R8 入 registry | **B1–B4 CLOSED（2026-08-21）**：B1 七 Task（review 四輪 8→3→1→0）、B2 五 Task（三輪 11→4→0）、B3 三 Task（條件引擎／產生器 adapter／五個 state-counter 算子；兩輪 9→0）、B4 兩 Task（pattern 橋 train-only＋candidate ledger→GAP-1 DSR/PBO/MinBTL，N 只從 ledger、AUC 型別閘；四輪 8→2→1→0）皆三家 RECONCILE-STAMP rc=0；event_samples 全套 224＋state_counters 17 tests；GAP-1 strategy_validation 272 不退步；§G-1 golden sha 不變；B5 follow-up：`requests.py` allowed_filtering_params 改讀契約出口。**B5 審查完工蓋章（2026-08-22）**：B5.1 API 接線＋`create_event_sample_pipeline()` 唯一出口＋規模 receipt（direct 76.978s／API 路徑 0.382s）、B5.2 前端三頁（匯出契約 JSON＋companion 來源檔／新契約匯入／事件模式選批＋三表）、B5.3 UAT checklist；review 五輪 11→5→4→1→0＋三家 RECONCILE-STAMP rc=0；UAT A 段 9 項 rc=0（receipt `20260822T040000Z-gap3-b5-uat-sectionA.log`）。**UAT 缺口修補 SPEC 進行中（2026-08-22，未 FROZEN）**：使用者 UAT 提出之 10 條問題寫成 `docs/GAP3_EVENT_UX_SPEC.md`（1168 行／7 Phase／41 Task／§V 18 條）；對抗審 R1 24 → R2 7 → R3 18 → **R4 19（全數 ACCEPT、0 條降殘留，§C0 條文 2）**。R4 為**去錨定輪**（brief 零「請攻」＋fact 全改可重跑 receipt＋標的 sha256 鎖版），較前三輪多出 5 條無人觸及之 findings（最重＝D-7 L3 與 `event_forward_return_table` 必填 `event_split_plan` 之真矛盾）。R4 九群集全數修訂完成（新增 Task 7.0／7.6／7.7、§G S-1..S-8 canonical serialization、V-14／V-15；§N #8／#10 殘留撤回改為 Task 7.7）。🔴 **上列 R4 之後續已完成，敘述以此為準（2026-08-24 更新，取代原「下一步＝派 R5」）**：SPEC 續審至 **R34** 後由**使用者直接裁定 FROZEN**（3,547 行／**42 Task**／§V 20 條；P0=0、新缺口連二十輪 0；委員四條件之①未滿足由使用者覆蓋，四條具名殘留 F-1..F-4 皆非量化正確性）。**TODO 亦 FROZEN v1.0**（`docs/GAP3_EVENT_UX_TODO.md`，1,618 行／42 Task；三輪對抗審 12→19→3 ＋ 戳記輪，三份 reconcile 皆三家 APPROVED；延伸檔 `docs/GAP3_EVENT_UX_TODO.D-001.md` 更正 §B 之 B1 列，**已於 2026-08-24 三家 APPROVED**）。**B1 實作已落地（2026-08-24，待 code review）**：Task 1.1 契約先行（`event_import_contract.json` 之 typed namespace-aware `receipt_schema`＋`flatten_receipt_schema`／`receipt_type_ok`／`validate_receipt_namespace`；凍結 baseline fixture sha256 `7111b2d7…`）、Task 1.10 lookahead registry（`future_column_lookahead.json` 登記 `CaseData` 之 37 個 future 欄，分 bar／hour／unknown 三類；`lookahead_registry.py` 含 provenance 信任邊界）、Task 2.1b 唯一深度函式 `lookahead_depth.py::depth_by_timeframe()`＋前端下界鎖定、Task 4.2 之 §G S-9 參考實作 `canonical_serialize.py`＋重複 horizon 守衛。驗收：`gap3_contract_reason_registry` 16／`lookahead_registry_complete` 12／`lookahead_rename_attack` 4／`gap3_lookahead_depth` 9／`canonical_serialize` 9／vitest 7 皆綠；13 條 mutation 逐條轉紅並還原轉綠（receipt `handoffs/run_receipts/gap3ux-b1-task{11,110,21b,42-s9}-mutation.receipt.json`）；G-1 `gap3_freeze_golden.py --check` rc=0；`npm run build` rc=0。延伸檔 `docs/GAP3_EVENT_UX_TODO.D-002.md`（A-002 S-9 應為 7 條、A-003 小時換算取 ceil、A-004 前端下界值來源殘留），**尚未過戳記**。**四輪 code review 已跑完（2026-08-24，commit `fa172e44`）**：findings 3→2→10→7，P0 全程 0、P1 連兩輪 0；第一輪之後皆屬測試強度而非產品邏輯。R1 之 `CODEX-R1-P1-01` 為**資料正確性真缺口**（SPEC Task 1.10 對 `future{1,2,4,6}_close_return` 之單位敘述與 producer 不符——producer 用裸 `shift(-N)`＝根，SPEC 寫小時；12h 線上 purge 低估六倍）；R3 之三條合併導致**設計改變**（把匯出包進 `withHorizonLowerBoundGuard` 之 `proceed`，使「阻擋早於網路動作」由需檢查之性質變成結構保證）。驗收：`gap3_contract_reason_registry` 等 54／`event_samples` 270／vitest 23／build rc=0／G-1 rc=0；**mutation 32 條、receipt `closure: CLOSED`**。延伸檔 `docs/GAP3_EVENT_UX_TODO.D-002.md` 累計 14 條修訂（A-002..A-015），**尚未過戳記**。🔴 **上列三個待裁定已由使用者於 2026-08-24／25 全數裁定並執行完畢（本段取代原「現停在使用者裁定」）**：①**R5 已跑**（`dec88c10`）——三家零 finding，R4 七條由**原提出方**各自重跑自己的反例確認閉合（章程 §B8）；mutation 累計 **32 條 `closure: CLOSED`**。②**D-002 已補戳記輪**（`51f1a65e`）——codex／composer／grok 全數 APPROVED，14 條修訂（A-002..A-015）生效。③殘留 `R-B1-1` **未釘死**，維持 `needs-research`。另有兩件由該批衍生：**repo 內 11 條既有紅測試已清**（`1f9dceac`；10 條修測試、1 條先標 `xfail(strict)`，**產品碼零改動**，依使用者「以現行程式碼為準、測試可能過時」之裁定）；**`survivor_contract` 事件模式對帳誤擋已修**（`0f09e30f`／`6981b79f`）——事件過濾後列數與全軸切分列數屬**不同母體**卻被直接相比，導致事件 IC 分析整段中止，修法三落點＋兩道守衛，7 條 mutation `closure: CLOSED`，review R2 三家零 finding。**B2 已落地（2026-08-25，待三家 code review）**：Task 1.2 CSV 欄名對映端點（對映層轉呼與 `/import-events` 同一 `import_records`，V-3 兩重 oracle）、Task 1.3 `source_file_digest` 綁完整 `CaseData` 列且一律由後端算（承載點＝既有 `/search/task/{id}/result` 增兩鍵，不新增 route；前端 `sha256Hex` 移出 `eventExport.ts` 至 `ruleDigest.ts` 使「無模組同時碰雜湊入口且寫 digest」成結構保證）、Task 1.4 `detect_t0_unit_ms()` 單一門檻導出兩帶且判不出即拒、Task 1.8 異質列拒收（比對正規化後之列，只在使用者匯入路徑啟用）。驗收：`gap3_csv_import` 13／`gap3_t0_unit_detect` 7／`gap3_heterogeneous_rows` 6／`gap3_source_digest` 12／vitest `canonicalSourceCoverage` 14／vitest 全套 198／`npm run build` rc=0；**mutation 9 條 `closure: CLOSED`**（receipt `handoffs/run_receipts/gap3ux-b2-all-mutations.receipt.json`）。🔴 一處與 TODO 字面不同已具名回報：Task 1.3 承載點採 SPEC 之「既有匯出流程服務端入口」＝`api/routes/case_search.py`，因 `api/routes/case.py` 之 `@router` 無一持有 `cases`。**B2 已收斂（2026-08-25）**：兩輪 code review，三家一致可進 B3；findings 2→1（皆 codex 提出，composer／grok 兩輪皆 0），P0／P1 全程 0；R2 由原提出方 codex 逐字重跑 R1 兩反例確認關閉（章程 §B8）。R1 補 `event_id` 之 D-2 identity 契約（公式進契約檔為 SoT、後端唯一實作、前端模板由 vitest 讀契約逐字對證、只在使用者匯入路徑強制）；R2 把 V-3 之形狀斷言升級為**執行期**斷言（工廠回來的物件之 `import_records` 就是共用那一份），一次關掉 assignment／setattr／subclass／factory-return 四類繞法——**刻意不再補第三條形狀黑名單**（交接 §6.1 之教訓）。mutation **14 條 `closure: CLOSED`**。新增具名殘留 `R-B2-1`（秒級 t0 之 event_id 屬 Task 1.5）／`R-B2-2`（factory-body 繞法，屬 B10）。**B3 已落地（2026-08-25，待三家 code review）**：Task 1.11 未知欄強制宣告（`lookahead_registry.requires_declaration()`＝`depth_by_timeframe()` 之不 raise 謂詞形；external_upload 之欄名不具證據力）、Task 1.12 不可證則禁進切分（新 `lookahead_gate.py` 之 L3 閘＋`pipeline.run_event_study_only()` 不切分 executor；`tables.event_forward_return_table` 之 `event_split_plan` 改 Optional、`None` 時 `ci` 標 `"unavailable"` 不計算、空 `clusters` 之假 plan raise；reason 字面經契約新增之 `capability_reason_bindings` 取得，`.py`／`.ts` grep 計數 0）、Task 1.9 答案窗宣告（逐 tf 收集、預設＝檔內最大可用 horizon、調低須勾選聲明、`embargo_ms_by_symbol` 投影；前端 `lookaheadDeclaration.ts`＋`LookaheadDeclarationFields.tsx`）。驗收：`lookahead_declaration` 4／`split_blocked` 7／`gap3_horizon_declaration` 7（下限 2／6／5）；`pytest tests/api tests/momentum/event_samples` 876 passed／3 failed（既有債名單）；vitest 206／build rc=0；**mutation 7 條 `closure: CLOSED`**（receipt `handoffs/run_receipts/gap3ux-b3-all-mutations.receipt.json`）。🔴 一處跨 Task 規則相撞已具名回報：Task 1.8 之 `label_definition` 同質維度 vs Task 1.9 ⑥ 之逐 tf `horizon_bars` ⇒ 投影排在 `validate()` 之後（R1 三家一致裁定成立）。**R1 code review 已收斂（2026-08-25）**：三家 Verdict 不一致（codex BLOCKING／grok 可進／composer 可派工），主委採 codex 嚴格版——其 4 條 P1 皆附可重跑 probe，且 `CODEX-R1-P1-01` 與 `GROK-R1-P1-01` 為同一 fail-open 之兩種攻法，composer「無具體反例」之前提被實例否證。六群集全修：**A** `filters` 引用欄抽取之 fail-open（四種編碼皆抽空 ⇒ 改為「有條件但抽不出即強制宣告」，不列舉編碼）、**B** L2 觸發時未強制勾選不可驗聲明（改為與調低無關一律要求）、**C 真實洩漏路徑**——宣告之 `embargo_ms_by_symbol` 未接進 split（`open_to_close` 下宣告 20 根實際只隔 1 根；改為 `max(請求值, 宣告下界)` 並揭露 `embargo.source`；🔴 併修 `EventAnalyzeResponse` 未宣告鍵被 `response_model` 靜默濾掉，`capability`／`lookahead_declaration` 原本前端永遠看不到）、**D** 假 plan 第二形態（`assignments` 與 `purged` 皆空 ⇒ raise，並以對照組釘住合法全 purge 不得誤擋）、**E** 1.9 ⑤ oracle 覆蓋缺口（fixture 改為引用可解析欄＋斷言餵入內容）、**F** 前端型別未跟上新契約。mutation **7→12 條**（新增 5 條各鎖一個群集）`closure: CLOSED`；`reconcile_cluster_attribution_check` 與 `completeness_check --lock` 皆 rc=0。新增殘留 `R-B3-1`（⑤ 之系統內篩選端到端對證，`blocked-by` Task 2.1／4.1）、`R-B3-2`（`filters` wire shape 未凍結，`blocked-by` Task 2.2）。**R2 閉合輪已跑（2026-08-25）**：R1 全部反例由**原提出方**逐條重跑確認關閉（codex 六條、grok 三組，composer 獨立複驗一致；章程 §B8）。本輪新增 3 條，其中 `CODEX-R2-P1-01` 為 **R1 修補自身引入之缺陷**——群集 C 的 `max(embargo_ms_by_symbol.values())` 正是 SPEC §D-3′-a(ii)「明令禁止」逐字所寫之「以單一 batch scalar `embargo_ms` 冒充 per-scope 下界」（同節載明取全批 max 對窗較小之 symbol **過度 purge**，違反「過度保守亦屬錯誤」）。主委逐字查證 SPEC 後採 codex 判定（grok 未觸及折疊面、composer 亦漏）。修法**不**提前實作 `EventSplitConfig.embargo_ms_by_symbol`（SPEC 已把其唯一實作與驗收鎖在 Task 7.0b ⑨，B3 做半套即雙源）⇒ 改為「能表達就套用、不能表達就拒絕」：各 symbol 下界相同（含單一 symbol）時 scalar 與 per-scope 等價可套用，不同即 fail-closed 拒絕分析（取 max 過度 purge、取 min 洩漏）並排在載 bars 前。另兩條：`EventAnalyzeResponse` 三新鍵之前端型別漂移（codex＋grok 共識，R1 只同步了 import 那一半）、`R-B3-1／2` 之登錄位置不實（HANDOFF 指向 §7.3 而該節無列）。mutation **7→13 條** `closure: CLOSED`；新增殘留 `R-B3-3`（per-symbol purge 下界，`blocked-by` Task 7.0b）。**R3 閉合輪已跑（2026-08-25）：三家零 finding、Verdict 一致「可收進 B4」（本 epic 首次三家一致）**。R2 兩反例由原提出方確認關閉（codex probe `REJECTED True`／同值 map `ACCEPTED`；grok 交叉 probe 同形態確認 raise）。爭議條「下界不一致即拒絕是正解或降級」三家皆判**正解**：禁令原文是「以單一 batch scalar **冒充** per-scope 下界」，而修法在 divergent 時 **raise、不送 scalar** ⇒ 不構成冒充；等價情形 scalar ≡ per-scope 由 `test_08b` 鎖住。grok 定性為「降級**時程**而非降級**正確性**」，並自評「service 層自動拆子批」之替代設計**不建議**（新 orchestration 面、易另開契約）；codex 補誠實邊界：混 TF 為 SPEC 允許之輸入形狀，但 repo 無證據可量化其業務頻率，故不以「很少用」為由、改以 `R-B3-3` 具名。三家皆未找到 `1.9-M5` 之繞法。方法論：composer 依 r2 synth 群集 K 之建議改採逐欄實查（`model_fields`／`grep`）並自陳前兩輪漏看成因，本輪三家結論一致。**B3 收批（`b63fc855`＋`ed426d34`＋`ed9b3fc4`），不派 R4。****B4 已落地（2026-08-25，待三家 code review）**：Task 1.5 CSV 上傳／預覽／逐項對映 UI（新元件 `EventCsvMappingForm.tsx`＋純函式 `csvPreview.ts`；下拉初始值一律未選＝A-4′，文案禁「label 正確」字樣，未勾確認 ⇒ upload 與 `fetch` 呼叫次數皆 0 之**執行期**斷言，送出鍵刻意保持可按以免形狀假綠；重複欄名以「第 N 欄」各自可辨且選中時 fail-loud）、Task 1.6 對映 provenance 落檔（契約先行：`receipt_schema` 新增 `mapping_provenance` namespace 五欄＋`receipt_type_ok` 新增 `Mapping[str,str]`；生效點＝新 R3 出口 `EventSamplePipeline.validate_receipt_values()` 回純資料，缺／批內不一致之 `source_file_digest` ⇒ 契約 validator fail-closed、落檔數 0；`confirmed_at_source` 揭露伺服器補記，不讓伺服器時間冒充使用者確認時間）、Task 1.7 可疑欄警示（`suspiciousBinaryColumns.ts`；只警示不阻擋、不持久化、**不與 Phase 2 篩選合併**，`suggestedLabelColumn` 恆 `null` 並**接為 label 下拉初始值之唯一來源**使 A-4′ 違規在純函式與 UI 同時現形）。**併解殘留 `R-B2-1`**（秒級 t0 之 `event_id` 摩擦）：`eventIdNormalization.ts` 於單位偵測後逐列算出契約毫秒版 `event_id` 並在送出前顯示期望值，單位門檻 `MS_MAGNITUDE_MIN` 與 `EVENT_ID_TEMPLATE` 皆由 vitest 讀契約檔逐字對證（禁第二份公式）。驗收：`gap3_csv_provenance` 7（下限 2）／vitest `gap3_csv` 8＋`suspiciousBinaryColumns` 5（下限 5／2）／vitest 全套 40 檔 221 條／`npm --prefix frontend run build` rc=0／`gap3_freeze_golden.py --check` rc=0；**mutation 3 條 `closure: CLOSED`**（receipt `handoffs/run_receipts/gap3ux-b4-all-mutations.receipt.json`）。**B4 已收斂（2026-08-26，六輪 review 7→4→4→1→1→0，三家一致）**：R1 七群集（P0＝確認前即發生網路動作且測試因 mock 而假綠；併 R-B2-1 之真解除＝`derive_event_id` opt-in 由後端在 t0 正規化後依契約模板產生 ID；provenance 新增 `source_digest_verified`／`event_id_source` 誠實揭露；🔴 三家共提之 pandas 靜默左移——每列比標頭多一格時把首欄當 index 且零 warning，label 實測由 0 翻成 1）。R2 三群集（SPEC Task 1.3 之 `source_file_digest` 語意自相矛盾且 L1427 讀法不可實作 ⇒ D-003 A-019 以來源檔讀法為準；欄數不齊改為以 `csv` 標準庫獨立驗列寬之**單一規則**，reader 參數降為後備；`confirmed_at` 改真日期解析）。R3 兩群集（🔴 **R2 修法自己**把空行誤判為 0 欄 ⇒ 三家各自獨立命中；後備層之 receipt 結論降級為「只證明 mutant 下之防禦行為」）。R4 一群集（舊式 Mac 換行 ⇒ 前端產出假欄名讓使用者對映，改為回空模型＋兩端明說不支援）。R5 一群集（🔴 **R4 修法自己**把前端判準做得比後端寬 ⇒ 前端擋／後端收，改採 fail-closed 使兩端同一條規則）＋三家裁定本線**具名收束**為 `R-B4-1`。R6 三家零 finding：codex 以 **9,331 個字串窮舉**比對兩端 predicate（含 BOM／未閉合引號／CR 各位置）`mismatch_count=0`。驗收：`gap3_csv_provenance` 10／`gap3_csv_ragged` 16／vitest 41 檔 235 條／build rc=0／golden rc=0；`pytest tests/api tests/momentum/event_samples` 912 passed／3 failed（既有債名單逐字相同）；**mutation 3→15 條 `closure: CLOSED`**（主委三次靠 `--record` 抓到自身假綠並修，皆寫入 receipt 之 `_why`）。**B5 已收斂（2026-08-26，四輪 review 6→3→1→0，三家一致）**：Task 2.1 篩選面板（`exportFilter.ts`＋`/search`；只篩數值欄且判準用**全樣本**、面板不改任何原始值、條件為空 ⇒ 筆數等於原筆數）、Task 2.2 條件寫入 `label_definition.filters`（形狀先進契約之 `wire_shape`；`event_id` 在該物件**之外**產生故條件不可能滲入 ID；無條件不寫該鍵）、Task 2.3 `computeExportCounts()` 成為計數之唯一來源（Task 1.5 之筆數已改由它算）。**三條必辦殘留全解除**：`D-002 A-004`（新增 `POST /case/lookahead-depth` ＋ R3 出口 `EventSamplePipeline.lookahead_depth()`，下界由唯一深度函式導出）／`R-B3-1`（端到端補上 page runtime）／`R-B3-2`（形狀凍結後精確抽取，不符者仍 fail-closed）。四輪 findings 全部由 codex 提出（composer／grok 前兩輪判可收、R1 五條全漏）：R1 六群集——🔴 其中兩條是**主委在 B3／B4 修過又犯的同型錯**（逐 tf map 被 `Math.max` 塌成 scalar＝SPEC §D-3′-a(ii) 明令禁止之「以單一 batch scalar 冒充 per-scope 下界」；「四個顯示點共用」之宣稱大於實作），另含下界算不出來時 fail-open、下界 72 時答案窗全被鎖死、`filters` 形狀驗證過鬆致多餘鍵可藏欄名、「端到端」停在 API 層未跑 page；R2 三群集——CSV 匯出忽略其正上方之篩選面板、`10**309` 使驗證器丟 `OverflowError`（會變 500）、`version:true` 冒充版本 1；R3 一群集——CSV 與畫面 `N` 之口徑差（修法取「說出來」而非讓 CSV 丟資料）；R4 三家零 finding。🔴 **R2 之 composer 對「R1 為何漏抓」之五層自陳**（執行期接線／狀態語意／B3 同型禁令／契約精確驗證／UI 域邊界）已採為往後同型批次之常設條款：**page runtime RECHECK ＋ malformed-filter probe ＋ 混 TF map 不得塌平**三件套。驗收：`gap3_export_filter` 32／vitest 45 檔 269 條／build rc=0／golden rc=0；`pytest tests/api tests/momentum/event_samples` 944 passed／3 failed（既有債名單逐字相同）；**mutation 10→19 條 `closure: CLOSED`**（主委三次靠 `--record` 抓到自身假綠，另兩次由 runner 之字面錨點 fail-loud 擋下過期 mutation）。**B6 已落地（2026-08-26，待三家 code review）**：Task 3.1 `DELETE /api/v1/case/events/{import_id}`（刪除範圍以 `batch_paths()` **枚舉**「`<import_id>.` 前綴之檔＋同名目錄」而非寫死單一檔名 ⇒ TODO 所要求之「刪除範圍隨 Phase 1／2 新產物同步擴張」成為**結構保證**；path traversal 防護抽成 `payload_path()` 之**唯一實作**，落檔端與四個讀取／刪除端共用；不連帶刪 kline 快取／Feature Library，不提供刪除全部端點；不存在之 id ⇒ 404）、Task 3.2 前端刪除鈕與二次確認（新元件 `EventBatchDeleteDialog.tsx`；不以 `window.confirm` 帶過，未確認 ⇒ **真實 `fetch` 之 DELETE 呼叫次數 0**（不 mock api helper），確認鍵刻意不設 `disabled` 以免形狀假綠）、Task 3.3 已被引用之警語（疊加於**同一**確認框，仍可刪；未被引用者不顯示）。🔴 **Task 3.3 之「已被引用」判準已具名請三家裁（`RULING-1`）**：偵察確認 `event_import_id` 在 `api/`／`momentum/` 完全不存在、`analyze()` 不落任何檔、`ic_survivors_*` 以 `case_id` 為鍵 ⇒ 現況**無伺服器端資料來源**；主委落地之臨時案（前端本機引用紀錄）標 PENDING-RULING，且刻意把資料來源與確認框切開（元件只吃 `isReferenced` prop）使改裁只需換 provider。驗收：`gap3_event_delete` 9（下限 4）／vitest `eventBatchDeleteConfirm` 6（下限 2）＋`eventBatchDeleteWarning` 7（下限 2）／vitest 全套 47 檔 282 條／build rc=0；**mutation 12 條 `closure: CLOSED`**（receipt `handoffs/run_receipts/gap3ux-b6-all-mutations.receipt.json`）。🔴 `--record` 再次抓到主委**兩條自身假綠**（皆錄到空紅集合）：①路徑穿越之斷言只走 HTTP，而 ASGI／httpx 在路由**之前**即正規化 `..%2F`，請求到不了防護 ⇒ 改在服務層對決策點直接斷言；②引用紀錄之型別過濾在畫面路徑上不可觀測（`Set.has()` 嚴格比較）⇒ 補單元層斷言並另立 `3.3-M4` 涵蓋畸形形狀側。**B6 已收斂（2026-08-26，六輪 review 5→2→2→1→兩家零→0，三家一致）**：R1 五群集（🔴 孤兒產物**永久刪不掉**——存在判準只問事件檔而刪除範圍枚舉全部路徑，**兩者不同源**；確認後無防重入，是上一批「確認鍵刻意保持可按」之修法自己開的破口；警語用全域語氣而紀錄只在本機＝「說了一件不成立的事」，codex 判此比恆不顯示更糟；測試側殘留掃法過寬會**誤紅正確實作**；同名 symlink 目錄使 `rmtree` 拋 OSError ⇒ 500）。R2 兩群集（**斷掉的** symlink 之 `is_file()`／`is_dir()` **皆為 False** ⇒ 漏枚舉，唯一殘留時**永久 404**；跨批 settle 競態——A 的回應關掉使用者正在看的 B，**又是 R1 修法自身之相鄰面**）。R3 一群集（`deleteBusy` 之同步行為**廉價綠燈**，grok 實跑 mutation 證明拿掉後仍全綠）＋ `RULING-2` 交裁。R4：`RULING-2` **三家一致裁 ①**（跟隨指向目錄的 root symlink＝既有 storage 語義，**codex 撤回其 P1**；該裁定**不放寬** ownership 邊界，溢出由 `R3I-M1` 鎖住）＋群集 J（composer／grok **兩家獨立**發現 `setDeleteError` 之比對閘無回歸鎖——R2 修法鎖了成功側、**失敗側沒鎖**，正是主委 brief 必答 3 自問之「`errorMessage` 是不是下一個 `deleteBusy`」，答案為是）。R5 因 composer 執行端 `resource_exhausted` 無產出致 quorum 不足，依工具**設計內**逃生口 `--abandon --kind collection-failed` 銷帳（**未改治理碼**）後重派 R6。R6 三家一致零 finding：群集 J 由原提出方 composer 重跑（baseline 19 → ungated 僅 ⑪ 紅 → 還原 19），三家實跑排除**第三個**同型塌平。`RULING-1` ＝ 選項 1（本瀏覽器曾成功分析過，附「文案須揭露本機範圍」之條件）。驗收：`gap3_event_delete` 15／vitest `eventBatchDeleteConfirm` 11＋`eventBatchDeleteWarning` 8／vitest 全套 47 檔 288 條／build rc=0／golden rc=0（sha 未變）／全量 959 passed／3 failed（既有債名單逐字相同）；**mutation 12→23 條 `closure: CLOSED`**。🔴 主委四次「宣稱大於實作」皆由 `--record` 之空紅集合或委員實跑當場抓出並具名記錄。🔴 **本行原停在「下一步＝B7」已過期三批，2026-08-28 補記**：**B7（Phase 4，三輪 7→3→0）／B8（Phase 5，五輪 5→1→4→2→0，非單調係 R3 首次請三家獨立重掃全部 21 條 definition＝新開攻擊面）／B9（Phase 6，IC 止血閘，五輪 10→3→4→5→3）皆已收案**；`D-004` 三家 APPROVED。**B10（Phase 7 全棧接線，9 Task）施工中（2026-08-28）**：偵察輪依規約由 Claude ＋三委員**平行各產一份**（非 Claude 先產委員後審），三家 17 條 findings **全部成立、主委逐條複驗無一被推翻**——其中兩條推翻主委自己的猜測（`_run_analysis` **連事件分支都不存在**、`event_forward_return_table` 之 `strata` **早已存在**，照錯誤基準開工會新建第九頂層鍵而違反 Task 7.5 要點 1），另查出 TODO 行號全數過時、以及六個標「無依賴」之 Task 至少有四條隱藏邊（`7.3←7.6`／`7.6←7.1`／`7.7←7.0b`／`7.4←4.1c 抽常數`）。**Task 7.0 ✅**（五維度參數化＋§F-3′ 把 `entry_price_semantic` 預設由 `trigger_open` 改 `trigger_close`；G-2 golden byte 級不變、**未重凍**；vitest 10；mutation 7 條 `closure: CLOSED`）。**Task 7.0b／7.7 🔧 過半**：新建 `keys.py`（三個唯一取值點）／`label_value_from_case.py`（5 個 frozen dataclass、唯一 normalizer、F-1′ 矩陣、§D-3′-a(ii) purge 權威式、spec 位元組綁定）／`check_feature_run_coverage()`；`EventSplitConfig.embargo_ms_by_symbol` 落地**解除殘留 `R-B3-3`**；`ICAnalyzeRequest` 增 `event_import_id`／`event_label_spec`；`RunInfo.time_range` 全鏈 wiring；`analysis_rejected` 由 1 個增為**五個** reason。驗收 `-k analysis_label_producer` 19／`-k feature_coverage_gate` 18／vitest `runInfoTimeRange` 3／event_samples 332／gap3 api 189／vitest 全套 60 檔 347。🔴 **新開 `docs/GAP3_EVENT_UX_TODO.D-005.md`（未過戳記）**：A-023＝FROZEN 條文**四條互斥**（驗收②要 signed／stage-5 簽章無 direction／`WindowRow` 恰七鍵／spec 恰四鍵），三家 P0 一致判須開延伸檔；實跑確認 consumer 側**無第二次乘 direction**（`ic_filter_orchestrator:2894-2908` 只做 finite gate、`ic_feed:80` 純複製）⇒ producer 回 unsigned 會使 short 批次 IC 符號**整個反轉**且無人補救。採 codex＋composer 之批次 scalar 版、否決 grok 之 `WindowRow` 第八鍵版，**理由為碼證非票數**（SPEC L3075 分類表把 direction 列批次事實欄且逐字寫「它決定 short 取負」／`import_contract.py:694-696` 已對混方向 fail-closed ⇒ 逐列欄會造出「可表達但非法」之狀態／`WindowRow` 七鍵被 §G G-3 signature 對證引用）；grok 異議具名保留並寫死失效觸發。A-024＝`⑧(a)` 前綴保留判準由「全域」改「逐 namespace ＋三條」——實跑證明舊式等於「非末端 namespace 永遠不能加欄」與 D-6 直接互斥，改形後**多抓一種**壞法（namespace 重排）。🔴 **上列「尚缺」已全部補完（2026-08-28 補記，本段取代之）**：`7.0／7.0b／7.7` 經 R1（14 條 findings 三家全數成立）／R2（原提出方逐條 CLOSED，另抓一條真 P1）兩輪收斂後收案；`D-003`＋`D-005` 戳記輪三家全數 APPROVED。**Task 7.1–7.6 已落地（2026-08-28，待 R3 三家 code review）**：7.1 `selectable(path,dim)=accepted−pathExclusions`（新 `eventDimensions.ts`＋共用元件 `EventDimensionFields.tsx`，兩類不可選值分別顯示契約 `rejected_with_reason` 與路徑排除理由）、7.2 三層機械閘（集合／round-trip／非 enum 欄；比對基準兩側**異源**——UI 走鏡像、期望值讀真契約）、7.3 七項動態揭露改由**欄位級 formatter registry** 驅動並取代 4.1b 之獨立實作（4.1b legacy 驗收全綠＝「⊆」之執行期證明）、7.4 IC decay 邊界揭露收斂為單一 exported 常數、7.5 `strata.by_label` 三組＋`control_kind` 經 `dedupe.py` merge 清單傳遞＋兩個 reason 登記 `ic_report_contract.json`＋三個分組標籤與兩個狀態文字登記 `event_metrics_glossary.json`（**解除殘留 `R-B8-1`**）＋前端三組垂直排列、7.6 detail 端點增 `batch_facts`（封閉五鍵）／`declaration_seeds`／`batch_fact_notes` 與 IC 分析頁「事實唯讀／參數可設定」兩區。驗收：event_samples **345**／`-k return_table_by_label` 13／`-k event_batch_detail_dims` 7／vitest **68 檔 410**／build rc=0／`tsc` 僅 8 行既有債／G-1 golden sha 未變；**新建 G-2 事件表 golden**（sha 由 §G S-9 參考實作算，數值先經 §G S-8 之**獨立手算 oracle** 逐格驗過，含 `horizons=[1,3,7]` 與尾端 omission，非「跑一次抄下來」）；**mutation 34→53 條 `closure: CLOSED`**、verdict 全 PASS、六個 baseline 還原後皆空紅。🔴 **三個主委裁定待三家攻**：(A) `decision_offset_bars` 於 `/search`／`/ic-analysis` 鎖 0 與 7.2 ③「輸入 k ⇒ 落檔 === k」**互斥**，③ 改落函式層＋`/data-preparation`；(B) 混批 `control_kind` 回 `null` 不取第一列——實查 `_HETEROGENEITY_DIMENSIONS` **不含** `control_kind`，SPEC 7.6「異質即 Task 1.8 拒收」對該欄不成立而 7.5 明禁多數決；(C) **保留**既有全批 macro／micro 表、三組另排其下。🔴 **本批 `--record` 又抓到主委一條假綠**：`7.2-M1` 錄到**空紅集合**——我把真契約注入生產元件使兩側同源，「契約改了 UI 沒跟」永遠不紅；另 `tsc` 抓到我測試檔重複鍵（其餘四維度被灌成空字串而測試照樣綠，`npm run build` 不涵蓋測試檔）。工具面：`scripts/mutation_worktree.py` 之 `git diff HEAD` **缺 `--binary`**，遇上 repo 內**被版控的** numba 快取即整個 runner 起不來，前九批僥倖未踩，已修。**R3＋R4 已收斂（2026-08-29）**：R3 三家八條 findings **全部成立、全部修訂、無一被推翻**——群集 A（**P1，三家一致**）＝裁定 A 之「鎖 k=0」只有 HTML `min`／`max` 提示、輸入未 `readOnly`、`onChange` 未 clamp ⇒ 使用者真的能讓落檔／`event_label_spec` 帶 `k=3`，🔴 **且我自己寫了 `expect(input.readOnly).toBe(false)` 把錯的性質釘住**；群集 B（codex＋grok 各自命中）＝`page.tsx` 註解宣稱由 `SEARCH_DISCLOSURE_FIELDS` 選欄而 JSX 硬編七段，**本 epic 第八次「宣稱大於實作」**；群集 C＝前端**其實能** import 契約 JSON（我 brief「我沒查的」猜錯），但**實作刻意不改**——兩家同時指出改了會讓 `7.2-M1` 變 false negative，鏡像是**雙源設計**非技術限制；群集 D＝裁定 A 宣稱的使用者路徑 `/data-preparation` 無機械閘；群集 E＝註解檔名錯。**R4 閉合輪**（原提出方重跑自己的反例，章程 §B8）：R3 八條**全數 CLOSED**（codex 逐條複驗，`R3-M1..M4` 紅集合 2／3／4／2），composer 與 grok **零 finding**；codex 新增 `CODEX-R4-P2-01`（P2，已修）＝`clampDecisionOffset` 之 `Math.trunc` 讓 `1.9` **靜默變成 1**，而契約為 `int`、後端已 `Number.isInteger` 顯式拒絕 ⇒ 前端先截掉會讓那條拒絕永不被看到——**這是 R3 修法自己引入的相鄰缺陷（同型第六次）**，修法＝clamp 只夾範圍＋`step={1}`＋送出前顯式阻擋＋探針兩條。🔴 **我在 R4 brief 具名請委員代打的自我宣稱**（「兩層一起拆才錄得到紅」）由 codex 實跑確認成立。裁定 B／C 三家皆不升格 finding。**mutation 53→58 條 `closure=CLOSED`**；🔴 `7.3-M1` 之字面錨點被群集 B 修訂改掉時 runner **當場 fail-loud**（B8 之設計正確生效），重新對錨後紅集合逐字相同。驗收：event_samples 345／gap3 api 十九項選擇器 279／vitest **68 檔 419**／build rc=0／tsc 僅 8 行既有債／G-1 golden sha 未變；兩輪 attribution／completeness 皆 rc=0、債皆已清。**下一步＝使用者 UAT B 段 13 項簽字 ⇒ epic 收案（未簽字不收案）**；另因本批動過 `scripts/` 須跑一次 `gov_check.sh --no-probe` | registry #3＋SPEC／TODO（FROZEN）＋`docs/GAP3_EVENT_TODO.D-001.md`＋`handoffs/reconcile/20260821-gap3-b{1-review-r4,2-review-r3,3-review-r2,4-review-r4}/synth.md` |
| GAP-4 Pooled/Panel IC | 未開票 | — | 同上 #4 |
| GAP-5 容量 ADV 接線 | 未開票 | **條件觸發**：volume 資料源 | 同上 #5 |
| GAP-6 430K 規模防護 | 未開票 | 併 IC-PERF epic | 同上 #6 |
| 票 A（timing-overlap 診斷） | **未開票** | Phase 4；前置見下節 | 本檔下節 |
| 票 B（多標的橫截面 attribution） | **未開票，條件觸發** | 只有宇宙變多標的才成立 | 本檔下節 |
| FU-1 exposure `fillna` fail-closed | 未做 | 碰到再處理 | 本檔下節 |
| FU-2 cache close carrier index 對齊 | 未做 | **票 A／B 的硬前置** | 本檔下節 |

🔴 **優先序（2026-08-14 使用者明示「現在開始就是要回去做量化主線」）**：
量化主線 **優先於** 治理。此句覆蓋兩條舊裁決——P0 之「完成後才回 IC」（2026-07-05）、
「治理優先於產品線」（2026-08-04）。

---

## 量化主線（IC 分析）

**已完成**：`ic-la0` → `ic-la1` → `ic-la2`（前瞻整治三站）→ `ic-1c`（Net IC 量綱）→
`ic-1cfr`（canonical 因子報酬序列 F0–F5.2）→ `ic-1d`（factor attribution 六批 B0–B5）→
`ic-1e+1b`（HAC 顯著性＋FDR 接線＋xsec p 值；**本行 2026-08-17 補記，原漏列**）。
🔴 `ic-1d` **B4／B5 亦已完工**（2026-08-14 逐項查證：7 支 mutation 探針＋cache/force 兩測、
`FactorExposureRadar.test.tsx`、Radar 契約地雷殘留 0、ExportButtons 舊判斷殘留 0、前端 triage 檔皆在）。

**✔ IC 全棧健檢 epic 已收工（2026-08-17）**——四步全走完（偵察四方 reconcile→SPEC/TODO 凍結
→六批實作 3×P0 修復＋契約 SoT＋wiring 閘門→三家 code review 全 CLOSED 三家戳記）。
原定四步（存檔備查）：

1. **discovery sweep**：Claude＋三委員平行，產「後端產出／前端消費／wiring／空態」四欄表
2. **quant gap analysis**：現況 vs 業界；複審 4 個 deferred（funnel／capacity／regime IC／walk-forward+CPCV）
3. **建 typed 契約 SoT ＋ wiring 閘門**（順手修 #1 幽靈＝原 `1f`）
4. 跑閘門確認閉合，之後自動守

設計原則（使用者洞察）：①audit 先天不完整 ⇒ time-box ②**手動快照會腐爛 ⇒ 把發現做成機器閘門**
③分層防禦。底稿＝`handoffs/20260624-ic-map-WHOLEMAP.md`（**6/24 版，已隔月過時，須逐條複核**）。

✅ **底稿複核已完成**（2026-08-17，四方獨立＋reconcile）：28 條逐條標定＝已修/部分修/變形 ≥15、
未修 7、未查具名 6；三個 P0 仍活（分位圖巢狀 schema 空圖／xsec 硬編空殼／事件 silent fallback）。
白話版＝`白話說明/IC健檢偵察結果.md`；技術收斂＝`handoffs/reconcile/20260817-ichc-x-consult-r1/`（本地）。

🔴 **本節曾整段消失十天**：`aae04295`（2026-08-05）把 ROADMAP 由 393 → 111 行、「敘事移出 Archived」，
連同量化主線的下一步一起砍掉，直到 2026-08-14 使用者追問才發現。完整敘事仍在
`docs/Archived/ROADMAP_P16_NARRATIVE_20260805.md:139-157`。

### 後續兩票（皆 Phase 4，不插隊）

- **票 A — 策略 timing-overlap／clone score 診斷**：回答「ML 是否只在做簡單因子規則」。
  **開票前置＝先修 equity curve 契約**（**已於 2026-08-18 PA-CUMSUM 完成**：`EquityCurveData` 改單利／複利四序列＋四鍵終值、多標的逐 timestamp 等權組合、缺值 fail-closed）；
  殘餘：`prediction_analyzer.calculate_strategy_equity_curve` 只有 long/flat 無做空、
  `api/routes/pattern_analysis.py` 之 `actual_return.fillna(0)` 仍在（缺報酬視為 0，`predicted_proba` 缺值已改 4xx）。
- **票 B — 真·多標的橫截面 attribution**：**條件觸發，只有宇宙變多標的才成立**。
  前置＝CS factor-return 管線（`factor_return_analyzer.py:272-287` 現僅收單一 `future_returns: pd.Series`）
  ＋持倉權重 canonical 定義＋`analyze_cross_sectional` 與 deep 棧整合。
- **根因備忘（防未來重撞）**：單標的下 `ls_returnᵢ=positionᵢ⊙r`、組合報酬＝`position_p⊙r`，
  共用同一 `r` ⇒ OLS 只識別 **position 重疊度**非風險曝險。β 可誠實命名 timing-overlap，
  **禁冒充 Barra attribution**。

### 兩筆 follow-up（2026-07-22 三方 IN-SCOPE-PASS 後登記，防丟）

- **FU-1 exposure 家族 `fillna` fail-closed 化**：`factor_exposure_analyzer.py:111-307` 三函式壞值靜默
  `fillna(0.0)`。嚴重度中（預設 `enabled=False`、僅餵 Radar 診斷非交易決策）。修法＝比照 `1d` B2。
- **FU-2 cache close all-NaN carrier index 對齊**：kline `RangeIndex` vs features `DatetimeIndex` 對不齊。
  **是票 A／票 B 的硬前置**（全 NaN carrier 上無法接真歸因）。

### 🔴 票 MEM-RSS：Feature Factory 之記憶體閘以 RSS 為判準，在 macOS 上可能**漏擋**（2026-08-27 登記；**不插隊**，使用者裁定先做完 B9／IC-Analysis）

**觸發**：GAP-3 B9 之 Task 6.2 實跑量測時，同一時刻實測 **RSS 72MB vs Physical footprint 5.7GB（差 79 倍）**。
機制：macOS 之 memory compression 會把不常碰的頁**就地壓縮**，壓縮後**不再算 resident**
⇒ 記憶體壓力愈大、RSS 反而愈小，**方向是反的**。

**受影響之處（讀碼所得，逐條可查）**：
- `momentum/FeatureEngineering/feature_factory.py:2215`
  `if float(ic_memory.peak_rss_gb) > peak_budget_gb: raise MemoryError(...)`
  ——**這是會真的擋下執行的閘**，判準為 RSS ⇒ 真正快爆時 `peak_rss_gb` 反而變小、閘門讓它過去。
- `feature_factory.py::_check_ic_memory_budget_after_raw_persist`
  以 `rss_before - rss_after` 當「gc 釋放了多少」⇒ 頁面只是被**壓縮**時也會得出好看的正數。
- `memmap_utils.py:213` 之 RSS 僅供 log，不做決策 ⇒ 不受影響（但同樣會誤導讀 log 的人）。

🔴 **誠實邊界**：以上為**讀碼＋機制推論**，**未對 Feature Factory 實跑對照量測**。
要斷定「該閘實際漏擋過」，須拿真實大 run 同時量 RSS 與 footprint 對照。
另：Linux 無預設記憶體壓縮，影響顯著較小（RSS 僅少了 swap 部分）——**本票之嚴重度綁 macOS**。

**修法（順序不得顛倒）**：①先做對照量測，**證明現行閘會漏擋**；②再把判準換成
macOS＝`sample`／`footprint` 之 Physical footprint、Linux＝`smaps_rollup` 或 cgroup 統計；
③保留 `_available_ram_gb()`（判系統可用 RAM，不受本問題影響）。
🔴 **先證明再改**——那是一道會 `raise` 的閘，憑推論就動它可能把正常的 run 擋掉。

**可複用**：`scripts/measure_ic_footprint.sh`（B9 產出，已含單一 pid 判定與安全閥）。

---

## 測試策略（2026-08-14 使用者定：「邊走邊建立」）

**建測試時的優先序**（前三類的紅綠，使用者可在**不讀程式碼**的前提下採信；第四類不可）：

1. **性質檢驗**（`t` 不得依賴 `t+1`、跨 symbol 換料另一標的輸出不變、合併前後守恆）
   ——**不需要凍結期望值，所以不會過期**
2. **真實 kline**（`data_cache/feature_klines/kline_cache.h5`；禁合成 fixture，既有鐵律）
3. **與第三方實作對照**（`scipy`／`statsmodels` 等）——量尺不是本專案產的
4. ⚠️ **凍結 golden 比對**：能不用就不用；非用不可時**改行為的當下必須重凍**

**病根（使用者 2026-08-14 指出，邏輯上無反駁餘地）**：基準與測試**兩側都是 Claude 產的**，
拿一個量另一個是循環論證 ⇒ 使用者無法判斷紅綠真假。**只有非本專案產生的量尺逃得出這個圈。**

**既有 32 個失效基準／40 個疑似孤兒＝不大清**（`scripts/golden_staleness_check.sh` 的歸屬判定
**已實測有兩個 bug**——檔名碰撞與動態組路徑，其輸出**不得用於刪檔**，詳見該檔頭）。
處置＝**碰到才處理**：動到某模組而其 golden 炸了，當場決定重凍或作廢。

---

## ✅ 已完成

歷史條目移至 `docs/ROADMAP_DETAIL.md`（**搬走不等於作廢**；要作廢請明寫）。
