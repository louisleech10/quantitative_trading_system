# #2 d* / FracDiff 非 CGSA 對齊 Manifest（V3｜2026-06-15）

> V1→V2：雙家族 adversarial（Codex 6B/5M + Composer 2B/5M，FAIL）reconcile。核心 reframe：**d* map parity 為主 oracle**（儲存格式無關），value parity 為次(雙 extractor 同名直比,exact-only)。
> 來源：使用者決策「選 A」+ 設計委員會三方一致 + adversarial 雙家族。

- [D2-1] 根因（§A 三方驗，行號 V2 修正）：非 CGSA `transform()`（feature_preprocessor.py:215）→`_transform_single`→`_transform_single_legacy` fracdiff（:2505-2508）→`_filter_fracdiff_target_columns`（:2886-2929）用 regex `^(L\d+)_` 對裸欄名全 unparsed→fracdiff no-op。**ADF（:3311-3378）獨立掃描，不由 fracdiff target empty 推出 no-op**（adversarial 修正）。factory L6.5 接線 :2478-2517。CGSA 走 `group.layer∈{L1,L2}`。
- [D2-2] provenance：`_combine_layers`（feature_factory.py:3619-3651）前 `layers=[L1..L6]` 有來源、合併後遺失。factory 新增 `_build_column_layer_map(layers)`（keep-first 對齊 dedup；fail-fast assert 非 str，不混用 str(col)）。**map 經 factory instance attr `self._column_layer_map`**（generate_features frame path build 時設，`_run_layer6_5_preprocessor` 讀並傳 `FeaturePreprocessor(column_layer_map=)`，**不改 `_layer6_5_legacy/_pre_ic` wrapper 簽名**；CGSA path 設 None）——解 Codex r2-B3 map 穿 wrapper。P0 另凍結 **CGSA `ColumnGroup.layer` provenance** 並交叉核對同名欄 layer 一致（B3）。
- [D2-3] 接線（scope 修正）：`_run_layer6_5_preprocessor`(現行 3 參數簽名不改)**讀 factory instance attr `self._column_layer_map`** 傳 `FeaturePreprocessor(column_layer_map=)`;`_transform_single`/`_transform_single_legacy`/`_transform_chunked`（:2651 flat path）傳遞。**移除 `_group_requires_slow_transform`（:1167-1206）**——該函式由 CGSA registry serial/parallel/raw-sink 排程呼叫（adversarial #4：違反 CGSA 禁改），flat path 不經它。post_ic（fracdiff 已關）為全量 map 子查詢。
- [D2-4] filter 優先序：`source_layer is None` 時 `ALL→explicit map（col in map and map[col] in fracdiff_layers）→regex fallback`；unknown 欄 fail-closed non-target+warning。CGSA `source_layer` 分支不動。
- [D2-5] 禁項：Lx_ 前綴 / attrs / 逐層 transform / 欄名 heuristic。
- [D2-6] tier2a 同批不同根因：builder（build_l65_golden.py:236-241）設 `_d_star_cache={}` 後讀，現行建 ephemeral DStarCache 不回填→空。同 PR 分開修:builder monkeypatch `_d_star_cache_dir`→tmp,transform 後 glob 單一 `d_star_*.json` 用 `read_d_star_json` 直讀(見 D2-7),斷言 **`all(layer_of_feature(key) in {"L1","L2"})`**（layer_of 來自 synthetic inventory，**非** d* key 直接比 layer label——adversarial #5 型別修正）+ 無 L3-L6 key。
- [D2-7] d* 公開 API：`DStarCache.export_d_star_values()->Dict[str,float]`（避免觸 private `_entries`）。builder lifecycle(Codex r2/r4-M10):monkeypatch `_d_star_cache_dir`→tmp,transform 後 glob tmp 單一 `d_star_*.json`(assert 恰 1)用 `read_d_star_json(path)` 直讀(**不重建 DStarCache**避 9-param hash 脆弱,**不設 `_d_star_cache_shared`**)。
- [D2-8] **Oracle reframe（核心，adversarial #1 化解）**：
  - **主 oracle = d* map parity（儲存格式無關）**：非 CGSA L1/L2 non-stationary 欄 d* == CGSA 同欄 d*(**fracdiff/d* 計算時兩路皆裸欄名,裸 key 直接比,無需正規化**;V3 Codex r2-B1)。d* 在 cache JSON，不受 parquet/HDF5 差異影響。這是「對齊 fractional differencing order」的真正意義。
  - **次 oracle = value parity（雙 extractor，同名直比）**：**事實修正（V3，Codex r2-B1）——兩路徑都插 tf tag 且格式相同**：CGSA stream `write_raw_from_registry_stream`（feature_storage.py:845-873，用 `group_id` tf 前綴）與非 CGSA `_apply_timeframe_tag`（:3371）都產 `close_12h_trend_EMA_20`。單 TF 12h **兩路欄名一致 → 直接比同名，不去 tag**。CGSA via `FeatureReader.load_columns_v2(symbol,tf,config_hash,cols,artifact_kind="raw")`（parquet）；非 CGSA via HDF5/features_df。**兩側欄名各由其 manifest 列出**;以 P0 provenance 取 L1/L2 expected set;**assert expected 非空+左右 key-set 對 expected exact(非僅交集,防 partial-vacuous)+row-index exact**;canonical float32 **exact**(Codex r2-B1/r4)。
- [D2-9] **exact-only 治理（adversarial #7 後門封死）**：batch2d 測試**禁止**新增 `rtol/atol>0`；value parity 不 exact → `pytest.fail` + 記分案 ID + BLOCKED，**本 PR 不得設 tolerance/改 gate**，另開有 frozen evidence+根因+獨立 review 的 SPEC。control=**顯式 `fractional_differencing.enabled=False` run**（非 HEAD 預設，adversarial Composer #2）。
- [D2-10] **Golden 保真（adversarial #2/#8/#9）**：P0 凍結 **全長 per-column float32 value hash + 全長 NaN-mask hash + row-index hash + ordered-column hash**（非 aggregate 抽樣）；凍結 provenance map；凍結 CGSA 全 baseline（ordered cols+row index+per-col value/mask hash+d* canonical）；凍結 config dump+config_hash+env（FFACT_L65_WORKERS/tier/ADF engine·precision）+ **storage schema version + 關鍵套件/engine version（numpy/pandas/statsmodels/pyarrow）**（Codex r2-M9）+ kline 檔 hash + cache fresh state。P4 讀 frozen P0（非只重跑 live）。cache 隔離：測試 monkeypatch `FeaturePreprocessor._d_star_cache_dir`→tmp（禁寫 repo data_cache）；production path-resolution 另 unit test。**scope 命名=「L1/L2 fracdiff selection + d* parity」**；L3-L6 跨路徑既有差異 out-of-scope（報告 inventory 不宣稱全對齊，adversarial #11）。流程：大型；Codex 實作+Composer review；V2 雙家族確認才派工；CGSA 禁改（frozen full hash 回歸）；欄名/欄數/schema 不變。

## 三方資料正確性裁定（2026-06-16，Codex+Composer 獨立調查一致 + Claude reconcile）
> 報告：handoffs/20260616-d2-parity-investigation-{codex,composer}.md。資料正確性鐵律滿足（三方一致）。
- **T3 d* parity（主 oracle）達成**：fracdiff 顯式 enabled=True、真實 BTCUSDT/12h、隔離 cache 下，L1/L2 交集 d* **exact 100%、0 mismatch**（Codex 3458/3458、Composer 3736/3736，交集定義差，皆 0 不符）。**#2 對齊在有意義層級（同 fractional differencing order）達成。**
- **T4 value parity = 既有結構差異，out-of-scope（非 #2 bug）**：(1) row-index「不符」=表示法（frame int64 epoch 秒 / CGSA datetime64ns，同時間戳）；(2) value 差 = **CGSA 存 float16、frame 存 float32**（max abs~0.0009，NaN mask 全符）+ 兩路 L6.5 執行拓撲不同（registry group+source_layer vs combined frame+chunked+column_layer_map）；(3) 欄集差 = post-L7 dead-drop 存活（delta 41，與 fracdiff-OFF control baseline 同 delta→證 pre-existing）。
- **P4 merge gate（兩家共識，覆蓋 §G T4）**：T3 d* parity pass + control L3-L6 unchanged + CGSA baseline regression；**T4 維持 exact-only→記 inventory 不放寬 rtol**。fracdiff 預設 False，T3 須顯式 enabled=True（不可用 control golden 測 T3）。
- **另立 out-of-scope ticket（本批不修）**：CGSA raw 將 L1/L2 存 float16（vs frame float32）精度落差——值得獨立評估是否該存 float32；與 #2 fracdiff 對齊無關。
