# 測試對齊清單（按模組分批）

> 目標：以現有可執行程式碼為核心，逐批對齊測試並驗證。
> 原則：每一批都需可獨立驗證 PASS，才進入下一批。

## Batch 1（已執行）— Phase24 Analysis 模組路徑對齊

**問題型態**
- 舊路徑 `tests/momentum/analysis/`（小寫）在目前專案不存在，造成 pytest `not found`。
- 對應可執行測試已位於 `tests/phase24/`。

**對齊策略**
- 先以 `tests/phase24/` 作為 Batch 1 標準路徑執行（不改動功能程式碼）。
- 驗證目標：以下 6 個模組測試全 PASS。

**模組與測試檔**
- `deep_analysis_config` → `tests/phase24/test_deep_analysis_config.py`
- `factor_return_analyzer` → `tests/phase24/test_factor_return_analyzer.py`
- `factor_centrality_analyzer` → `tests/phase24/test_factor_centrality_analyzer.py`
- `trend_analyzer` → `tests/phase24/test_trend_analyzer.py`
- `parameter_sensitivity_analyzer` → `tests/phase24/test_parameter_sensitivity_analyzer.py`
- `rolling_oos_validator` → `tests/phase24/test_rolling_oos_validator.py`

**驗證命令**
```bash
/Users/louis/Desktop/quantitative_trading_system/venv/bin/python -m pytest \
  tests/phase24/test_deep_analysis_config.py \
  tests/phase24/test_factor_return_analyzer.py \
  tests/phase24/test_factor_centrality_analyzer.py \
  tests/phase24/test_trend_analyzer.py \
  tests/phase24/test_parameter_sensitivity_analyzer.py \
  tests/phase24/test_rolling_oos_validator.py \
  -v --tb=short
```

**狀態**
- [x] Batch 1 驗證 PASS（62 passed）

---

## Batch 2（已完成）— `tests/momentum/Analysis/` Phase3 核心分析模組

**範圍**
- `lightgbm_analyzer`, `xgboost_analyzer`, `model_comparison`, `model_config_manager`, `model_trainer_protocol` 等。

**重點**
- import path 對齊
- 既有 API/欄位命名差異對齊
- fixture 與 mock 介面對齊

**完成標準**
- `tests/momentum/Analysis/*.py` 選定批次全 PASS。

**驗證命令**
```bash
/Users/louis/Desktop/quantitative_trading_system/venv/bin/python -m pytest tests/momentum/Analysis/*.py -v --tb=short
```

**狀態**
- [x] Batch 2 驗證 PASS（183 passed）

---

## Batch 3（已完成）— `tests/momentum/` 單檔模組測試

**範圍**
- `ic_*`, `cv_validator`, `oot_validator`, `rolling_auc`, `psi_calculator`, `event_filter` 等。

**完成標準**
- 逐模組收斂，單檔執行 PASS 後再擴大回歸。

**驗證命令**
```bash
/Users/louis/Desktop/quantitative_trading_system/venv/bin/python -m pytest tests/momentum/test_*.py -v --tb=short
```

**狀態**
- [x] Batch 3 驗證 PASS（318 passed, 3 skipped）

---

## Batch 4（已完成）— `tests/momentum/Optimization/`

**範圍**
- `test_optuna_objectives.py`。

**完成標準**
- 匯入、依賴、目標函式行為與目前 optimizer 實作一致。

**驗證命令**
```bash
/Users/louis/Desktop/quantitative_trading_system/venv/bin/python -m pytest tests/momentum/Optimization/test_optuna_objectives.py -v --tb=short
```

**狀態**
- [x] Batch 4 驗證 PASS（5 passed）

---

## 最終回歸

```bash
/Users/louis/Desktop/quantitative_trading_system/venv/bin/python -m pytest tests/momentum/ -v --tb=short
```

- 僅在 Batch 1-4 全部 PASS 後執行。

**狀態**
- [x] 最終回歸驗證 PASS（506 passed, 3 skipped, 312 warnings）
