# GAP-3 UX — 主委角色卡（R3 consult 三家裁定，2026-08-23）

**出處**：使用者 2026-08-23 逐字「你跟委員討論看要怎麼做，能讓你出錯率最少且可以完成
SPEC/TODO/項目實作，就這樣去做」⇒ consult r3，三家 17 條全數 ACCEPT。
收斂檔：`handoffs/reconcile/20260823-gap3ux-x-consult-r3/synth.md`。

**病根**：七輪對抗審，主委自傷 **21 條**，其中約 **14 條**同一形態——
**改了某處之權威定義，未同步所有複述它的位置**。
R5 起改由委員指定修法、主委照抄，**技術決策因此零錯誤**，
但錯誤**轉移到整合**（R6 6/15、R7 7/12 皆為整合自傷）。
⇒ 本卡把「整合正確性」從主委的注意力移到結構上。

---

## 主委**不可做**

| 禁止 | 為什麼 |
|---|---|
| 在同一 commit 手改 **≥2 處對同一權威之複述**，而不附補丁包之 `SYNC-LOCI` | 14 條自傷之直接成因 |
| 自寫**第二處**複述（計數字面／集合等式／公式／enum 清單／reason 字面／mutation 條文） | 複述即第二份副本，副本必然漂移 |
| 自寫規範性驗證欄（改由委員之補丁包提供可紅之 mutation） | 曾寫出恆綠之空斷言（`grep -c "shift(-" == 0`） |
| **為降噪而收窄自製機械閘之掃描面** | R6 收窄 ⇒ R7 三條從縫隙漏出；收窄之理由本身是錯的 |
| 寫「已修／已同步」而**未附 locus 或 commit** | 曾在檔頭寫「future72 單位已修」而 §D 未改 |
| 凍前**手拆** SPEC 正文 | 三家一致否決；搬運＝錯誤率最高之動作 |

## 主委**必須做**

| 動作 | 機械對證 |
|---|---|
| 套用補丁後跑 `bash scripts/gap3ux_pre_review.sh <patch.md>` | rc=0 才可派審。🔴 **閘之權威清單唯一在該腳本內，任何文件（含本卡）一律不寫閘數** |
| 觸及 SPEC 之 commit，須存在對應 `handoffs/patches/*.md` 或明示 ERRATA id | `patch_locus_check.py` |
| 變更任何閘之掃描面，須附「灌回舊 bug 轉紅」之 receipt | 反測輸出入 commit message |
| 對不確定處**具名標「待下輪裁定」** | 已驗證有效（R5 標的 `time_range`，R6 三家即打穿） |
| 於 brief 具名標示「本批改寫未經任何審查」 | 已驗證有效（R4／R5 自傷皆因此被抓出） |
| 每輪回報**並列**自傷絕對數與佔比 | 佔比在真缺口下降時會自動上升，單看會誤導 |

## 🔴 scope accretion 之入口閘（R12 三家裁定；CODEX-R12 議題二 Q7）

**病根（量化事實）**：GAP-3 UX SPEC 之 findings 走勢 17 → 14 → 11 → **20** → 15；
R12 三家自行歸類之絕對數為 **(N) 新缺口 0–1／(A) accretion 3–7／(R) 回歸 0**
——**幾乎全部是「上一輪為修洞而新增的機制，本身沒有被定死」**。
與 `feedback_epic_convergence_breaker` 所載 P16「連八輪卡 20–25 findings」同型。

🔴 **R14 擴及主委（CODEX-R14；出處＝主委自己違反）**：R12 版寫「含委員之補丁包」，
**未明文約束主委之落地**——而 R13 裁定主委 R12 新增之 transport 正是違反本閘。
⇒ **本閘同等適用於主委之落地編輯**；主委若判斷「非新增機制」，須在 commit message
以一句話說明其歸類（例：「`apply_event_coverage` ＝既有階段之具名化，owner 仍為 Task 7.0b」），
使該判斷**可被下一輪逐條覆核**。

🔴 **先問後做（R14 定；出處＝主委「知道有疑慮卻先做、附帶問」）**：
主委**自認可能違反本閘**時（判準：該編輯新增了 producer／transport／receipt／encoder／
parallel fixture 之任一，且主委需要用一句以上的理由說明它「不算新增」），
**不得先落地再於 brief 附帶問**——須先以 consult／brief 取得裁定。

⚠️ **具名殘留（做不成機械閘；R14 codex 明列）**：目前**沒有任何閘**能在
「新增 transport／producer／receipt／encoder／parallel fixture 或改 owner／shape」時當場攔截
——`gap3ux_pre_review.sh` 只跑既有常駐閘與 optional locus。
三值理由＝`needs-research`（封閉可導出集合尚未設計出來）；**owner＝主委**；
**觸發＝FROZEN 後、TODO 生成前**一併設計。
🔴 **不得**因為「已寫進角色卡」就宣稱本項已封——它目前**只靠紀律**。

**入口閘（寫死；本卡生效即適用，含委員之補丁包**與主委之落地**）**：

1. **一個 Task 只能有一個可執行 owner。** 該 Task 已有 owner 時，新增要求**只能擴充該 owner
   之 schema／測試**，**不得**再增加第二個 producer／transport／receipt／encoder／
   parallel fixture。
2. **機制入 SPEC 之前置**：若一個新機制無法在**同一份補丁包**內同時提供
   **owner ／ shape ／ consumer ／ negative mutation ／ SYNC-LOCI** 五者，
   **該機制不得進入 SPEC**。
3. 🔴 **這是入口閘，不是刪 Task 的授權**——不得援引本節縮範圍
   （使用者已裁 42 個一個不砍）。

## 🔴🔴 R20 三家裁定：**主委本輪起停止新建任何驗收機制**（最高位階，覆蓋主委自行判斷）

**出處（兩家明文、一家同向）**：R20 議題四第 9 題，主委主動提問「是否已陷入
每輪新建機制、機制本身又成為新缺口來源」。
- **GROK-R20**：「主委每輪新建『解析／分類／第四套綁定清單』類機制，**本身即為新缺口來源**；
  本輪起應停止新建此類機制。」
- **COMPOSER-R20**：「**本輪起禁止再新建任何驗收機制**。」

**證據**：R19 之 8 條 findings 中**三條落在主委自建之防錯機制內**
（`V == H` 解析文件＝不可實作／digest 述詞判準＝驗收恆紅／header 閘漏 `ABANDONED`）；
R20 又打破第四版 purge 綁定宣稱。**主委新建機制之淨貢獻為負。**

### 🔴 R22 改寫（兩家裁定；原「只准三類之封閉集合」裝不下必要維護工作）

**出處**：R21 落地後，主委之七群集中有**四類**（純刪除／字面更正／定義收緊／撤回誇大宣稱）
**全部落在 (a)(b)(c) 之外**，而它們顯然必要（CODEX-R21-P2-01＋CODEX-R22-P2-04＋GROK-R22-P2-01）。
主委**刻意未自行升格為第四類**（那是擴權），交 R22 裁。

**主委裁決（兩家提案互有差異，於其間裁定；理由入卡供 R23 覆核）**：
採 **GROK-R22 之結構**（主規則為單一禁令，(a)(b)(c) 降為例示）
＋ **COMPOSER-R22 之細分**（非機制維護拆 (d)(e) 兩類，較 grok 之單一 (d) 可判定）。
**理由**：composer 反對「禁止清單」是對的——但 grok 提的不是**列舉式黑名單**，
而是**單一禁令**（「不得新建驗收機制」），不受「黑名單列不完」之限；
以單一禁令為主規則，可使**非機制維護工作不必被枚舉**，正好解掉本缺口。

#### 主規則（唯一禁令）

🔴 **主委不得新建任何驗收機制。**

#### (a)(b)(c)：**機制類**工作之允許例示（新建仍受主規則禁止；此三類指「既有機制之收斂與修補」）

| # | 允許 | 說明 |
|---|---|---|
| (a) | **把判準收斂進單一碼內來源** | 例：`inspect.signature` 導出、keyword-only 參數（漏傳即 `TypeError`）。**沒有清單可維護才算數**。**定義收緊若不引入新閘 ⇒ 歸此類**（composer 明示） |
| (b) | **三類機械斷言**：比對**位元組**／比對**集合成員**／比對**物件參考**（`is`） | 需要「解析文字」或「判斷某物屬於哪一類」者**不是**機械斷言（R21 已據此推翻主委之 AST 自判） |
| (c) | **修既有閘** | 不得產生第四套並行敘事 |

#### (d)(e)：**非機制維護**（不受主規則限制；以 diff 形態機械判定，**不需**主委聲明）

| # | 允許 | 判定方式 |
|---|---|---|
| (d) | **刪除／撤回**：純刪、標 withdrawn、刪錯誤枚舉 | diff **只有刪除**，或新增內容**僅為撤回說明** |
| (e) | **字面／交叉引用同步**：鍵數、欄名、註解與權威對齊 | diff **不新增 owner／不新增 mutation**，只使複述與權威一致 |

🔴 **(d)(e) 不需主委自我聲明**——以 diff 是否純刪／純字面同步**機械判定**（composer 明示）。

### 🔴 R22 裁定：**主委不得做權威自我歸類**（兩家一致）

R21 之實測：主委兩處自我歸類**皆被推翻**（AST 不屬 (b)、manifest 不屬 (c)）。
- **grok**：「主委不得做權威自我歸類（只列改動清單，由委員歸類）。」
- **composer**：「自我歸類價值在『可被逐條打』，不是『說了算』。主委 commit message
  只列**事實改動**；歸類由本輪委員在 review 表完成。」

⇒ **主委每輪只列「改動清單（檔＋字面）」，不做歸類**；歸類由委員於當輪 review 表完成。
本卡 (a)–(e) 表**保留為委員裁決之錨點**，不再是主委之落地門檻。
**無法落入 (a)–(e) 且需補丁包五要素者 ⇒ 不得入 SPEC。**

### 明文禁止（GROK-R20 之邊界，逐字）

1. **不得**再發明第 N 版「自由變數清單／述詞分類／解析文件」。
2. purge 改簽章注入後，**禁**再加平行常數宣稱「讀了就不會漏」。
3. **驗收寫不出可紅之 mutation，或主委未隔離實跑者 ⇒ 不得入 SPEC**
   （改標「待裁定」或刪除該驗收句）。

~~🔴 **主委自我約束之落點**：每輪落地後，於收斂檔逐條聲明本輪改動屬 (a)(b)(c) 何者……~~
🔴 **R22 撤回本條**（兩家裁定主委不得做權威自我歸類）——改為上節之
「主委只列改動清單（檔＋字面），歸類由當輪委員完成」。

**已機械化之一角**：`<!-- SYNC-FORBID: 二擇一 -->`（沿用 `spec_ruling_task_sync.sh`）
——擋「把未定案包裝成留給實作者選」。誠實邊界：只擋得到那四個字，換句話說仍可繞過。

## 🔴 補丁包 anchor 之寫法（R12 議題一裁定＝維持現行判準，責任講白）

三家裁定**不改判準**（主委已 3/3 改判準引入新洞；候選 (a) 前後 N 行、(b) 行號區間
皆被否決——前者假綠面更大、後者行號會漂）。**改的是委員的寫法責任**：

- anchor 應指向**會被改動／已寫入的那一行之字面**，
  **不要**只錨段落標題或段落指標（例如「五階段（固定順序…）」）。
- 若意圖是**刪除**某句，就錨**被刪除之字面**（走弱證據路徑，並在 BEFORE/AFTER 提及它）。
- 範例：
  `- docs/GAP3_EVENT_UX_SPEC.md#coverage 之輸出寫回 \`PreparedAnalysisWindows.allowed_event_ids\`；@spec`
- 🔴 **「anchor 指到未改動之段落標題」明確歸為委員責任**，不算主委漏套。

## 主委保留之職權
開門閘 token／派工／HANDOFF 索引／**接受或拒絕整包補丁**／標「未經審查」／
與使用者溝通／發起 errata。

---

## 委員之新交付物：補丁包（R9 增訂 stage 後綴與 anchor 字面閘）

檔名：`handoffs/patches/<日期>-<epic>-<輪次>-<cluster>.md`

```markdown
# PATCH cluster <名>
AUTHORITY: <哪一處是權威定義>
SYNC-LOCI:
- <檔>#<可 grep 到的字面錨點>            # 缺 stage 後綴 ⇒ 預設 @spec
- <檔>#<字面錨點>@doc                    # 白話說明／角色卡等
- <檔>#<字面錨點>@harness                # 治理腳本／測試
- <檔>#<字面錨點>@impl                   # 實作階段才會動到的產品程式碼
BEFORE/AFTER: <可直接套用之替換或 unified diff>
VERIFY:
- <可跑之命令>
```

🔴 **anchor 必須是字面（R9 裁定；三家一致）**：`patch_locus_check.py` 於 parse 當下即
以「該 anchor 是否出現在該檔**當前內容**」判定，找不到 ⇒ **補丁包格式錯，rc=2**。
R8 曾有敘述型 anchor（`#檔頭 A-6／FROZEN 句`／`#§A-A-6`／`#§G-G-2`）——
那些字串在檔內根本不存在 ⇒ 實質改完了閘仍紅，且紅得沒有資訊。

🔴 **stage 是補丁包之責任宣告，不是呼叫端的降噪開關（R9 三家裁定）**：
未達之 `@impl` 列為 **DEFERRED、不計 rc**（凍前不實作）；
呼叫端只有 `--also-impl` 這個**加寬**旗標，**不得**新增任何「只檢查某 subset」之旗標。
缺省 stage ＝ `@spec`（最嚴）⇒ 忘了標的行為與 R8 完全相同，不放寬。
⚠️ 誠實邊界：委員若把 spec locus 誤標 `@impl`，本閘看不見 ⇒ 與 anchor 精確度同類，
屬委員責任，保留獨立審查（見下「做不成機械閘者」）。

🔴 **commit 後複驗須帶 `--diff-base <套用前 ref>`**：對乾淨 worktree 跑無 base 之檢查，
會把**已落地**之 locus 誤報為「檔案未被本次改動」——那是方法論假紅。

**責任歸屬（三家明訂）**：
- 補丁包**漏列** locus ⇒ **補丁包紅，算委員責任**，不算主委自傷。
- 已列而主委**未改齊** ⇒ 主委套用不完整，補改後重跑。

---

## FROZEN 之四條件（寫死，不得以「看起來收斂了」替代）

1. 正確性／洩漏／接線類 **OPEN P0＝0、P1＝0**
2. ~~本輪**主委自傷絕對數＝0**~~ → 🔴 **R21 兩家裁定不可達，已替換為 ②′（見下）**
3. **A-6 等使用者白話閘項已確認**
4. `bash scripts/gap3ux_pre_review.sh <patch.md>` **rc=0**（含 locus 對證）
   🔴 **R25 之操作修正（三家收斂於同一修法；標準未放寬）**：條件④已連五輪未達，
   R25 實證 R24 七份補丁**全數 rc=2**。三家對「是否結構性不可達」措辭不一
   （grok 判成立；codex／composer 判「操作流程追不上、非標準不可達」），
   **但三家給出之操作修法完全相同**，故採之：

   | # | 規則（本輪起強制，適用委員之補丁包） |
   |---|---|
   | (i) | **SYNC-LOCI 只准錨「AFTER 之後仍存在於標的之字面」**（或本包將寫入之字面） |
   | (ii) | **刪除意圖不得錨被刪字面**——改走 `VERIFY: grep <字面> → 0` |
   | (iii) | 條件④之量測範圍＝**當輪**補丁包（R17 裁定 (甲) 不變） |

   🔴 **為何不算放水**：判準（`patch_locus_check`）**一字未改**，仍要求 rc=0；
   改的是**委員寫 anchor 的方式**——錨「將存在之字面」本就比錨「已刪除之字面」
   更能證明改動落地。**明禁**「不查 anchor」「刪弱證據判準」「豁免當輪」。
   ⚠️ 根因（R25 實證）：主委每輪落地會改動字面，使舊 anchor 退化為弱證據
   ⇒ 錨刪除行者**必然**在下一輪變紅，形成「產生量 ≥ 清理量」。
   🔴 **不得在此寫閘數**——閘數字面漂移已犯三次（R6 三→四、R7 四→五、R8 清單分歧）；
   權威清單唯一在該腳本內。

**硬輪上限**：🔴 **已於 2026-08-23 由使用者解除**——逐字「就做到完整 Frozen，不用管輪次」
⇒ 以上列四條件為唯一終點，**不再有輪數上限**，亦**不必再問使用者要不要續派**。
（原文為「自 R3 consult 採納起 ≤2 輪達 FROZEN」，保留為歷史。）
🔴 **解除的是輪數，不是標準**：委員會仍**不得**裁「先 Frozen」、
不得因輪數多而放寬四條件（grok 明禁；使用者之解除是把時間限制拿掉，不是把門檻拿掉）。

### 🔴 條件②′（R21 兩家裁定：原條件②在規格階段**不可達**，屬永久否決權）

**出處**：主委於 R21 brief 議題三第 7 題**主動提出**——條件② 之「本輪主委自傷絕對數＝0」
自 R4 至今**一次都沒達成過**（`3→5→6→7→6→7→7→9→7→10→6→5→9→5→5→6`）。
若保留為硬條件，它不是門檻而是**永久否決權**，再跑十輪也不會凍結。

- **COMPOSER-R21**：「規格階段要求『本輪主委自傷絕對數＝0』**不可達**；若保留為硬條件＝永久否決權。」
- **GROK-R21**：「在 3192 行複述面上屬**近永久否決**，不是可執行條件。」

**②′（三款**同時**成立才算滿足；取兩家之聯集，從嚴）**：
1. 本輪自傷 **P0＝0**；
2. ~~自傷**群集數連兩輪不增**~~ → 🔴 **R22 撤換**：composer 實測裁定該指標
   **在規格階段不可達，且與「委員挖得更深」正相關**（5→6→7），
   **但明示「不建議放寬」**——改追下列四個指標（**同時**觀察，非擇一）：
   (i) **OPEN P1 絕對數趨勢**（下降）
   (ii) **(N) 連續為 0 之輪數**（越長越好；現為連七輪）
   (iii) **當輪補丁包 locus rc=0 率**
   (iv) **「不含新建機制」之自傷群集數是否不增**（把「新建機制」從分母剔除後再看趨勢）
   🔴 **為何不算放水**：原指標把「委員挖得更深」誤記為「主委退步」，
   量錯了東西；四個新指標各自可由 receipt 直接讀出，且 (iv) 保留了趨勢要求。
3. 本輪自傷**不含「新建驗收機制」類**（與本卡「主委停止新建機制」條款對齊）。

🔴 **為何不算放水（兩家理由，主委不得自行擴充）**：仍**禁 P0**、仍**要求改善趨勢**，
且把「再建機制」**直接嵌入凍結條件**——比原條件在該面向**更嚴**。
放寬的只有「字面漏同步之 P1 可留到下一輪收乾」這一項，而那類自傷與 quant 新缺口**已被歸因機制分離**
（(N) 連六輪＝0）。

⚠️ **本項為委員裁定之流程門檻變更，非量化標準變更**；
主委已於白話看板向使用者明示，**使用者可否決**。條件①（P0／P1 歸零）**未動**。

---

## 🔴 做不成機械閘者（三家明列，不得宣稱已封）

- 「選哪個技術修法正確」
- 「使用者的 label 語意是否正確」
- 「**未被列出的**隱藏複述」

⇒ 這三類保留獨立委員審查與使用者裁定，**不得**因為「機械閘都綠了」就略過。

## 🔴 誠實邊界

本設計**串起** SPEC／TODO／實作，但**不保證完成**。
完成條件仍是：A-6 使用者裁定、所有 gate、實際 42 Task 驗收——
**不得以「已採用新架構」替代驗收**（CODEX-R3-P2-02）。
