# GAP3_EVENT_UX_TODO — D 延伸 001（凍結後第一份修訂）

BASE: docs/GAP3_EVENT_UX_TODO.md @ afa70967
PREDECESSOR: none

改什麼: §B 批次表之 **B1 列**——由 `1.1、1.10` 更正為 **`1.1、1.10、2.1b、4.2（僅 §G S-9 參考實作部分）`**。
為什麼: R2 已裁定把兩個跨批共用產物提前至 B1 並寫進「跨批單點依賴表」，**但 §B 表格該列未同步** ⇒ 同一節內兩處互斥，執行端照表格做只會做兩個 Task，缺的兩個正是其餘各批之前置。檔名依 `docs/FROZEN_DOC_AMENDMENT_PROCEDURE_V2.md` §2.2（`*.D-NNN.md` 機讀規約；`*_AMENDMENTS.md` 會被 doc 檢查誤判為 todo 型別）。

## 觸及面宣告

新增: none
覆寫: §B 批次執行策略之 `| **B1 契約與深度根基** | 1.1、1.10 | 無 | … |` 該列
依賴: none

## 內容

### A-001 — §B 表之 B1 內容與「跨批單點依賴表」互斥

- **原文**（BASE §B 表格）：`| **B1 契約與深度根基** | 1.1、1.10 | 無 | … | 中 |`
- **同節下方之跨批單點依賴表**（R2 依 `CODEX-R2-P1-04`／`GROK-R2-P1-04` 新增）寫明：
  「⇒ **B1 之實際內容為：Task 1.1、1.10、2.1b、4.2（僅 S-9 參考實作部分）**」
- **互斥**：同一節內兩處對「B1 含哪些 Task」給出不同答案。
  照表格做 ⇒ 只做 1.1／1.10 ⇒ **B2 之 Task 1.3 找不到 `canonical_serialize.py`、
  B3 之 Task 1.9 與 B7 之 Task 4.1 找不到 `depth_by_timeframe()`**，兩批當場停擺。

🔴 **發現經過（誠實記錄）**：本條**不是委員抓到的**——三輪對抗審 ＋ 戳記輪**都沒抓到**。
是主委在為使用者製作「施工看板」時，以機械方式從 §B 表格抽出各批內容，
發現抽出來的 B1 與跨批依賴表對不上。
⇒ **把文件做成看板這個動作本身，抓到了四輪審查沒抓到的互斥。**

**更正後之 B1**：

| 批 | 含 Task | 依賴 | 合併理由 | 規模 |
|---|---|---|---|---|
| **B1 契約與深度根基** | **1.1、1.10、2.1b、4.2（僅 §G S-9 參考實作部分）** | 無 | 1.1 定 reason／derived 欄／typed receipt schema；1.10 定 lookahead registry；**2.1b 建立唯一深度函式 `depth_by_timeframe()`**（B3 之 1.9 與 B7 之 4.1 皆消費）；**4.2 之 `canonical_serialize.py`（§G S-9 參考實作）為 B2 之 1.3 所需**。四者皆為其餘批次之前置 | 大 |

**連動（不改其餘列之字面，只釐清讀法）**：
- **B5**「Phase 2 全部」＝ Phase 2 全部**扣除已於 B1 完成之 2.1b**。
- **B7**「Phase 4 全部」＝ Phase 4 全部；其中 **4.2 之 S-9 參考實作部分已於 B1 完成**，
  本批只做其 horizon 曲線部分。

**驗證**：開工 B1 時，**四個** Task 之驗證欄命令皆須納入該批之 Phase Gate；
`grep -c '1.1、1.10、2.1b、4.2' docs/GAP3_EVENT_UX_TODO.D-001.md` `>= 1`。
**mutation**：把 B1 之含 Task 改回兩個 ⇒ B2 開工時 `ls momentum/Analysis/event_samples/canonical_serialize.py`
須為「不存在」⇒ 該批停擺（此即本條要防之失敗形態）。

## 修訂索引

| 編號 | 標的 | 一句話 | 日期 |
|---|---|---|---|
| **A-001** | §B 之 B1 列 | B1 實際含四個 Task（1.1／1.10／2.1b／4.2 之 S-9 部分），表格漏同步 | 2026-08-24 |

## 戳記

（委員於此 append；格式：
`RECONCILE-STAMP: <family> APPROVED <YYYY-MM-DD> sha256:<body-hash> task:<harness-task-id>`）

⚠️ **本延伸檔尚未取得委員戳記**——A-001 為主委自查所得之互斥更正，
屬「文件內部兩處互斥」之機械事實（非語意判斷），已附 mutation 與驗證命令。
**若使用者裁定開工 B1，本檔須先過戳記輪**（與 TODO 定版時同一程序）。
