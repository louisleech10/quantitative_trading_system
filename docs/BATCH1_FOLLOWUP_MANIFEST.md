# Batch1 Follow-up Manifest（V3｜2026-06-12）

> coverage_check.sh 追溯基準；每 ID 必須出現在 SPEC 與 TODO。
> V2→V3：合併 Codex r2（5 BLOCKING/2 MAJOR）+ Composer 獨立版（1 BLOCKING/3 MAJOR/2 MINOR）reconcile。

- [B1-1] N4：max_nan_ratio artifact production-owned canonical copy（`momentum/FeatureEngineering/_resources/`）+ module 級可注入常數（測試不改真檔）+ fail-closed raise 保留 + 兩 json sha256 防漂移 + **packaging 證據步驟**（repo 無 wheel 打包則記錄 source-deploy 證據入交接文件；有則補 package-data 驗證）。
- [B1-2] N6：stream nan_ratio warmup-aware。ownership=新 `utils/nan_stats.py`；**語義以現行 `_abnormal_nan_count`（feature_factory.py:2773-2787，2D 入參）為準：all-NaN 欄 `abnormal=total_nan`（fail-closed，V2 寫 0 是錯的）**；O(1) per-column 累積器（finalize 時 `not seen_valid → abnormal=nan_total`），掛點=`_write_group` 既有 `nan_mask = np.isnan(array)`（**feature_storage.py:917-918**，V2 行號 938-943 錯誤），禁全寬新陣列；fallback 唯一：producer 必產鍵，消費端缺鍵 → warning + 沿用 `1.0-coverage`。
- [B1-3] N6 驗收雙層：(i) 精確單元/整合——真 ColumnGroupRegistry（tmp）→ `write_raw_from_registry_stream` → summary → `_apply_runtime_quality_gate`，**mid-hole fixture abnormal/total 必須 > 0.17（> 實測門檻 0.16346，手算入測試註解）**；(ii) **真實 kline gate（鐵律不得降級）**：`data_cache/feature_klines/kline_cache.h5` BTCUSDT/12h 切片經真實 CGSA 生成（persist tmp）→ stream summary 含 nan_ratio、值==對已寫盤資料重算、quality_status 合理（slow marker）。
- [B1-4] N7：manifest 持久化契約不動（裸 `L{n}`）；result.metadata 邊界統一 `L{n}:{tf}`，**確定 scope 三處**：`feature_factory.py:3070-3071`（stream CGSA）+ **`:3325-3326`（legacy L7 `_layer7_validate_and_persist`，Composer P1）** + multi-TF worker 聚合（multi_tf_generator.py:546）；用新 `utils/layer_ids.py` **冪等 canonicalizer**（`L\d+$`→補 `:{tf}`；`L\d+:\d+[hdm]`→不變；`failure_reasons` 現行為扁平字串 `L3:<reason>`（feature_storage.py:582）→ 規則：第二段 match `^\d+[hdm]$` 視為已限定，否則在首個冒號後插入 tf）；stream 與 legacy 兩路徑都要硬編預期斷言 + 舊裸格式 fixture 兼容測試。
- [B1-5] N3：winsor 注入**定案 per-call 參數**（拒 setter：避免共享 mutable state；Codex r2 B2）：`FeatureValidator.validate_factory_output(result, winsor_window: Optional[int]=None)` 與 `winsorize(df, window: Optional[int]=None)`；`None → 252`（顯式 None 判斷，**禁 `or 252`**——window=0 必須 raise 不得靜默變 252）；`<=0 → ValueError`；factory 呼叫點 `feature_factory.py:3340` 傳 `config.preprocessing.winsorization.window`；API standalone caller `api/services/feature_task_service.py:185` 不傳參=維持 252 既有行為（聲明入交接文件）；min_periods 共用 resolver `min(window, max(20, window//4))` 抽 `utils/winsor_params.py`，preprocessor（feature_preprocessor.py:156-158）同步改用，byte 不變由 golden 保護；**最終測試必走 public `FeatureValidator` 路徑對 baseline hash**（Codex r2 M7）。
- [B1-6] T5：`actual_timeframes`→`present_timeframes`，3 producer（multi_tf_generator.py:327/619/1376）收斂單一函式；**scripts/ 2 個消費者列入 scope**（`scripts/profile_multi_tf_baseline.py:414`、`scripts/profile_v6v7_comparison.py:466` 同步改名，Composer P4）；grep gate 擴為 `momentum/ api/ frontend/src scripts/`；舊 task record 無讀取端、不遷移不 alias；4 個既有測試更新逐條登記。
- [B1-7] Golden baseline：`scripts/freeze_batch1_baseline.py` 在 production 改動前單獨 commit；內容=winsor 預設+w100 hash、max_nan_ratio 值、**nan_stats reference cases（empty/all-NaN/leading-only/trailing-only/mid-hole/跨 chunk 邊界 6 案例的 HEAD `_abnormal_nan_count` 輸出凍結，Codex r2 B1）**、**perf 基準（2000×20000 stream write 之 wall median-of-3 + tracemalloc 峰值，Codex r2 B5）**；測試只讀、缺檔=FAIL、hash 定義唯一（value/mask 分離）。
- [B1-8] 回歸 bundle 基線：78 passed（Codex 199.09s + Composer 217.79s 雙方獨立實測）；驗收=exit 0 且 pass ≥ 78。
- [B1-9] 效能 gate（重設計，無 production flag）：與 [B1-7] perf 基準同機比對——post-change 同 benchmark wall ≤ baseline×1.15、tracemalloc 峰值 ≤ baseline×1.10；固定 shard/workers 參數、warmup 1 輪丟棄、median-of-3（Composer P6）；slow marker；另加結構斷言：accumulator 僅標量狀態（單元測試 tracemalloc 證明 update 無 O(rows×cols) 配置）。
- [B1-10] 流程：大型；Codex 實作 + Composer code review；V3 經雙家族確認（Codex r2 BLOCKING 1-5 + Composer P1-P7 逐條收斂）後才派工；P0-P4 各自獨立 commit 可回退；scan-CGSA 路徑（:3142-3226 無 completeness 組裝）登記 out-of-scope（當前主路徑=stream，Composer P8）。
