/**
 * 事件分析頁**參數說明文案**之單一來源（`GAP3_EVENT_DISCLOSURE` Task 1.4）。
 *
 * ## 為什麼要有這個檔
 *
 * 2026-09-06 UAT：使用者逐項問「答案窗是什麼」「k 是什麼」「亂數種子是什麼」
 * ——這些數字**要他填**，畫面卻沒說它們是什麼、影響什麼。
 * 文案散在元件裡的後果是：同一個概念在兩處各寫一句、日後只改一處。
 *
 * ## 🔴 與契約 `doc` 的分界（不得跨界）
 *
 * **契約已有 `doc` 的欄位不進本檔**——`entry_price_semantic`／`label_return_mode`
 * 之說明住 `eventContractDocs.ts`（契約檔之逐字鏡像）。在此再寫一份就是第二份真相源。
 * 本檔只收**契約沒有、但使用者要填**的參數：分析參數（h／k）、UI 概念（進階區）、
 * 隨機對照組之抽樣參數（seed／鄰域／embargo）。
 * 兩邊鍵集**不得相交**，由 `eventParamDocs.test.ts` 機械對證。
 *
 * ## 寫法約定
 *
 * 每鍵兩句：`what`＝這是什麼（一句話講完），`effect`＝影響什麼（為什麼你該在意）。
 * **不寫數值門檻**——門檻由後端揭露（見 `oos_downgrade`），寫死在前端會過期。
 */

export interface EventParamDoc {
  /** 這是什麼（一句話）。 */
  what: string;
  /** 影響什麼——為什麼使用者該在意這個數字。 */
  effect: string;
}

export const EVENT_PARAM_DOCS: Record<string, EventParamDoc> = {
  horizon_bars: {
    what: '答案窗：事件發生後往後看幾根 K 線，來決定這次算「對」還是「錯」。',
    effect:
      '改 h 等於換一個問題——h=1 問「下一根會不會漲」，h=24 問「未來一天會不會漲」。'
      + '答案整批換一套，IC 就是另一個數字，**不同 h 的 IC 不能互相比較**。',
  },
  /**
   * 🔴 鍵名帶 `_analysis` 是**刻意的**：契約也有一個 `decision_offset_bars`
   * （匯入欄位＝這批當初記錄的 k），其說明住 `eventContractDocs.ts`。
   * 本鍵講的是**本次分析要用的 k**——B-D4 把兩者分開，正是因為它們不是同一件事
   * （面板以 `ic-param-k-dual` 並排顯示兩個值）。同名會讓它們又被當成一個。
   */
  decision_offset_bars_analysis: {
    what: '本次分析要提前幾根做決定：k=0 是事件那根就決定，k=2 是往前推兩根就決定。',
    effect:
      '它決定特徵的截止點——k 越大，你能用的資料越舊，IC 通常越低。'
      + '如果實務上你要提前決定，就該用提前的資料算 IC，否則 IC 會偏高。'
      + '這個 k 與「這批當初記錄的 k」無關，同一批可以用不同的 k 各分析一次。',
  },
  advanced_pair: {
    what: '進階：直接改「進場價」與「報酬算法」兩欄，組出上面三個 preset 沒有的組合。',
    effect:
      '一般用不到——三個 preset 已涵蓋常用情形。它存在是因為系統支援 13 種組合，'
      + '不給入口等於做了沒交付。組出的組合若幾何上算不出來，送出時會被擋下並說明原因。',
  },
  seed: {
    what: '亂數種子：固定它，同樣的設定每次都抽到同一批 K 線。',
    effect:
      '結果可重現的前提。改它等於換一組隨機樣本——如果換個種子結論就翻盤，'
      + '那代表樣本太小，不是找到了訊號。',
  },
  neighborhood_bars: {
    what: '前鄰域：每個事件往前幾根一併排除，不許被抽進對照組。',
    effect:
      '避免對照組抽到緊貼事件之前的 K 線。那些 K 線的行情跟事件本身高度重疊，'
      + '抽進來會讓兩邊的差被自己稀釋。',
  },
  embargo_bars: {
    what: '後 embargo：每個事件的答案窗結束之後，再多排除幾根。',
    effect:
      '與前鄰域同一個目的，但擋的是事件之後。答案窗剛結束那幾根仍在同一段行情裡，'
      + '不排除的話對照組就不是獨立樣本。',
  },
  n_requested: {
    what: '想抽幾筆隨機對照樣本。',
    effect:
      '抽越多，對照組的基準率估得越穩（少量樣本的正例率會抖得很厲害）。'
      + '但候選被排除區間扣掉之後可能不夠——抽不滿會明講，不會假裝成功。',
  },
  decision_offset_bars_scan_max: {
    what: 'k 掃描的上限：勾了之後會從 0 掃到你填的這個數，每個 k 各跑一次完整分析。',
    effect:
      '掃出來的每一格都是獨立的分析，格數＝(k 的值數)×(h 的值數)，有硬上限。'
      + '填越大跑越久——它是實際計算量，不是顯示選項。',
  },
  horizon_bars_scan_max: {
    what: 'h 掃描的上限：勾了之後會從 1 掃到你填的這個數。',
    effect:
      '同上，每一格各跑一次完整分析。🔴 不同 h 的 IC 之間**不能互相比較**'
      + '（換 h 等於換問題），這張表是給你看「哪個答案窗下訊號較強」，不是比大小。',
  },
  /**
   * 🔴 `h_inert_same_bar` 與 `h_scan_inapplicable` **不是重複**（`CODEX-R1-P2-05` 之澄清）：
   * 兩者同源於「當根不用 h」，但講的是**不同後果**——前者是單值 h 欄（送出時固定 1），
   * 後者是 h 掃描（每一格會算出相同結果）。兩條都住本檔，元件內不留散文。
   */
  h_inert_same_bar: {
    what: '「當根」不用 h（送出時固定為 1）。',
    effect: '當根只看事件那一根，答案窗長度不參與計算，所以這個欄位對它沒有作用。',
  },
  h_scan_inapplicable: {
    what: '報酬量法選「當根」時，h 掃描不適用。',
    effect:
      '當根只看事件那一根（開盤買、收盤賣），h 完全不參與計算——'
      + '掃出來每一格都會是一模一樣的數字。要掃 h 請改選「續漲」或「持有」。',
  },
};

/** 本檔涵蓋之參數鍵（測試以此對證 DOM 實際 render 的集合）。 */
export const EVENT_PARAM_DOC_KEYS = Object.keys(EVENT_PARAM_DOCS);
