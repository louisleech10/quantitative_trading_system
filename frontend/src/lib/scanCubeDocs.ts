/**
 * 掃描結果瀏覽器之**文案單一來源**（`SCANCUBE` Task 4.1）。
 *
 * 沿用 `eventParamDocs.ts` 之慣例：元件內**不得**寫任何說明字面，
 * 否則同一個概念會有兩份文字、日後只改一份。
 */

export interface ScanCubeDoc {
  what: string;
  effect: string;
}

export const SCAN_CUBE_DOCS: Record<string, ScanCubeDoc> = {
  cube_view: {
    what: '要用哪種方式看這批掃描結果：單格明細、單特徵跨格、或單格單特徵的圖表。',
    effect:
      '掃描出來的是一個「組合 × 特徵 × 指標」的立方體，一次只能從一個角度切。'
      + '換視角不會重算，只是換一種切法看同一批數字。',
  },
  cube_cell: {
    what: '要看哪一個參數組合（k＝提前幾根決定，h＝往後看幾根算答案）。',
    effect: '每一格是一次獨立的完整分析——換格等於換一整套結果，不是同一套換個排序。',
  },
  cube_feature: {
    what: '要看哪一個特徵。可以打字篩選名稱。',
    effect: '跨格視圖會把這個特徵在每個 (k,h) 的同一個指標並排，看它在哪些組合下比較強。',
  },
  cube_metric: {
    what: '跨格視圖要比哪一個指標。',
    effect: '一次只能比一個——同時比兩個指標的表格沒辦法讓你判斷任何事。',
  },
  cube_page_size: {
    what: '這張表一頁要顯示幾列資料。',
    effect: '有上限（後端契約定的）。填超過會被擋下並說明，不會安靜地少給你資料。',
  },
};

/**
 * 🔴 跨 h 比較之限制。這是**呈現規則**，不是建議。
 *
 * 出處：SPEC §C-4。不同 h ＝ 不同的問題（答案窗長度不同），
 * 其 IC 不是同一個估計量，比大小沒有意義；跨格取 max 更是向上偏誤估計量，
 * 偏誤隨格數增加。所以這個瀏覽器**不提供**「自動選最佳格」。
 */
export const CROSS_H_WARNING =
  '🔴 不同的 h 是不同的問題（答案窗長度不一樣），它們的 IC 不能直接比大小。'
  + '這張表是給你看「哪個答案窗下訊號較強」的線索，不是排名。'
  + '跨格挑最大的那一格會系統性高估——格數越多高估越多。';

/** Tier B 未保存時的說明。數字由後端 `fits_hint` 帶入，前端不寫死。 */
export const CHARTS_NOT_STORED_HEAD =
  '這次掃描的圖表資料沒有保存下來——資料量超過上限。指標表不受影響，仍然完整。';

/** 被排除的節之說明。 */
export const EXCLUDED_SECTION_NOTE =
  '相關性矩陣（correlation_matrix）不在這個瀏覽器裡：它是「特徵兩兩之間」的資料，'
  + '不是每個特徵各自的資料，數量隨特徵數的平方成長，另外處理。';
