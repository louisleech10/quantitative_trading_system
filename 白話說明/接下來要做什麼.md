# 接下來要做什麼

**這份只回答三件事：現在在哪、下一步做什麼、為什麼是這個順序。**
不是流水帳——過程紀錄在 [治理進度日誌.md](治理進度日誌.md)、[流程摩擦記錄.md](流程摩擦記錄.md)。

> 🔴 **順序的唯一來源**是 `docs/GOVERNANCE_EXECUTION_ORDER.md`（技術版，由資料檔生成）。
> 本檔是投影；**兩邊對不上時以那份為準，這裡就是錯的**。

最後更新：**2026-08-10**（你解除排序凍結、指示以淨摩擦排序之後）

---

## 現在在哪

| | 狀態 |
|---|---|
| 第 0 批（摩擦止血） | `B0`–`B2` ✅｜`B3` 停手→由 `B3R` 取代｜**`B3R` 已落地**（殘留一項未交付）｜`B4`–`B7` 未開工 |
| 第 1 批（派工輸入品質＋機制自咬） | 十批做完，**但 `b1` 的收斂檔沒蓋章、`b4` 只做完階段 1** |
| 測試 | **1283 條全過**；epic 最終閘 12/12 PASS |
| 票數 | **53 張**（本次新增 `B-53`） |

🔴 **「批次做完」不等於「票關掉」。** 第 1 批服務的七張票裡，至少三張明確沒關：
`B-31`（只到「產出端有檢查點」，**不得說「強制」**）、`B-38`（票裡自己寫著「不能算關閉」）、
`B-15`（誤擋仍在，見下）。

---

## 下一步：三站，都是今天用血換來的

### 站 2.5 — 把「哪批做完、哪張票關了」變成資料

**病**：這些狀態現在只寫在散文裡，同一句話在四份文件各有一份副本。

**證據**：你問「B1–B10 清空了嗎」那一次，我一連講錯 4 個狀態；
接著修正時**一份一份改，連續漏掉三份**，最後是用 `grep` 機械掃才找齊。

**修法**：狀態寫進資料檔，文件只放指標——**生成器已經存在**（順序表就是這樣做的），只需擴充。

### 站 2.6 — 摩擦統計（唯讀最小版）

**病**：這兩週每次改完，都說不出改善了多少。

**證據**：今天為了回答你「摩擦有沒有下降」，我手工湊了十幾個指令，
中途還被一個資料格式差異騙過一次（見下方「已知地雷」）。

**修法**：一支唯讀腳本，從稽核紀錄導出「哪個節點、什麼原因、多少次」。
**前置條件三天前就滿足了**，資料也累積 676 筆，只是沒人去用。

### 站 2.7 — 補收案缺口

`b1` 的收斂檔**沒蓋章**（機檢對它是紅的，三天沒人發現）；`b4` 只做完階段 1。

---

## 之後的順序

```
站 4  B3R                    ✅ 已落地（O(n) scanner 未交付，不得宣稱達標）
站 5  B4 → B5 → B6 → B7      B4 含「唯讀指令被誤擋」的剩餘部分
站 6  補編號登記（B-26）      ← 原本排站 3，這次降級
站 7  治理殘留票 B-48…B-53
```

**為什麼把編號登記降級**：撞號 8 次是 08-04 的事，**之後沒再發生**；
而站 2.5–2.7 是**現在正在流血的**。

---

## 這兩週到底有沒有變好

**有，但只在做成閘門的地方。**

| 指標 | 前 | 後 |
|---|---|---|
| 白跑輪（開了一輪卻收不到東西） | 60–67% | **12%** |
| 委員交件格式不合規（要整份重交） | 每天 2–8 次 | **0** |
| 唯讀指令被誤擋 | 39–41% | **20%**（做了一半） |
| **靠紀律、沒做成閘門的部分** | — | **零改善** |

最後一列不是修辭。今天一天之內：同一個誇大句漏改三次、
同一個「讀錯 rc」的陷阱踩兩次、把進度手寫進沒人看管的檔一次。
**這些全都寫在規則裡，我全都知道，還是照犯。**

⇒ 所以站 2.5／2.6 排在最前面：**它們是把剩下靠紀律的部分變成閘門**，不是新功能。

---

## 已知地雷（動工前必看）

| 地雷 | 影響 |
|---|---|
| 稽核紀錄裡 `gate_deny` 的格式與其他事件**不一樣**（少一個空格） | 用標準方式掃描會**靜默漏掉全部 1343 筆**。站 2.6 會直接踩到，**必須先處理** |
| 順序表的「生成區塊」有**三份副本**（正式文件＋兩份測試素材） | 改資料檔要同步三處；有機器擋，但會擋在推送時 |
| 說明檔守衛只驗**時序**不驗**內容** | 檔尾補一行標記就會變綠，標頭寫錯它不管 |

---

## 需要你決定的

目前**沒有**卡在你身上的決策。上一輪的三項（排序、殘留票落位、地雷入票）你都已裁定。

<!-- next-tasks: 2026-08-10 依淨摩擦重排後建立；本檔為順序表之投影，狀態一律指回技術版 -->
