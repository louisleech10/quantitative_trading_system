# GAP-1 施工進度看板（策略層防過擬合：MinBTL／DSR／PBO）— 🏁 已收工（2026-08-18）

**這份是白話版**：清單（TODO）在做哪些事、每件做到哪。技術版＝`docs/GAP1_STRATEGY_OVERFIT_TODO.md`（**FROZEN R3**）
＋延伸檔 `docs/GAP1_STRATEGY_OVERFIT_AMENDMENTS.md`（A1-1..A1-24；規格已定版，修訂只走延伸檔）。
狀態欄我每完成一批就改；最即時的程式碼證據看 git log 的 `feat(gap1)`／`fix(gap1)` 系列。

## 走到哪了（一眼版）

```
偵察（四方查證）完成 → SPEC 七輪對抗審查定版（你白話閘裁決三項收回）
→ TODO 兩輪對抗審查（22 條→5 條）＋我自產 3 條數值錯誤 → 三家蓋章 → **FROZEN R3**
→ **第1批完工**（三家 code review 10 條全處置、三家蓋章）
→ **第2批寫完 → 三家 code review 21 條全處置 → 修補完（141 條測試全過、12 條 mutation 全紅）**
→ **第2批修補三家蓋章 PASS**（B2 CLOSED）→ **小任務 3b 完工**（白話說明 → HTML 閱讀介面，掛 pre-commit 同 commit 產出）
→ **第3批修補三家蓋章 PASS**（B3 CLOSED）→ **第4批寫完 → 三家 code review 13 條全處置 → 修補完 → 三家蓋章 PASS**（B4 CLOSED）
→ 🏁 **GAP-1 四批全部收工（2026-08-18）**：280 條測試全過、20 條 mutation 全紅、接線閘 rc=0、殘留 G1-R1..R7＋R9／R10／R11 逐項複核觸發未成立（三家蓋章時複核）
```

## 這個 epic 到底在做什麼（一句話）

「試了 N 個策略挑最好的，是真本事還是運氣？」——因子層已有 FDR，策略層原本裸奔。
做三個檢定：**MinBTL**（資料夠不夠長，才有資格宣稱這個夏普）、**DSR**（冠軍扣掉「試很多次」的膨脹後還顯著嗎）、
**PBO**（換一段資料重選，還是它嗎）。不合格＝降級展示＋明顯警語，**不硬擋**（你 8/17 定的）。

## 四批任務與狀態

| 批 | 任務 | 白話 | 狀態 |
|---|---|---|---|
| **第1批** | 1.1 年化頻率解析 | 「幾小時 K 線＝一年幾期」變成一個函式，未知週期**直接報錯**，不准回預設 730 | ✅ 完工＋審過＋蓋章 |
| | 1.2 typed Sharpe | 夏普自己算；算不出來（樣本太少／含 NaN／全一樣）回 NaN＋狀態碼，**不准回 0.0 裝有結果** | ✅ |
| | 1.3 回測帶年化來源 | 回測結果多一欄「年化是真解析還是退回預設」，機器可判讀；改了兩個既有檔（白名單內），既有測試斷言一條沒動 | ✅ |
| | 1.4 唯一合法輸入序列 | 三種取法（逐 bar／非零 bar／逐筆交易）；逐 bar 一律標「不適用」，因為結構性的 0 會讓顯著性虛胖 | ✅ |
| **第2批** | 2.1 契約 JSON | 16 個頂層鍵，所有欄位／枚舉**只住這一個檔**；狀態碼不複製，用 ref 指向 IC 契約，執行期解析失敗即報錯；**審後加碼**：讀契約檔時多／少一個鍵、枚舉形狀不對、reason 表漂移一律直接報錯（以前會靜默放行） | ✅ 完工＋審過＋修補＋三家蓋章 |
| | 2.2 N 帳本讀取 | 讀「試過幾個候選」的帳本；**帳本不存在＝誠實回 N 未知**，不准假裝 N=1、不准拿請求參數頂替；**審後加碼**：NaN／無限大的夏普值一律當非法列（不准混進 DSR）、檔在但全是壞列要說「帳本壞了」不是「沒帳本」、快照 hash 改成不會撞的編碼 | ✅ 完工＋審過＋修補＋三家蓋章 |
| | 2.3 生產者合約 | 唯一寫入口＋可執行合約：未來搜尋引擎接不對就紅；**審後加碼**：寫入前先上檔案鎖（多行程也不會把同一筆記兩次、長列不會交錯）、不准把 A 帳本的列寫進 B 帳本、只收純 Python 數值（Enum／numpy 冒充一律拒） | ✅ 完工＋審過＋修補＋三家蓋章 |
| **第3批** | 3.1 MinBTL | 「想宣稱這個夏普，資料至少要幾年」上界＋「這麼長的資料最多准試幾個」預算；公式常數不准調 | ✅ 完工＋審過＋修補＋三家蓋章 |
| | 3.2 DSR | 冠軍檢定，全式寫死；N=1 時退化成 PSR（可證偽的錨點） | ✅ 完工＋審過＋修補＋三家蓋章 |
| | 3.3 報告區段 | 三關結果的機器可讀輸出；任一關不 ok ⇒ 降級＋警語，結構裡**不准有推薦類欄位** | ✅ 完工＋審過＋修補＋三家蓋章 |
| | 3.4 API 附警語 | `ml_pipeline` 回應多三個欄位（資格／降級／警語鍵）；**不拒絕請求**（你的裁決）；程式 bug 不准被吞成「計算失敗」 | ✅ 完工＋審過＋修補＋三家蓋章 |
| **第4批** | 4.1 CSCV 切分 | 組合式切分，lazy 不落地，超預算直接拒（防 OOM） | ✅ 完工＋審過＋修補＋三家蓋章 |
| | 4.2 PBO | 每條路徑樣本內選冠軍、樣本外看排名；冠軍在樣本外退化就跳過該路徑（審查抓到的越界 bug 已寫進規格） | ✅ 完工＋審過＋修補＋三家蓋章 |
| | 4.3 候選宇宙守衛 | 禁「先挑前 K 名再算」：集合相等＋數量三方相等＋hash 三道；但**帳本本身是否完整**純統計層證不了 → 具名殘留 G1-R9 | ✅ 完工＋審過＋修補＋三家蓋章 |
| | 2.4 接線閘（移到最後） | 契約 vs 程式的封閉集合比對（AST，只認函式頂層無條件字面鍵；死分支不算） | ✅ 完工＋審過＋修補＋三家蓋章 |

## 每批的驗收證據（不只 pass/fail）

- **測試**：`strategy_validation/`＋API 測試目前 **280 條全過**（第1批 45＋第2批 90＋第3批 89＋第4批 56）；`Strategy/`＋`Optimization/` 207 過／2 紅（那 2 條是別的模組的既有紅，與本 epic 無關）。
- **真實資料**：Task 1.4 的測試用你電腦上的**真實 K 線**（`data_cache/feature_klines/kline_cache.h5`）跑真回測，不是造出來的假資料；**檔案不在時直接失敗**，不准 skip 裝綠（審查抓的）。
- **改壞一定要變紅**（mutation）：把 **21** 種已知錯誤寫法逐一植入（第1批 8 種：未知週期回 730／退化回 0.0／Mertens 係數改錯／rf 用 0.02／逐 bar 改成 ok／放行預設 730／帳本缺檔回 N=1／bar 數當年數；第2批新增 4 種：放行 NaN／快照 hash 改回會撞的舊法／帳本目錄名改掉／拿掉檔案鎖；第3批新增 5 種：MinBTL 的 ln 改成 n／預算 floor 改 round／DSR 刪 γ 項／DSR 分母改用跨試驗變異數／矩公式代入年化夏普；第4批新增 3 種：PBO 冠軍改用樣本外挑／拿掉候選宇宙守衛／名次改用原始索引；G1-R11 修補新增 1 種：拿掉「位元全等即常數」判定），**21 次全部讓測試變紅**（收據 `handoffs/run_receipts/20260818T120000Z-gap1-r11-mutation.log`）；第4批那條「名次改用原始索引」的植入**第一次沒變紅**——是探針抓到我的測試沒真的踩到那個洞，補了一條測試才紅。另外接線閘本身有 6 條植入測試（契約多節／自創 reason 兩形／節名只寫註解／f-string／死分支）全部變紅。探針本身有互斥鎖，不能兩人同時跑（血淚見下）。

## 這輪審查抓到、且值得你知道的事

**第2批（21 條，三家一致「修完可進第3批」，沒有一條要重做；分歧只在嚴重度，一律取較嚴的那版，全部這輪修、不留待辦）**：
1. **我對自己程式碼的描述是錯的**：我在派工單上寫「年化夏普那種列我算進『壞列』」，三家實跑都證明程式其實**沒有**算進壞列（那是合法列，只是不進 DSR）。而且三家一致判定：清單裡那句「年化列要算壞列」本身就寫錯了（合法枚舉不該當非法）。程式對、我的說法錯、清單字面錯 ⇒ 延伸檔 A1-21 把那句作廢，測試加了「壞列數＝0」的明確斷言鎖住。
2. **同一筆可能被記兩次**：唯一性檢查是「先讀再寫」，兩個行程同時寫就都會通過（Grok 硬逼出來重現了）。Codex 另抓到「可以把 A 帳本的列寫進 B 帳本」。修：寫入前上檔案鎖＋帳本身分必須相符；並做成「拿掉鎖就一定變紅」的測試。
3. **NaN 可以寫進帳本、還會被 DSR 拿去算**：三家中兩家抓到。修：寫入和讀取兩端都擋。
4. **快照 hash 會撞**：用 `|` 拼接三段字串，段落裡若有 `|` 就撞（三家各給一組反例）。修：改 JSON 定界，反例全數變成不撞。
5. **我自認最可疑的地方，三家確認確實是洞**：帳本路徑怎麼算的那段程式，之前所有測試都把它整個換掉，等於**沒測過**。修：新增一檔測試走真路徑，改個目錄名就紅。
6. 其他：讀契約檔多／少鍵不再靜默放行、型別檢查改精確比對（Enum／numpy 冒充一律拒）、快取改成外面改不到、錯誤訊息會說缺哪個多哪個。

**第3批（12 條，三家一致「修完可進第4批」；全部這輪修）**：
1. **API 錯誤分類有洞**（兩家 MAJOR）：reporter 若丟出一般 `ValueError`，路由會把它當「用戶輸入錯」回 400；而「接線 bug」那類雖然回 500，訊息卻被外層再包一層。修：reporter 路徑的錯一律 500、訊息固定字面、外層不再重包；補測試。
2. **失敗還留垃圾**（兩家抓到，我 brief 沒列）：路由先把 pipeline 檔寫進磁碟、再呼叫 reporter，reporter 一報錯就留下一個孤兒檔。修：reporter 先跑（它只讀不寫），失敗就不落盤；測試斷言目錄空。
3. **驗收字面「skew=0、kurt=3」我沒真的固定住**（兩家）：我用對稱序列讓 skew 恰為 0，但峰度是樣本值 2.68 不是 3。修：另造一個峰度**恰為 3** 的序列（十字型：±s 各 n 個加 4n 個 0），先斷言矩再斷言 DSR 等於閉式 PSR——原本那條保留當第二道鎖。
4. **測試用的假帳本違反帳本自己的不變式**（兩家）：修成由構造滿足。
5. **`n_source` 三個字面沒住進契約**（Grok；Composer 也建議）：加進契約枚舉（頂層鍵 16→17，延伸檔 A1-22 覆寫「恰 16」），loader 的機械枚舉對映自動接管、寫入端也驗。
6. **我用 sed 多插了一行沒用的 import**（Codex）：同一個模式命中兩處，我沒先 `grep -c`。已刪；本 epic 第二次同型錯，寫進收斂檔。

**第4批（13 條，三家一致「修完可收工」；全部這輪修）**——這批我先自己在延伸檔（A1-23）列了七項「可能不忠於清單字面」的地方請委員裁決，結果三項被推翻、四項可接受：
1. **接線閘的「放行名單」太寬**（兩家 MAJOR，各給反例）：我為了讓「傳遞既有 reason」不被誤擋，放行了任何 `x.某屬性`／`x["某鍵"]`；Grok 實跑證明可以用 `reason=data["x"]` 塞自創字面繞過。修：收窄成只認 `.reason`／`["reason"]`／`.get("reason", …)`／同函式範圍的變數；補 5 條反例測試（全部變紅）＋正例。另外「死枚舉」判定也太寬（只在註解或沒用到的常數裡出現也算接線）——修成必須真的用在 reason 位置或被引用。
2. **PBO 排名的分母我改了定義**（Codex P1 引原始論文）：我用「樣本外也算得出來的候選」當分母，論文與清單字面都是「該路徑全部有效候選」。修：守字面；任何候選樣本外算不出來就整條路徑跳過，不在縮小的母體上排名。
3. **退化計數重複算**（兩家）：冠軍在樣本外退化時我 +1 了兩次。修：每候選每路徑最多 +1，測試改精確斷言。
4. **雙冠測試是廉價綠燈**（三家一致，我 brief 也自承）：修成手算可證偽——平手時取小索引 ⇒ ω=ln(2/3)，取大索引 ⇒ ln 4，測試鎖 1e-12。
5. **向量化夏普與原函式在「幾乎常數」的欄會給不同的天文數字**（兩家）：修成逐欄用同一個一維算法 ⇒ 逐位相同；順帶暴露第 1 批 `compute_sharpe` 對這種序列不當退化——第 1 批已蓋章的語意，登記殘留 G1-R11（要研究容差怎麼定）。
6. golden 檔的防改 sha 只掛在一個測試檔（Grok）：抽成唯一 loader，三檔測試都經它驗。

**第1批（10 條）**：
1. **我在文件裡寫的一句話被推翻**：我寫「已不可能靜默退回 730」，Codex 和 Grok 各自寫反例證明——只要回測引擎「收下參數但不回填年化資訊」，程式就默默用 730。已改成直接報錯，並把那句話在延伸檔明文作廢（A1-20）。
2. **Codex 兩次拒絕蓋章，兩次都對**：第一次因為我在牠驗收時繼續改工作區；第二次因為三家同時跑我的 mutation 探針、互相看到對方植入的錯誤，baseline 不穩。前者是我違反自己的規矩，後者是探針設計缺陷。修法都做成機械強制（工作區先 commit 凍結；探針加鎖，併發直接退出）。
3. **我常批評的病自己犯**：mutation 探針的「還原後全綠」自檢原本是空殼（只印 rc 不擋）。已改成 fail-closed，而且它馬上就抓到一次真實污染。

## 殘留（不是不做，是登記了為什麼現在不做）

`docs/IC_QUANT_GAP_REGISTRY.md`「GAP-1 待補完」表：G1-R1..R7（原有）＋ **G1-R9**（帳本完整性要生產者側才證得了）＋ **G1-R10**（回測引擎 Protocol 沒宣告新參數，白名單不准改既有測試替身）＋ **G1-R11**（夏普對「浮點上幾乎是常數」的序列不當退化）——**已修（8/18）**：三家 consult 查了業界五套件源碼與文獻，一致採「所有值位元全等即常數」併判、反對加容差；改一行＋測試＋探針 §V-16，三家 review 中。每條都有「卡在誰／誰的裁決／需要研究」三值之一與觸發條件。

## 推送

**每批收尾都直接推上 GitHub**（你 8/18 定的：commit＋push 秒級、每次同步）。本機與 `origin/main` 應零落差；
要查就 `git log origin/main..HEAD`（應為空）。
