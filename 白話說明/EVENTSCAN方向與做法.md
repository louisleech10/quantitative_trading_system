# EVENTSCAN — 條件掃描 → 持有報酬 vs 隨機對照

> **這份是給你看的方向說明，不是規格。** 你看過同意，我才去寫規格。
> 依據＝2026-09-20 委員唯讀偵察（composer 已交件、codex 進行中）＋我自己的讀碼查證。
> 🔴 **文中所有「數字」分兩類**：標「實查」的是我實際從 repo／資料讀出來的；標「示意」的是排版用的假數字，**不是實測結果**。

---

## 一、一句話

**你設一個條件，系統在 K 線上找出所有符合的時點，算「從那時買入、持有 N 根之後賺多少」，再跟「隨機挑同樣多的時點、持有同樣久」比。**

---

## 二、名詞對照（後面會一直用）

| 白話 | 系統裡的說法 | 意思 |
|---|---|---|
| 根 | bar | 一根 K 線。1h run 一根＝1 小時，12h run 一根＝12 小時 |
| 觸發點 | 事件 / event，時間點叫 `t0` | 條件成立的那一根 |
| 觸發批 | 事件批 / event batch | 一次掃描找出的所有觸發點，打包成一批 |
| 對照批 | 隨機對照批 / random control batch | 隨機挑的時點，用來當「不挑時機」的基準 |
| 持有期 | horizon（`h`） | 買入後持有幾根。可以一次看好幾個（你要的複選） |
| 答案欄 | `label`（0 或 1） | 這次「算不算成功」。**你不用填，系統自己算** |
| 算好的特徵 | FF run / Feature Factory run | 事先算好的所有指標（EMA、RSI、MACD…），存在檔案裡 |

---

## 三、從頭到尾走一遍（完整舉例）

### 第 0 步：你選一個算好的資料

系統裡**現有 14 個可用的 FF run**（實查 `data_cache/features/registry.json`）。舉其中一個：

| 項目 | 值（實查） |
|---|---|
| 標的 | `ETHUSDT` |
| 週期 | `1h`（一根＝1 小時） |
| 識別碼 | `4a8a0b3726cc906ab3534994605e77f5` |
| 期間 | 2024-01-01 00:00 ～ 2026-04-27 23:00（UTC） |
| 有幾根 | 20,352 根 |
| 算好幾個特徵 | 437,066 個 |

🔴 **資料源就是 FF，FF 有什麼就用什麼**（你 2026-09-20 裁定）。系統不會自己再算任何指標。

### 第 1 步：你寫一個條件

**你原本舉的 `EMA5 > EMA10 > EMA30` 不能直接用——因為這個 run 裡沒有 EMA_30。**

實查這個 run 的 EMA 週期，有這 14 個：

```
5, 8, 10, 13, 20, 21, 34, 50, 55, 89, 100, 144, 200, 233
```

所以你可以寫（例如）：

```
close_1h_trend_EMA_5 > close_1h_trend_EMA_10
  AND close_1h_trend_EMA_10 > close_1h_trend_EMA_34
```

這是「短期均線在中期之上、中期在長期之上」——多頭排列，跟你原本的意思一樣，只是用 34 代替 30。

**其他能用的指標家族**（實查同一份 manifest）：

| 類別 | 有哪些 |
|---|---|
| 均線 | `EMA`、`SMA`、`WMA`、`DEMA`、`TEMA`、`TRIMA`、`KAMA`、`MAMA`、`MAVP`、`MA`、`MIDPOINT`、`BBANDS`（布林帶） |
| 動能 | `RSI`、`MACDFIX`、`MOM`、`ROC`、`ROCP`、`ROCR`、`CMO`、`TRIX`、`PPO`、`APO` |
| 統計 | `LINEARREG`、`STDDEV`、`TSF`、`VAR` |

---

#### 🔴 那你怎麼知道有哪些欄位、名字怎麼拼？（你 2026-09-20 提問）

**這是真問題。光這一個 run 就有 18 萬個以上的欄名**（實查：`close_1h_` 開頭的就有 32,747 個），
平鋪一張清單給你完全沒用。

**好消息是欄名完全有結構**：

```
close_1h_trend_EMA_5
 │     │    │     │  └ 參數（5）
 │     │    │     └ 指標（EMA）
 │     │    └ 類別（trend）
 │     └ 週期（1h）
 └ 來源欄（close）
```

實查這個 run 的五個層級各有什麼：

| 層級 | 有哪些（實查） |
|---|---|
| 來源欄 | `close`、`volume`、`hlc`、`hl`、`tr`、`ms` |
| 週期 | `1h`、`12h`（這個 run 同時有兩個週期） |
| 類別 | `trend`、`momentum`、`statistics`、`Lag`… |
| 指標 | 見上面的家族表 |
| 參數 | 每個指標各自的週期數，例如 EMA 有 14 個 |

**為什麼會到 18 萬**：因為還有兩兩組合的衍生欄，例如
`close_12h_momentum_CMO_13_144_Cross`（CMO13 與 CMO144 的交叉）、`..._Ratio`（比值）。

**所以畫面要這樣做**（這是本票要新做的東西）：

1. **逐層下拉**：選來源欄 → 選週期 → 選類別 → 選指標 → 選參數。每一層只有幾個到幾十個選項，
   選完自動把正確欄名填進條件式。**你永遠不用自己拼字串。**
2. **打字搜尋**：想直接打 `EMA` 就跳出所有含 EMA 的欄位，選一個貼進去。
3. **打錯要報錯**：現在系統找不到欄位時**直接判「條件不成立」**，你會看到「零筆命中」而不是錯誤訊息
   （缺口 2）。本票會改成明確報錯並告訴你最接近的欄名。

**現況**：後端已經有 `GET /api/v1/ic/features/list`（給 標的＋週期＋run識別碼，回該 run 的全部欄名），
前端也有呼叫它的程式（`fetchAvailableFeatures`）。**缺的是上面這三件事**——現在只能拿到一整包平鋪清單。

### 第 2 步：你輸入持有期

你 2026-09-20 裁定：**用「根」計算，但畫面要顯示大約幾天；而且要支援複選**。

所以畫面會長這樣（示意）：

```
持有期（直接輸入，逗號分隔，要幾個都行）
  ┌─────────────────────────────┐
  │ 5, 10, 30, 55               │
  └─────────────────────────────┘
  → 5 根 ≈ 5 小時 ｜ 10 根 ≈ 10 小時 ｜ 30 根 ≈ 1.3 天 ｜ 55 根 ≈ 2.3 天
```

**不是勾選清單**（你 2026-09-20 裁定）：直接打字輸入，想看幾個就打幾個，不受預設選項限制。
你一邊打，底下就即時顯示每個數字大約是多久。

換算由系統算：`根數 × 週期長度`。同樣打 55，在 12h run 上會顯示「≈ 27.5 天」。

### 第 3 步：系統掃描

在 20,352 根裡逐根求值你的條件，找出所有成立的根。

假設找到 **340 根**（示意）。

### 第 4 步：系統填 `label`（你不用做任何事，也不會被問任何門檻）

🔴 **你 2026-09-20 裁定：完全不要門檻，也不會給。** 這一步照你的裁定設計。

匯入契約**強制**每筆要有 `label`（0 或 1），而且一批裡必須兩類都有——這是硬的，
實查 `momentum/Analysis/contracts/event_import_contract.json` 的 `required_fields`，繞不過去。

所以 `label` 用**漲跌的正負號**填：

- 報酬 > 0 → `label = 1`
- 報酬 ≤ 0 → `label = 0`

**這不是門檻，是漲跌的定義。** 沒有任何數字要你設定，畫面上也不會出現門檻欄位。

🔴 **誠實講**：這個 `label` 對你要的統計數據**一點貢獻都沒有**（勝率是另外從報酬直接算的，見第 6 步）。
它純粹是匯入契約的形式要求，你在畫面上完全不會看到它。

> **如果你連這個內部欄位都不想要**，另一條路是完全不走事件匯入、另建一條掃描→報酬統計的路徑。
> 你看到的數字會一模一樣，但**既有的未來資料防護、時點對齊檢查全部用不到**，等於一條沒護網的平行路。
> 我不建議，但這是你的選擇。

### 第 5 步：系統產一批隨機對照

從同一個 run 隨機挑 **340 根**（跟觸發批同樣多），規則：

- 避開觸發點附近（前後各留緩衝，免得對照組其實也在條件成立的區間裡）
- 同一個標的、同一個週期、同一段期間

### 第 5 步：系統產一批隨機對照

從同一個 run 隨機挑 **340 根**（跟觸發批同樣多），規則：

- 避開觸發點附近（前後各留緩衝，免得對照組其實也在條件成立的區間裡）
- 同一個標的、同一個週期、同一段期間
- 亂數種子固定 → 同樣的設定重跑會得到**一模一樣**的結果

### 第 6 步：兩批各自算報酬，並排給你看

報酬公式（實查 `momentum/Analysis/event_samples/tables.py`）：

```
報酬 = (出場那根的收盤價 − 進場價) / 進場價
```

做空會反號。資料不夠長的事件（例如最後 55 根之內的觸發點算不出 55 根報酬）**直接排除，不會灌 0 拉低平均**。

🔴 **進場價用哪一根、哪個價——這一條交委員會，還沒定案。**

**你的想法**（2026-09-20）：用條件成立那根的**下一根開盤價**（系統裡叫 `next_open`）。
你同時聲明：這只是想法，沒深入評估是否有風險、統計數據或量化金融計算上的誤區。

**我查到的現況**：`next_open` 系統已經支援——實查 `momentum/Analysis/event_samples/alignment.py:102-106`，
取 `t0_idx + 1` 那根的 `open`，資料不夠（t0 是最後一根）就標 `missing_bar` 排除。
五個可選值為 `trigger_open`／`trigger_close`／`next_open`／`decision_bar_open`／`decision_bar_close`。

**我為什麼覺得你的直覺有道理**：條件要用到 t0 那根的收盤價才算得出來（EMA 是收盤價算的）。
如果進場價用 t0 的收盤價，等於假設「收盤瞬間同時算完指標並成交」。
用下一根的開盤價比較接近真的買得到的價格。

**但這正是要交委員會的原因**——我不是量化金融專家，下面幾件事我沒有能力自己判斷：
跳空風險怎麼處理、不同選擇會不會讓報酬統計產生系統性偏誤、
隨機對照批是否必須用完全相同的進場語意、事件研究的標準做法是什麼。
**這一條在委員會給出結論前不算定案。**

另外系統有一個並列揭露的口徑 `ret_label_anchor`（一律以 t0 收盤價為基準，不隨設定變），
codex 2026-09-20 查出兩者並存。**不論最後選哪個：觸發批與隨機批一律用同一個，且畫面標明是哪個**，
否則兩批各用一個去比，數字會失真。

你要的是「**統計數據**」，不只是一個平均值。所以每個持有期都給這幾個（示意數字，非實測）。

🔴 **只有「全體」一組**——委員會 2026-09-20 裁定：不得顯示「正例組／反例組」，那在本票的 `label` 定義下是套套邏輯（見 7-2）。
🔴 **筆數是「每段連續成立算一次」**，不是每根都算（見 7-4）。

**觸發批**

| 持有 | 筆數 | 平均 | 中位數 | 勝率 | 標準差 | 最好 | 最差 | 成本歸零點 |
|---|---|---|---|---|---|---|---|---|
| 5 根 ≈ 5 小時 | 340 | +0.42% | +0.31% | 56% | 1.8% | +7.2% | −5.1% | 4.2 bps |
| 10 根 ≈ 10 小時 | 340 | +0.68% | +0.44% | 58% | 2.6% | +11.3% | −7.8% | 6.8 bps |
| 30 根 ≈ 1.3 天 | 338 | +1.05% | +0.71% | 61% | 4.7% | +19.6% | −13.2% | 10.5 bps |
| 55 根 ≈ 2.3 天 | 335 | +0.91% | +0.62% | 57% | 6.3% | +24.1% | −18.7% | 9.1 bps |

> **「成本歸零點」怎麼看**：手續費加滑價**超過這個數字**，這個持有期就不賺了。
> 例如上表 5 根那列是 4.2 bps——如果你的來回成本是 5 bps，**那一列就是假的獲利**。
> 這一欄的用意是：你不必先猜費率，它反過來告訴你門檻在哪。詳見 7-9。

**隨機對照批**（同樣的欄位）

| 持有 | 筆數 | 平均 | 中位數 | 勝率 | 標準差 | 最好 | 最差 |
|---|---|---|---|---|---|---|---|
| 5 根 | 340 | +0.05% | +0.02% | 50% | 1.9% | +8.0% | −6.3% |
| 10 根 | 340 | +0.11% | +0.04% | 51% | 2.7% | +12.1% | −9.0% |
| 30 根 | 338 | +0.33% | +0.15% | 52% | 4.9% | +20.2% | −15.4% |
| 55 根 | 335 | +0.60% | +0.28% | 53% | 6.5% | +25.8% | −20.1% |

**差（觸發 − 隨機）**

| 持有 | 平均差 | 勝率差 |
|---|---|---|
| 5 根 | **+0.37%** | +6 個百分點 |
| 10 根 | **+0.57%** | +7 個百分點 |
| 30 根 | **+0.72%** | +9 個百分點 |
| 55 根 | +0.31% | +4 個百分點 |

> 🔴 **這張表上面會有一條硬性橫幅**（委員會 2026-09-20 裁定，見 7-5）：
> 「**無信賴區間；平均差為正不得讀成這條規則會賺。**」
> 要拿掉這條橫幅，必須先做出「差」的 bootstrap 區間。在那之前這些數字只是描述，不是證據。

**這就是你要的東西。** 哪些現成、哪些要做（我實查 `momentum/Analysis/event_samples/tables.py:118-131`）：

| 欄位 | 現況 |
|---|---|
| 筆數 `n` | ✅ 現成 |
| 平均 | ✅ 現成 |
| 中位數 | ✅ 現成 |
| **勝率** | ✅ **現成，而且不靠 `label`**——它直接用「報酬 > 0 的比例」算（`win_rate`） |
| 信賴區間 | ✅ 現成（bootstrap）。但**沒做切分時會標 unavailable**，因為沒有簇可以重抽樣 |
| 標準差 | ❌ **沒有，要加** |
| 最好／最差 | ❌ **沒有，要加** |

🔴 **上一版我寫「勝率是 `label` 加總得來的」——那是錯的，我查了程式才發現。**
勝率是直接從報酬的正負算的，跟 `label` 無關。

**這件事讓 `label` 的定位更清楚了**：它對你要的統計數據**一點貢獻都沒有**，純粹是匯入契約的形式要求。
你看到的每一個數字都是從報酬直接算出來的。

其他兩點：

- **筆數會隨持有期變少**（340→335），因為資料尾端算不出長持有期的報酬，那些事件**直接排除不灌 0**
- **為什麼要中位數和標準差**：平均值會被少數極端值拉走。中位數告訴你「典型情況」，標準差告訴你「有多不穩」——只看平均會誤判

**這張表就是你要的東西。**

---

## 四、現在有什麼、缺什麼

### 已經有的（不用重做）

| 零件 | 狀態 |
|---|---|
| 條件掃描產事件批的函式 | ✅ `generate_events` 已存在，能跑，過同一個驗證器 |
| label 自動算 | ✅ 已在上述函式內 |
| 算持有 N 根報酬 | ✅ `event_forward_return_table`，N 可指定任意一組，**不寫死** |
| 不開 IC 也能算報酬 | ✅ 報酬表跟 IC 的答案窗**沒綁在一起**（實查） |
| 產生隨機對照批 | ✅ 已有端點，會避開觸發點，種子可重現 |
| 讀取算好的特徵 | ✅ `FeatureLibrary.load` 能依「標的／週期／識別碼」讀出整張特徵表 |
| 對一張表求條件 | ✅ `parse_condition` ＋ `evaluate_condition` |

### 缺的四塊

| # | 缺口 | 白話 |
|---|---|---|
| 1 | **掃描函式沒有 API 接口** | 函式在，但 `api/` 裡**零個呼叫者**——前端根本碰不到它 |
| 2 | **條件讀不到 FF 欄位** | 「讀特徵表」和「求條件」兩端都有，**中間沒接**：沒有任何程式把 FF 的欄位名註冊給條件引擎。🔴 **而且現在寫錯欄名不會報錯**——找不到該欄時條件直接判「不成立」，你會得到「零筆命中」而不是錯誤訊息（codex 查出）。這條必須在規格裡改成**報錯**，否則你永遠分不清「條件真的沒成立」和「欄名打錯」 |
| 3 | **比較比的是達標率，不是報酬** | 現有比較端點回「觸發批裡 label=1 佔幾成 vs 隨機批佔幾成」，不是平均報酬。而且它**常常是灰的**——它要求觸發批帶「標籤規則收據」（`label_rule`），一般匯入沒有，就回「無從確認兩批用同一條規則」而不並排 |
| 4 | **隨機抽樣現在不公平** | 見下表 |
| 5 | **統計欄位差兩個** | 現成的有筆數／平均／中位數／勝率／信賴區間；**標準差**與**最好／最差**沒有，要加（實查 `tables.py:118-131`） |
| 6 | **沒有欄位選擇器** | 這個 run 有 18 萬個以上欄名，後端能回清單但前端只能拿到一整包平鋪的。**沒有逐層下拉＋搜尋，你寫不出條件** |

#### 缺口 4 細項（四項都要修）

| 項 | 現況（實查） | 應該是 |
|---|---|---|
| 抽幾筆 | 前端預設 100 筆，**不跟著觸發批** | 跟觸發批同筆數 |
| 候選資格 | 只看**單一**答案窗長度 | 看你選的**最長**持有期 |
| 樣本之間 | 只禁抽到同一根，**不禁答案窗互相重疊** | 應互斥 |
| 期間池 | 取自觸發批最早～最晚的 `t0` | 應為整個 run 的期間 |

---

## 五、做法：九步

（原本七步；委員會 2026-09-20 挖出兩件我沒算進去的工作，見第七節）

1. **條件引擎吃 FF 欄位**——把讀到的特徵表欄位名註冊給條件引擎，讓你能寫 `close_1h_trend_EMA_5 > close_1h_trend_EMA_10`。**並且欄名打錯要報錯，不能靜默回零命中**
2. **欄位選擇器**——逐層下拉（來源欄→週期→類別→指標→參數）＋打字搜尋。**沒有這個你根本沒辦法從 18 萬個欄名裡挑出要的**
3. **掃描函式接上 API**——條件 → 事件批 → 匯入（`label` 用漲跌正負號填，進場價用 `next_open`）
4. 🔴 **統一「持有 N 根」的時鐘**——鎖定「從成交那根起算」，並讓 `label` 與報酬表用同一個出場根（現在差一根，見 7-3）
5. 🔴 **連續成立只進場一次**（first-of-run）——否則 340 筆可能只是 10 段趨勢（見 7-4）
6. **報酬表補兩個統計量**——加上**標準差**與**最好／最差**
7. **產隨機對照批**——進場語意／出場錨／持有期集合**由觸發批帶入**，並修掉抽樣四項（見 7-6、7-7）
8. **比較端點改成比報酬**——現在只讀 label 算達標率；並加上 Δ 的時間塊／群集 bootstrap 區間（見 7-5）
9. **前端**——掃描設定畫面（持有期**直接輸入**、即時顯示約幾天）＋ 兩組統計並排的表＋**硬性橫幅**（無區間時不得讀成會賺）

### 兩條我查過、確定不走的路

| 曾考慮 | 為什麼不走 |
|---|---|
| 改匯入契約，允許「只有觸發點、沒有反例」的批 | **不需要**。label 系統自己算，契約天生就滿足 |
| 掃描時把隨機列混進同一批 | **被程式碼明文擋死**。理由寫在註解裡：對照組混進條件樣本，分母不再是無條件基準——「**那正是 estimand 的死法**」 |

---

## 六、你已經定的（2026-09-20）

| # | 你的裁定 |
|---|---|
| 1 | 持有期**用根計算，畫面顯示約多少天**；**前端直接輸入，不要勾選清單**；輸入和輸出都要支援多個 |
| 2 | **FF 有什麼就用什麼**，資料源就是 FF，不為了補 EMA_30 重跑 |
| 3 | **完全沒有門檻設定，也沒有要給。** 要的是「符合條件買入、持有特定時間後漲跌多少、統計數據如何」 |
| 4 | **接受「內部用漲跌正負號當 `label`」**——走既有管線，保住未來資料防護 |
| 5 | **進場價傾向用下一根開盤價**（`next_open`）——但你聲明這只是想法、沒深入評估風險與統計誤區，**交委員會討論**（見第七節） |
| 6 | **順序＝本票方向與做法定案 → 白話文檔整理 → 才做本票實作** |
| 7 | **欄位選擇器＝逐層下拉（來源欄→週期→類別→指標→參數）＋打字搜尋，兩個都做** |
| 8 | 先看完整白話文檔＋舉例，才決定放不放行 |

---

## 七、委員會的裁定（2026-09-20，兩家唯讀，十四條）

我先寫了自己的答案讓他們攻。結果：**他們推翻我三處、確認我三處、挖出四件我完全沒想到的事。**

### 7-1 進場價：結論對，但我的理由是錯的

| | |
|---|---|
| **結論** | ✅ **用 `next_open`**（下一根開盤價）。兩家都同意你的直覺是對的 |
| 🔴 **我錯在哪** | 我寫「跳空成本由 `next_open` 自然承擔」——**方向講反了**。`next_open` 的損益**不包含** t0 收盤到次根開盤那一段；是 `trigger_close` 把它算進去了。所以**有樂觀偏誤的是 `trigger_close`，不是 `next_open` 多付成本** |
| **正確說法** | 對「這條規則實際買得到的報酬」，`next_open` 算出來比較低的那個數字才是對的 |
| **要揭露而算不進去的** | 開盤第一筆成交價 ≠ 你真的買得到的均價、資金費、滑價 |

### 7-2 🔴 `label` 的問題比我以為的嚴重——它會出現在主表

**我原本判斷「`label` 不會進到你看得到的數字」，兩家都說錯了。**

- 前端的事件報酬表**一律讀「正例組／反例組／全體」三組**，不讀我以為的那個全體表
- `label` 若＝某個持有期的漲跌正負，那一列的**「正例組」平均報酬恆為正、勝率恆為 100%**——這是**套套邏輯**（把賺的挑出來再說他們賺）
- 而且它會出現在**主表**，不是旁註

**處置**：**主表只顯示全體一組**，正例／反例分組在本票不顯示。`label` 只活在匯入契約裡。

🔴 **另外查出一件跟你的裁定直接衝突的**：前端隨機對照的**預設門檻是 0.02**，不是你要的「完全無門檻」。這條必須改掉。

### 7-3 🔴「持有 N 根」現在有兩套時鐘（我完全沒想到）

兩張會同時出現的表，對「持有 5 根」的定義不一樣：

| 表 | 出場在哪 |
|---|---|
| 事件報酬表 | 進場那根 + 5 |
| 全 K 線表 | t0 + 5 |

選 `next_open` 時進場根是 t0+1 ⇒ **一張表出場在 t0+6、另一張在 t0+5**。
更糟的是：同一個事件可能在報酬表顯示**正報酬**、卻被標成 `label=0`。

**處置**：EVENTSCAN 鎖定「**從成交那根起算 N 根**」（對齊你說的「買入後持有 N 根」）。全 K 線表本票不顯示。

### 7-4 🔴 均線條件會連續好幾根都成立（我完全沒想到）

多頭排列一旦成立，通常會**連續維持好幾十根**。如果每一根都算一筆「買入持有 N 根」：

> **340 筆可能其實只是 10 段趨勢**，你會以為有 340 次獨立的買賣，實際上沒有。

**處置**：主要數字用「**每一段連續成立只進場一次**」（first-of-run）。每根都算的版本保留為參考，但不當主數字。

### 7-5 「差 +0.72%」是真的還是運氣？現在答不出來（推翻我）

**問題長這樣**：表上寫「觸發批平均 +1.05%、隨機批 +0.33%、**差 +0.72%**」。

那 **+0.72% 到底是這條規則真的有效，還是剛好抽到好樣本？** 這是你看完表最該問的問題。

一般統計用**信賴區間**回答：「差是 +0.72%，區間 [+0.31%, +1.13%]，不含 0 ⇒ 大概不是運氣」。
但**這條路現在算不出這個區間**，兩個原因：

**原因一（技術性的）**：系統算區間需要先做「切分」這個前置步驟，而事件掃描這條路**沒有做切分** ⇒ 系統直接標「無法提供」。

**原因二（比較根本的）**：你的 340 筆**不是 340 個獨立樣本**。

> 多頭排列連續成立時，第 100 根和第 101 根的「持有 30 根報酬」
> 有 29 根是**重疊的同一段價格**。它們幾乎是同一筆交易，不是兩次獨立的下注。

如果拿「340 個獨立樣本」的公式去算區間，**算出來會太窄**——讓你以為「很確定有效」，其實資訊量遠沒有 340 筆那麼多。

**還有一個常見的誤用**：有人會分別畫觸發批和隨機批各自的區間，看它們重不重疊。**這不能回答「差是不是 0」**——兩個區間重疊，差仍可能顯著；不重疊，也不保證顯著。要看的是**「差」本身的區間**。

**處置**：用 bootstrap（把資料重複重抽幾百次、每次重算一遍差、看這些差分布在哪）算出「差」的區間，而且**重抽的單位要是「一整段連續成立」而不是「一根」**，這樣才不會把重疊當成獨立。

**在做出來之前**，表上方會有一條硬性橫幅：

> 「**無信賴區間；平均差為正不得讀成這條規則會賺。**」

### 7-6 隨機對照要從「哪段時間」裡挑？（我自我推翻是對的，但還不夠）

**問題長這樣**：要挑 340 根隨機的 K 線當對照，從哪段時間裡挑？

**如果從整個 run 挑**（2024-01 到 2026-04，兩年多）：

> 假設你的條件剛好都在 2024 年那波多頭成立。那你比的是
> 「**2024 多頭的報酬** vs **兩年平均的報酬**」——
> 差異來自**行情**，不是來自你的條件。條件根本沒被驗證到。

所以要**從有觸發點的那些月份裡挑**，這樣兩邊在同一種行情下比。我先前寫「該用整個 run」是錯的，自己推翻掉。

**但還不夠**，委員又抓到一層：

> 現在系統分配名額是按「**每個月有多少根可以挑**」，不是按「**每個月有幾個觸發點**」。
>
> 假設觸發點 80% 集中在 1 月，但 1、2、3 月的 K 線數量差不多 ⇒
> 隨機批還是會**平均分到三個月**。結果又變成「以 1 月為主 vs 三個月平均」，
> 同一個毛病換個形式又出現。

**三條機械規則**：

1. 期間池＝**有觸發點的那些月份**（UTC）的聯集
2. 名額按**觸發筆數**分：1 月有 80% 觸發，1 月就抽 80% 的對照。某月候選不夠就**直接回報失敗**，不把缺額倒到別的月充數
3. 「整個 run」那版只能當**另一張參考表**，標題要寫清楚它回答的是不同的問題（「這個條件是不是只是選到了好行情」）

### 7-7 抽樣不足時不准偷偷縮水（推翻我的措辭）

我說「樣本必須互斥」——codex 說這措辭過強：重疊**不影響平均值**，影響的是**信賴區間和有效樣本數**。但後果判斷我對。

現行最危險的是：候選不夠時會**靜默把 n 縮小**（`n_target = min(n_requested, candidate_count)`）——**估計量被換掉了，畫面看起來卻像成功**。

**處置**（採較嚴的版本）：不縮 n、不放寬互斥、不抽相鄰根充數。互斥排下來不夠就**回報「無法比較」**並列出四個計數，讓你知道為什麼。

### 7-8 還沒決定的

| # | 問題 | 誰決定 |
|---|---|---|
| 1 | **`ret_entry` 與 `ret_label_anchor` 主顯示哪個** | 委員會已建議：主欄 `ret_entry`（可成交的那個），`ret_label_anchor` 當診斷、**不拿來跟隨機批相減**。我採納 |
| 2 | **多持有期時 `label` 綁哪一個** | 委員會建議綁一個「主持有期」，其他持有期不做 label 衍生的推論。**這題要你選主持有期**，或授權我跟委員定 |
| 3 | **成本：主數字扣不扣？要不要加 breakeven 欄？** | 我建議「主數字不扣＋加 breakeven 欄＋揭露三項算不進去的」，見 7-9。等你一句話 |
| 4 | **多標的什麼時候做** | 你已裁定先做單標的，多標的併 GAP-4 |

### 7-9 手續費與滑點要不要算？（你 2026-09-20 提問）

**業界做法分三層，答案不一樣**：

| 算什麼 | 扣不扣成本 | 為什麼 |
|---|---|---|
| **IC 本身** | **不扣**（業界標準） | IC 是「因子值 vs 未來報酬」的相關係數，衡量**排序能力**。成本發生在**換倉**，不發生在「某個時點的相關性」上——扣了沒有明確定義 |
| **淨 IC（net IC）** | 扣 | 進階診斷：扣掉換手成本後因子還有沒有效。**不是 IC 的標準定義**，是額外一層 |
| **事件研究的報酬** | 學術通常不扣 | 它要衡量的是「資訊效應」，所以報未扣成本的異常報酬。但實務要判斷「能不能真的賺」就得扣 |
| **回測** | **一定扣** | 它宣稱的就是「這樣做會賺多少」，不扣就是假的 |

**本專案現況**（實查 `momentum/Analysis/net_ic_analyzer.py`）：

- 已有淨 IC，公式＝`淨值 = 毛值 − (成本bps ÷ 10000) × 換手率`
- **預設關閉**（`cost_enabled = False`）
- 🔴 裡面有一個設計比「扣成本」更有用：**`breakeven_cost_bps`**

**`breakeven_cost_bps` 是什麼**：它不要你猜費率，而是**反過來告訴你「成本要高到多少，這個獲利才會被吃光」**。

> 例：它回 `3.2`，而幣安吃單費約 5 bps ⇒ 你當場就知道
> **這條規則扣完手續費就沒了**，完全不必去設定任何費率。

**我對 EVENTSCAN 的建議**：

1. **主數字報未扣成本**（跟事件研究慣例一致，也跟委員建議一致）
2. **但加一欄 breakeven**：這批事件的平均報酬，在多少 bps 成本下會歸零
3. **不在本票發明費率**——我不知道你用哪家交易所、什麼手續費等級、吃單還是掛單

理由：讓你猜一個費率再扣，猜錯了整張表就是錯的；breakeven 反過來給你一個**不需要假設**的判準。

**委員另外提醒的三項成本**（算不進報酬公式，只能揭露）：
開盤第一筆成交價 ≠ 你真的買得到的均價、永續合約的資金費、滑價。

### 7-10 委員另外列了二十多條「該定而未定」

codex 列 16 條、composer 列 10 條，包括：零報酬算漲還是跌、交易成本要不要算、重疊持有不能加總成資金曲線、
反覆換條件試到有效果（多重比較）、排除窗太寬會讓對照變成「條件區 vs 平靜區」等等。

**這些我會在寫規格時逐條處理**，不需要你現在決定——但其中兩條我要先講，因為它們影響你怎麼讀數字：

- **本票不做權益曲線**。340 筆重疊的持有期**不能**加總成「策略報酬」，那是另一件事（回測）。
- **換 EMA 組合試到有效果 = 沒有意義**。畫面會寫「這是同一段樣本上的描述，不是驗證過的策略」。

---

## 八、這張票多大

**大票**——命中「跨模組／共用路徑」，會動到條件掃描、事件匯入、報酬計算、比較端點、前端五個地方。

依規矩要走完整流程：規格 → 施工清單 → 兩家委員對抗審查 → 實作 → 審碼 → 收斂。

### 做的順序（你 2026-09-20 裁定）

```
① 本票的方向與做法定案   ← 現在在這裡
     ├─ 委員會回答第七節的量化問題（進場價口徑等）
     └─ 你確認最終版方向說明
② 白話文檔整理（中票）    ← 定案後先做這個
③ 本票實作               ← 規格 → 施工清單 → 審查 → 實作
```

**為什麼白話整理插在中間**：本票是大票、會跑很多輪，期間會長出一批新的白話文檔。
文檔結構還亂的時候先跑大票，等於讓問題擴大再收。先把索引與封存整理好，本票的文件就照規則進去。

---

## 九、委員會推翻我的三處（2026-09-20 consult r2）

我依規矩先寫了自己的答案讓他們攻。結果如下——留著給你看我在哪些地方會錯：

| 題 | 我的答案 | 委員會 |
|---|---|---|
| 進場價跳空 | 「跳空成本由 `next_open` 自然承擔」 | **方向反了**。`next_open` 不含那段，`trigger_close` 才含 ⇒ 有樂觀偏誤的是後者。結論（選 `next_open`）仍對，理由錯 |
| `label` 副作用 | 「對使用者看到的數字零影響」 | **不成立**。前端主表一律讀正例／反例分組 ⇒ 套套邏輯會上主畫面 |
| 兩張表並排 | 「這是有效的統計比較」 | **不是**。沒有信賴區間、報酬序列高度重疊、兩側區間重疊與否也不能回答差是否為 0 |

**兩處確認我對**：兩批進場語意不一致是阻斷性缺陷（而且比我說的更嚴重）、期間池我自我推翻是對的。

**四件我完全沒想到**：持有 N 根有兩套時鐘、均線條件連續成立會讓 340 筆其實只是 10 段、
候選不足時會靜默縮 n、前端隨機對照預設門檻 0.02 與你的裁定衝突。

---

## 十、我在偵察那輪自己抓到的三個錯

留著給你看，不是自責，是讓你知道哪些地方我容易出錯：

1. **我原本推薦的做法（掃描時把隨機列混進同一批）是錯的**，在給你看之前查碼才發現被明文擋死。
2. **委員給的 EMA 週期清單不完整**（他列 7 個，實際有 14 個）。我沒有直接轉述，自己查了 manifest 才寫進上面的表。
3. **我寫「勝率是 `label` 加總得來的」，錯了**。實際上勝率是直接從報酬的正負算的（`tables.py:128`），跟 `label` 無關。
   這個錯不是無關緊要——它讓 `label` 看起來有用，實際上它對你要的數字毫無貢獻。查了程式才發現。

**共同模式**：三個錯都是「我以為系統是這樣，沒去查」。這也是為什麼你定的新規則（先討論方向再寫規格）是對的——
早點把理解攤開來，錯誤在寫規格前就被抓到，不是七輪之後。
