# GAP-2 施工看板：邊際 IC／多因子組合（2a）＋倖存因子輸出契約（2b）

> 一句話：補「這個因子相對已經選進來的那幾個，到底多帶來多少**新**資訊」的產品答案，並先把倖存因子交給 ML 的**契約格式**釘死（橋本體等 ML 層穩定）。
> 技術文件：`docs/GAP2_MARGINAL_IC_SPEC.md`（R7 FROZEN）、`docs/GAP2_MARGINAL_IC_TODO.md`；殘留登記 `docs/IC_QUANT_GAP_REGISTRY.md`「GAP-2 待補完」。

## 現在在哪
- ✅ **規格（SPEC）定版**（8/18）：偵察 1 輪（三家＋我）＋對抗審 6 輪（14→12→4→4→2→0），七份收斂檔全數三家蓋章；你白話閘核准，並裁定前端表格＋開關（預設開）納入第 5 批。
- 🔄 **清單（TODO）草稿 R2**：第一輪對抗審三家 20 條（都是「執行端讀了會不會卡」的可執行性缺口，非設計爭議）已全部寫回；第二輪複核已派出，等結果 → 定版。
- ⏳ 之後：清單定版（FROZEN）→ 第 1 批動工。

## 五批（每批：pytest → 探針 → commit → push → 白話同步；三家 code review＋蓋章）
| 批 | 內容 | 狀態 |
|---|---|---|
| B1 | 契約 JSON 單一真相源＋邊際 IC 純函式＋9 類 oracle＋探針腳本 | 待清單定版 |
| B2 | 多因子組合＋區塊 bootstrap 信賴區間 | — |
| B3 | 契約讀取／驗證／組裝（缺欄／多欄／枚舉外／身分不符都擋） | — |
| B4 | 接進 IC 主流程（三個入口＋fallback）、倖存者檔、報告新節、golden、wiring 改讀契約、預算 receipt | — |
| B5 | 前端：型別鏡像＋開關（預設開）＋一張唯讀表格 | — |

## 殘留（為何現在不做；已登記 registry，不會忘）
- G2-R1 橋本體：你裁定 blocked-by ML 層。 - G2-R2 用邊際 IC 二次挑選：多重比較政策無公認方法（needs-research）。 - G2-R3 橫截面路徑：等 #4 重建。 - G2-R5 真獨立 OOS：要改主線切分。

## 這輪最值得你知道的
- 主線「測試段」其實已被拿來挑倖存者，所以邊際 IC 在測試段的數字**不能說是獨立驗證**——契約強制帶機器可讀的揭露欄，並並列訓練段數字。這是 codex 唯一抓到、其他家沒抓到的。
- 我在前三輪**漏跑收斂檔蓋章**被 codex 依合約停工（GAP-1 同事故再犯），已補齊七輪蓋章。
