# 現在做到哪

> **這份回答一個問題：現在在做什麼、要不要你決定。**
>
> 🔴 **只有現在。** 狀態變了就**整段覆蓋**，不往上疊、不留舊條目。
> 想看做過什麼 → 各票的結論書；想看還有什麼沒做 → [還沒做的事.md](還沒做的事.md)。

---

## 現在在做

**FF-STAT：要不要做平穩化，改成看特徵「是什麼」，不再看名字。**（你 2026-09-24 第 4 點開的票）

- **程式改了沒**：**正在改，5 批已完成前 3 批**，每批都經委員審過程式碼才算收。
  - ✅ **第 1 批**：拿掉「看名字就跳過檢定」的清單；要不要做 fracdiff 只看特徵真正屬於哪一層（查不到就直接報錯，不再從名字猜）；每一欄的判斷結果（層、檢定 p 值、有沒有做、用的 d）都寫進產出紀錄。
  - ✅ **第 2 批**：d\* 算不出來時不再偷偷用 d=1 硬套，改成保留原值、記下失敗、整次產出標記為「部分完成」；快取檔壞掉、寫不進去也都會記下來。
  - ✅ **第 3 批**：判斷只用開始日期之前的資料（修掉偷看未來的關鍵），三個檢定步驟用同一個樣本數 N；依你 2026-09-26 的裁定，某欄歷史不夠時只有那一欄不做平穩化、標記「歷史不足」並把整批標為「部分完成」。後半審了 4 輪（Codex 最後一輪放行），期間修掉 6 個問題：同一個程式物件重跑「IC 優先」時會沿用上一次的暫存資料、參考標的在開始日前沒資料會整批失敗、非 UTC 時區的市場（台股、美股）日期比較會當掉、跨標的參考資料的快取不分時間段（早的時間段會拿到晚的資料，產出靜默變空）、多標的平行跑完不刪暫存目錄；另外補修 3 支因 FF-STAT 而壞掉、之前沒被跑到的測試。
  - ⏳ **第 4 批**：沒填開始日期時自動預留校準段，並在前端顯示（順手修第 3 批最後一輪提到的暫存清理小漏洞）。**程式還沒改**：動工前發現原本測試窗根本湊不滿前史，規格重新設計（依你裁定「主訓練週期優先、主週期＝介面主框架」），委員審到第 12 輪兩家都放行；逐條白話版在 [FF-STAT未填起始日審閱.md](FF-STAT未填起始日審閱.md)，**等你同意才動程式**。
  - ⬜ **第 5 批**：量 500／1000／2000 根的耗時、記憶體與判斷差異，帶數字請你定 N，並請你選 N 的定義（「N 個有效值」或「N 根時間長度」）。
- **速度代價（已實測）**：用預設設定（平穩化關閉）生成時，改前改後一樣快；打開 fracdiff 時，12h 600 根一次多約 95 秒（336 → 431 秒），在你之前同意的「每次多約 2 分鐘」範圍內。

**IC-First 管線合一（原 ICFIRSTALIGN）**：**程式還沒改**。你 2026-09-26 裁定 `run_ic_first` 不刪、FF-STAT 之後立刻做：IC 頁「套用後處理」改用特徵工廠正式轉換程式（順便修掉偷看未來）、`run_ic_first` 不再借用工廠殘留狀態、修真實資料 IC 報錯。

**FKPERF**：程式已改完並接上；收尾前還要找一個沒在改檔的空檔跑一次全套治理測試。

**FF-NAME／FFDSTAR**：**程式都還沒改**，排在 IC-First 管線合一之後。

## 做到哪

| 項目 | 狀態 |
|---|---|
| FF-STAT 規格書、施工清單 | ✅ 定案 |
| FF-STAT 第 1、2 批 | ✅ 完成並審過 |
| FF-STAT 第 3 批 | ✅ 完成並審過（後半審 4 輪，Codex 放行） |
| FF-STAT 第 4 批規格（沒填開始日期） | ✅ 委員審到第 12 輪放行，⏳ 等你逐條審閱 |
| FF-STAT 第 4 批程式（自動預留校準段＋前端顯示） | ⬜ 你同意後動工 |
| FF-STAT 第 5 批（實測後請你定 N） | ⬜ 第 4 批之後 |
| 回歸時查到的 9 支舊紅測試（含特徵工廠 L1 與凍結基準對不上） | ⬜ 已登記、待查，不是 FF-STAT 造成 |
| FF-STAT 校準窗長 N | ⬜ 第 5 批實測後請你定 |
| `run_ic_first` 去留查證 | ✅ 三方查完，你裁定不刪 |
| IC-First 管線合一（含 IC 頁偷看未來） | ⬜ FF-STAT 之後立刻做 |
| FKPERF 收尾全套測試 | ⏳ 待空檔 |
| FF-NAME、FFDSTAR | ⬜ IC-First 管線合一之後 |

## 要你決定嗎

**要。** 請看 [FF-STAT未填起始日審閱.md](FF-STAT未填起始日審閱.md)，同意後才動第 4 批程式。之後會問你的：校準窗長 N 與 N 的定義（第 5 批量出數字後）、FFDSTAR 多出一個輸出檔的大小。

## 卡住嗎

沒有。只有一件要你動手：我這邊的權限不能刪檔，測試暫存（約 1.1 GB）的清理指令已在對話中給你，執行過就不用管。

## 下一件事

你同意第 4 批規格後：寫程式 → 委員審 → 修 → 收。
