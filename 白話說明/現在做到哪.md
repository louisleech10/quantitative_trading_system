# 現在做到哪

> **這份回答一個問題：現在在做什麼、要不要你決定。**
>
> 🔴 **只有現在。** 狀態變了就**整段覆蓋**，不往上疊、不留舊條目。
> 想看做過什麼 → 各票的結論書；想看還有什麼沒做 → [還沒做的事.md](還沒做的事.md)。

---

## 現在在做

**FF-STAT：要不要做平穩化，改成看特徵「是什麼」，不再看名字。**（你 2026-09-24 第 4 點開的票）

- **程式改了沒**：**正在改，已完成 5 批中的前 2 批半**（第 3 批前半已收），每批都經 Codex、Composer 審過程式碼才算收。
  - ✅ **第 1 批**：拿掉「看名字就跳過檢定」的清單；要不要做 fracdiff 只看特徵真正屬於哪一層（查不到就直接報錯，不再從名字猜）；每一欄的判斷結果（層、檢定 p 值、有沒有做、用的 d）都寫進產出紀錄。
  - ✅ **第 2 批**：d\* 算不出來時不再偷偷用 d=1 硬套，改成保留原值、記下失敗、整次產出標記為「部分完成」；快取檔壞掉、寫不進去也都會記下來。
  - ✅ **第 3 批前半**：三個檢定步驟改用同一個樣本數 N（原本有一處寫死 500、有一處會依週期偷偷縮小）；另外寫好校準要用的基本工具。審查抓到三處問題已修（多週期時預留的前史長度算錯、校準資料的完整性沒驗、時區混用會直接當掉）。
  - ⏳ **第 3 批後半（程式已改完、審查中）**：判斷只用開始日期之前的資料（修掉偷看未來的關鍵）。依你 2026-09-26 的裁定，某欄歷史不夠時只有那一欄不做平穩化、標記「歷史不足」並把整批標為「部分完成」，不再整批停。
  - ⬜ **第 4 批**：沒填開始日期時自動預留校準段，並在前端顯示。
  - ⬜ **第 5 批**：量 500／1000／2000 根的耗時、記憶體與判斷差異，帶數字請你定 N。
- **速度代價（已實測）**：用預設設定（平穩化關閉）生成時，改前改後一樣快；打開 fracdiff 時，12h 600 根一次多約 95 秒（336 → 431 秒），在你之前同意的「每次多約 2 分鐘」範圍內。

**另外記下的舊問題**：「IC 優先」選特徵路徑用真實資料一定在最後算 IC 那步報錯（ICFIRSTALIGN），不是這張票弄壞的，排在 FF-STAT 之後。

**FKPERF**：程式已改完並接上；收尾前還要找一個沒在改檔的空檔跑一次全套治理測試。

**FF-NAME／FFDSTAR**：**程式都還沒改**，排在 FF-STAT 之後。

## 做到哪

| 項目 | 狀態 |
|---|---|
| FF-STAT 規格書、施工清單 | ✅ 定案 |
| FF-STAT 第 1、2 批 | ✅ 完成並審過 |
| FF-STAT 第 3 批前半 | ✅ 完成並審過 |
| FF-STAT 第 3 批後半 | ⏳ 程式已改完、Codex／Composer 審查中 |
| FF-STAT 第 4、5 批 | ⬜ 依序 |
| FF-STAT 校準窗長 N | ⬜ 第 5 批實測後請你定 |
| ICFIRSTALIGN（IC 優先路徑報錯，舊問題） | ⬜ FF-STAT 之後 |
| FKPERF 收尾全套測試 | ⏳ 待空檔 |
| FF-NAME、FFDSTAR | ⬜ FF-STAT 之後 |

## 要你決定嗎

**現在不用。** 接下來會問你的只有：校準窗長 N（第 5 批量出數字後）、FFDSTAR 多出一個輸出檔的大小。

還有一件你之前沒回的：探針先前誤寫進共用快取的 `data_cache/feature_preprocessing/d_star_BTCUSDT_1h_dcc154ced6b6.json`，要刪還是留？它是快取，不會造成錯誤數值。

## 卡住嗎

沒有。今天電腦重開機一次、磁碟一度只剩 1.5 GiB（測試暫存累積），已刪掉我自己產生的暫存約 47 GB，資料完好。Codex 已照你的指示改用 GPT-5.6 Luna、effort max。

## 下一件事

等第 3 批後半審查 → 修正 → 第 4 批（沒填開始日期時自動預留歷史）。
