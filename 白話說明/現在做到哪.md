# 現在做到哪

> **這份回答一個問題：現在在做什麼、要不要你決定。**
>
> 🔴 **只有現在。** 狀態變了就**整段覆蓋**，不往上疊、不留舊條目。
> 想看做過什麼 → 各票的結論書；想看還有什麼沒做 → [還沒做的事.md](還沒做的事.md)。

---

## 現在在做

**FF-STAT：要不要做平穩化，改成看特徵「是什麼」，不再看名字。**（你 2026-09-24 第 4 點開的票）

- **程式改了沒**：**還沒改**。目前在審「施工清單」——也就是先寫好的 121 支驗收測試，外加一份「改之前長什麼樣」的快照。實作時就拿這些來證明做對了。
- **規格書**：已定案。你 2026-09-24 回了「同意施工」。之後寫施工清單時實跑發現兩個事實，改了兩處描述：平穩化的結果另存新欄、原欄不動；真實 K 線資料沒有任何缺口。
- **你的裁定已寫進去**：
  - 校準窗長「先實測再定」：預設 500 根，實作完量 500／1000／2000 根的耗時、記憶體、判定差異，再請你定。
  - 沒填開始日期時「自動預留校準段」。
- **審查進度**：清單已審到第 20 輪。第 19 輪 codex 又抓到 6 個漏洞，全都修了。例如：
  - 有一支測試在兩邊都失敗時也會算通過。
  - 自動預留和手動填同一天時，沒有比對產出內容是否相同。
  - 記憶體量測會把共用的記憶體重複算進去。

**另外發現一個舊問題（不是這張票弄壞的，已另開一票 ICFIRSTALIGN）**：用真實資料跑「IC 優先」那條選特徵路徑時，最後算 IC 的那一步一定會報錯。原因是兩份資料的時間標籤對不上——一邊是時間，另一邊是 0、1、2… 的流水號。既有的測試在主線上本來就是紅的。FF-STAT 的驗收已改成看報錯之前那一步的產出，不受它影響。這個修正排在 FF-STAT 之後。

**FKPERF：讓事實登記表的存檔檢查不再越來越慢。**

- **程式改了沒**：**已改並接上**。登記量放大 40 倍時，存檔檢查從 10.7 秒降到 0.56 秒。
- **剩下**：收尾前要跑一次全套治理測試（小時級，放背景）。這個測試跑的時候不能改檔案，上次跑到一半因為要改 FF-STAT 規格而停掉了，要等沒在改檔的空檔重跑。

**FF-NAME／FFDSTAR**：**程式都還沒改**。依序排在 FF-STAT 之後。

## 做到哪

| 項目 | 狀態 |
|---|---|
| FF-TFMETA | ✅ 完工 |
| FKPERF 新核心接上、變快 | ✅ 完成並審過 |
| FKPERF 收尾全套測試 | ⏳ 待空檔重跑 |
| FF-STAT 規格書 | ✅ 你已同意施工 |
| FF-STAT 施工清單（驗收測試） | ⏳ 第 20 輪審查中 |
| FF-STAT 實作 | ⬜ 清單審過後 |
| FF-STAT 校準窗長 | ⬜ 實作後實測，再請你定 |
| ICFIRSTALIGN（IC 優先路徑報錯，舊問題） | ⬜ FF-STAT 之後 |
| FF-NAME 規格書重寫 | ⬜ FF-STAT 之後 |
| FFDSTAR 規格書第 4 輪審查 | ⬜ FF-NAME 定案後 |

## 要你決定嗎

**現在不用。** 接下來會問你的只有兩件：

- **校準窗長**：FF-STAT 實作完、量出數字後才會問。
- **FFDSTAR 會多出一個輸出檔**：做到那裡時會量好大小再問你。

另外還有一件你之前沒回的：探針先前誤寫進共用快取的檔案 `data_cache/feature_preprocessing/d_star_BTCUSDT_1h_dcc154ced6b6.json`，要刪還是留？它是快取，不會造成錯誤數值。

## 卡住嗎

沒有。EVENTSCAN 照你說的沒動。Grok 仍暫停（額度用完），目前只有 Codex、Composer 兩家審查。

## 下一件事

等 FF-STAT 施工清單第 20 輪審查 → 兩家放行後我開始實作 → 委員審程式碼。
