# 現在做到哪 — 施工流程即時進度

> **這份是給你隨時開著看的。** 我每完成一個步驟就更新這裡，不用問我。
> 想知道「這票在解什麼」看 [G3-D2灰色項目說明.md](G3-D2灰色項目說明.md)；
> 想知道「踩過什麼坑」看 [流程摩擦記錄.md](流程摩擦記錄.md)。
>
> 🔴 本檔**只寫當前狀態**，不堆歷史。已完成的批次收成一行。

---

## 一眼看完

| 項 | 現在 |
|---|---|
| **在做哪一票** | `G3-D2` 灰色項目完成（五段串行） |
| **在做第幾段** | **第 3 段（B-D3）✅ 收工**——四輪複核（R1→R4），最後一輪三家一致零問題。**下一件＝第 4 段（全矩陣＋k／h 掃描）** |
| **卡住嗎** | 沒有 |
| **需要你決定嗎** | 🔴 **有一件**：治理測試被一條**本來就壞**的檢查卡住，跑不完整套。這不在本票範圍內，等於現在沒人負責修——要不要另開一張票？見文末「需要你決定的事」 |
| **最後更新** | 見 `git log -1 -- 白話說明/現在做到哪.md`（不手寫日期，手寫的會漂） |

---

## 五段大進度

| 段 | 內容 | 狀態 |
|---|---|---|
| 第 0 段 | 修取價（開盤語意別名錯價）＋建 label 凍結檔機制 | ✅ **完成並上 GitHub**（`49204458`） |
| **第 1 段** | 預測型解灰：契約 provenance／支援矩陣／golden 擴充／前端解灰／IC 頁三種報酬選項 | ✅ **完成並上 GitHub**（`54d8eb8e`；五輪複核 R2→R6，末輪三家一致零問題） |
| **第 3 段** | 兩段式（含未標籤匯出路徑） | ✅ **完成並上 GitHub**（`52295c6a`；四輪複核 R1→R4，末輪三家一致零問題） |
| 第 4 段 | 其餘進場語意全矩陣＋k／h 掃描 | ⬜ 未開工 |
| 第 5 段 | 平台隨機對照組 | ⬜ 未開工 |

---

---

## 第 3 段（兩段式）做了什麼

**你現在可以在搜尋頁選「兩段式」了。** 選了之後：

1. **一定要有兩段條件**（正例＋反例都要填）。只填一段時，兩顆匯出鈕**直接變灰**並寫明原因
   ——系統**不會**自作主張把它當成單段情境送出去。
2. **這條路不產生答案**。匯出的檔案裡**根本沒有「答案」這個值**（不是空白、不是 0，是整個欄位不存在），
   來源欄寫著「搜尋頁未標籤匯出」。這種檔**故意匯不進去**——你必須在 Excel 補完答案、
   把來源改成「我自己標的」才匯得進來。
3. **CSV 一定留一個空的答案欄給你填**。這是三位委員一起抓到的漏洞：原本 CSV 的欄位是
   「有哪些列出現過的欄」聯集，而這條路的答案欄根本不存在 ⇒ **CSV 裡連那一欄都沒有**，
   你在 Excel 裡沒地方填，整條「補標後匯入」的路就斷了。現在改成由**契約**決定該有哪些欄。
4. **摘要記下是哪兩段條件產生的**（兩段各自的指紋），順序有意義、第一段在前。

**驗過的數字**：後端 415 通過、前端 502 通過、型別檢查 8 行（都是本來就有的舊債）、
凍結檔 23/23、解耦檢查無新增違反。**破壞測試四批 31 種全紅**（每一種都確認「改壞了會被抓到」）。

---

## 這一段最值得記的一件事

**同一個毛病我犯了四次，每次的「修法」都還不夠，直到第四次才真的堵住。**

毛病是：我寫一條測試宣稱「這個值是從契約讀來的，不是我手寫的」，但測試只檢查**值對不對**。
問題在於——手寫一個跟契約一模一樣的值，測試看起來完全一樣，**什麼也沒證明**。

四次演進：
1. 第一次（上一段）：改用「跟手寫值**不同**的值」去測。
2. 第二次：我拿「明顯不同」的假值去驗，結果漏掉「**把契約現有的值一字不差抄一遍**」那種。
3. 第三次：委員預測了第二點會漏，我把那一刀做出來——**真的抓不到**。
4. **最終判準**：要證明一個常數的來源，必須**把來源整個換掉再重新載入**，看常數跟不跟著變。
   只餵假資料給「導出用的函式」，證明的是**那個函式**，不是**那個常數**。

第四次之後，我又請委員自己去想繞過的寫法。其中一位試了三種（假裝呼叫但丟掉回傳、
同一個檔案裡另抄一份、包一層函式再回傳寫死的值）——**三種全被抓到**。

這件事的價值不在這個票，在於：**以後任何「這個值來自某個單一來源」的宣稱，都有一個可用的驗法了。**

---

## 🔴 需要你決定的事（只有一件）

**治理測試（`pytest tests/governance`）跑不完整套**——它卡在一條**本來就壞掉**的檢查上
（代號 g7），三位委員這幾輪都撞到同一個地方。

我原本把這條記成「太貴所以沒跑」，委員指出那**不誠實**：不是太貴，是**被既有的紅擋住、根本不可判**。
已改成正確分類。

但改完之後有個問題：**修那條 g7 不在這張票的範圍內，等於現在沒有人負責推進它。**
codex 明確把這件事交回給你：**要不要為它另開一張治理票？**

我沒有自己決定，因為治理工作的排程是你的裁量範圍。
