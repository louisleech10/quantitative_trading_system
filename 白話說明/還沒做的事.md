# 還沒做的事 — 全專案唯一清單

> **這份回答一個問題：還有哪些沒做。** 不回答「怎麼做」「做過什麼」。
>
> 🔴 **做完就刪行，不留紀錄。** 想看做過什麼看各票的結論書。
>
> 本檔**取代**先前散在五個地方的殘留清單（`GAP-3還沒做的事`／`GAP-3還差什麼才算完整`／
> `接下來要做什麼`／`接下來要做的票`／ROADMAP 三張殘留表）。**以後只看這一份。**

---

## 一、會影響你現在能不能用（優先）

| 項目 | 白話 | 卡在哪 |
|---|---|---|
| **EVENTSCAN** | 條件成立買入、持有 N 期報酬 vs 隨機對照 | 🔴 **暫停中**：SPEC 審至第 12 輪、未凍結、仍剩 11 條 P1；你會另開 session 處理 |
| **事實登記表的生成器變慢** | 登記項目兩天內從 17 個翻倍到 35 個；改登記表或它產生的區塊時，存檔當下的檢查要等約 10 秒 | 等你決定要不要開票優化（不開也不會出錯，只是每次多等） |
| **GAP-3 的 UAT 驗收** | 事件型分析你自己實機驗一次 | 排最後；要含「改過 k／h 的事件批」 |
| **IC 頁「報酬量法」送出的 h 固定是 1** | 你宣告的深度不會傳下去，除非你完全不碰那個面板 | 前端要先能預覽後端解析結果 |
| **事件型沒有「成本歸零點」** | 序列型有、事件型沒有；不是做不到是沒做 | 排入 EVENTSCAN 第 10 步 |
| **邊際 IC 沒有事件型驗收** | 事件 run 會照算，但沒有人驗過它算得對不對 | 排入 ICPATH |

---

## 二、排定順序的票

| # | 票 | 一句話 | 狀態 |
|---|---|---|---|
| 1 | **EVENTSCAN** | 條件掃描 → 持有報酬 vs 隨機對照 | 🔴 **暫停**（SPEC R12 收斂、未凍結）；你會另開 session 處理；寫的同時建 B，見下 |
| 2 | **FF-TFMETA** | FF run 的週期標記只記單一週期，多週期 run 會標錯 | **程式一行都沒改**。已查清楚是真缺陷；只對新 run 生效；新格式清單檔範例已備 |
| 3 | **FF-NAME** | 衍生層欄名沒經過命名正規化（3,596 欄／0.859%） | **程式一行都沒改**。已查清楚是真缺陷；排在 FF-TFMETA 之後 |
| 4 | **ICPATH** | 已完成項目逐項確認兩路（全域序列型／事件型）都支援且算得對 | 未開工 |
| 5 | **GLOBALH** | 多個持有期全部算、逐列標 h／k、可篩 | 未開工 |
| 6 | **UAT 驗收** | 你實機驗收（一定最後） | 未開工 |

### 夾在 EVENTSCAN 裡的 B（不是獨立的票）

寫 SPEC 時，凡是**會被 TODO／測試／白話再抄一次的值**，改成「只寫一次」的格子，不手打。
約 40–60 條（時鐘定義、進場只算一次、隨機對照抽樣四條、統計欄、成本歸零點公式、
失敗原因碼、欄名打錯要報錯、你的七項裁定…）。

- **它解什麼**：同一個值被抄到多處、改一處漏多處；值打錯但格式合法
- **它不解什麼**：架構對不對、零件怎麼接、為什麼這樣設計、同一份檔內前後矛盾
- **有沒有效怎麼看**：before 已記（每輪 1.00 條文件同步類問題，見
  `handoffs/run_receipts/20260921-eventscan-b-baseline.json`），做完用同一支指標比
- **已知的洞**：`docs/` 不在手寫偵測範圍內 ⇒ 貼了標記的格會同步，但在 SPEC 別的段落
  手打同一個值不會被擋。補洞代價還沒查

---

## 三、待觸發（條件沒到就不做，不是拖延）

| 項目 | 要等什麼 |
|---|---|
| GAP-4 Pooled/Panel IC | 排程即可開票；EVENTSCAN 的多標的也併這裡 |
| GAP-5 容量 ADV 接線 | 等成交量資料源就緒 |
| GAP-6 430K 規模防護 | 併 IC-PERF／串流 epic |
| 票 A（timing-overlap 診斷） | 硬前置 FU-2 未補完 |
| 票 B（多標的橫截面 attribution） | 等研究宇宙變多標的；硬前置 FU-2 |
| FU-1 exposure fillna fail-closed | 下次動 exposure 家族時順手做 |
| GAP-2b 橋本體 | 等 ML 層穩定 |

---

## 四、技術殘留（你不會看到，我自己追）

**SPLITUNIFY**：`R-4` 另開接線票／`SU-RESID-1`,`4`,`5`,`9A-UI`,`C5-TARGETS`,`V8-ATTEST`,
`PAUSED-NO-RESULT`,`COMMITTEE-MODEL-EVIDENCE` 八條皆為待觸發。

**GAP-3**：20 條殘留，其中 5 條你在畫面上看得到（已併入第一節）；
其餘為測試強度、規格洞、治理環境三類。逐條見各票結論書。

**FU-2**：對齊與缺席 fail-closed 已落地，剩「對齊後整欄 NaN 不擋」一條守衛。

**治理線**：兩條治理工作線已於 2026-08-14 由你裁定不再擴建，故本清單不列（狀態見 `docs/ROADMAP.md` 生成區塊）。

---

## 五、這份怎麼維護

- **新增**：開票時在第二節加一行；發現待觸發項在第三節加一行
- **刪除**：做完**直接刪行**，不改成「已完成」也不留刪除線
- **不寫**：不寫日期、不寫過程、不寫「為什麼延後」（那些在票的結論書）
