# IC 健檢施工進度看板

**這份是白話版**：SPEC/TODO 在做哪些事、每件做到哪。技術版＝`docs/IC_HEALTHCHECK_SPEC.md`＋`docs/IC_HEALTHCHECK_TODO.md`（皆已凍結）。
狀態欄我每次 commit 會更新；要看最即時的程式碼證據，看 git log 的 `feat(ic-hc)` 系列。

## 走到哪了（一眼版）

```
偵察（四方查證）完成 → SPEC（三家審→修→複驗→蓋章）凍結 → TODO（同流程）凍結
→ **六批全部完成** → **三家 code review 完成**（8 條意見：5 修 2 具名殘留 1 契約回應；修補經原提出方複驗全關閉、三家蓋章）→ **✔ EPIC 收工（2026-08-17）**
→ 下一步＝缺口票排程（docs/IC_QUANT_GAP_REGISTRY.md，首位 DSR/PBO/MinBTL）
```

## 這個 epic 到底在做什麼（一句話）

修掉健檢抓到的**三個會誤導你的錯**，並建一套**機器自動抓「假開關、空圖、沒人看的數字」的閘門**，讓這類病永久絕種。

## 六批任務與狀態

| 批 | 任務 | 白話 | 狀態 |
|---|---|---|---|
| **第1批** | 1.1 契約檔 | 前後端資料格式寫成一份「憲法檔」，誰違反機器就報錯 | ✅ **完成已 commit**（12 測試綠） |
| **第2批** | 2.0 凍結基準 | 改碼前先用真實 K 線把分位圖數值全部拍照存證（ETHUSDT/12h、14 特徵） | ✅ 完成（重跑兩次 byte 相同） |
| **第2批** | 2.1 修分位圖空白 | 後端把「盒子套盒子」攤平成前端讀得懂的形狀；**數值一個都不准變**（跟拍照比對） | ✅ **完成已 commit**——基準比對 exact 相同；出口矩陣（JSON/CSV/落地重讀）4 綠；前端圖測 3 綠；API 子路由 7 綠；全套 A/B 比對**零新增紅**（14 條既有紅與改動前逐字相同，已具名列管） |
| **第3批** | 3.1+3.2 修橫截面空殼 | 四類空資料改成明說「此模式不適用」；前端顯示對應文案；漏斗圖在橫截面模式收起來（它的資料格式在該模式對不上） | ✅ **完成已 commit**——後端 status 4 綠；前端 49 綠（新增契約文案卡＋四節分流＋漏斗 gate）；全套 A/B 零新增紅 |
| **第4批** | 4.2+4.1 修事件靜默騙人 | 你傳的事件時間戳從「收了就丟」改成真的生效；事件樣本不足退回全樣本時，頁面亮紅條說明 | ✅ **完成已 commit**——接線當天挖出**四個一直藏著的真 bug**（時間戳清單塞爆報告、epoch 整數欄恒零命中、兩條路徑的 fallback 丟參）；其中「fallback 該不該保留事件」觸發斷路器開委員會，三家一致裁定保留（我原本的傾向被推翻）；12 測試綠＋全套零新增紅 |
| **第5批** | 5.3 換手開關真語意 | 關掉換手開關就真的不算（現在關了照算）；Net IC 因此沒輸入時，顯式說「不可用：換手已停用」而不是靜默消失 | ✅ **完成已 commit**——4 測試綠（含預設仍開啟的回歸） |
| **第5批** | 5.1+5.2 接線閘門 | 機器自動查三件事：UI 開關必須真連後端／後端算的必須有人看／空白必須有原因。這次抓到的幽靈開關全部列管 | ✅ **完成已 commit**——掃描器上線（24 開關全查、11 條殘留具名列管、注入假開關必變紅）；首跑就抓到一條假陽性並修正，證明它真的在看 |
| **第6批** | 6.1–6.3 誠實契約 | 容量沒資料就標 unknown（測試釘死防回退）；文件明寫「切分現在只有 holdout」；兩個沒人讀的死設定移除（舊設定檔不會爆，會警告） | ✅ **完成已 commit**——12 測試綠；移除死設定連帶 config 指紋變更，基準重凍並機械證明 14 特徵數值逐位不變 |
| **第6批** | 6.4 缺口登記 | 六張未來票（DSR 防過擬合／IC-ML 橋／事件真套件／pooled IC／容量資料／規模上限）寫進 registry＋ROADMAP，白話檔加排程對照 | ✅ **完成已 commit**——registry 六票＋ROADMAP 六列＋白話排程節 |

## 品質關卡（每批都要過）

- 每批完工→測試全綠→獨立 commit（可單獨回退）
- 七條「故意改壞必須變紅」的防偽探針（M1–M7），驗收時逐條實跑
- 全部批次完工後→Codex＋Composer＋Grok 三家 code review→白話總結給你

## 流程軌跡（審查不是橡皮圖章的證據）

| 關卡 | 結果 |
|---|---|
| SPEC 三家對抗審 | 19 條意見（含 1 條三家同判的我方錯誤），全採納修訂 |
| SPEC 複驗＋蓋章 | 三家重跑自己反例 19/19 關閉，全數 APPROVED |
| TODO 三家對抗審 | 20 條意見（含 2 條行為級抓漏：紅標落點錯、呼叫鏈斷點），全採納修訂 |
| TODO 複驗＋蓋章 | 20/20 關閉，全數 APPROVED |
| B1 實作 | 12 新測試＋23 回歸全綠 |
| B2 基準比對 | 攤平前後數值 exact 相同（golden 6/6） |
