# GAP-3 事件型分析 — 討論文檔（活的；討論就改這份）

**這份是什麼**：你 8/19 說「先寫成一個文檔，然後討論就修改文檔，這樣不會被洗掉」。這就是那份，**獨立的一份**，就地改寫、不往下堆。
規則：討論中的每個結論都改進這裡（改動靠 git 留痕）；**問題訂清楚之前不問委員、不寫規格、不動程式**（你 8/19 定）。

> 讀法：§1 詞講白 → §2 你要做的事（照你兩輪回答）→ §3 陷阱與正確做法（3.1 是整票靈魂；3.4–3.8 是你第二輪問的）→ §4 多空 → §5 事件類型盤點 → §6 系統怎麼建 → §7 已定／待你確認 → §8 討論紀錄。想到什麼就問，我改文檔。

---

## §1 先把詞講白（整份只用這幾個詞）

| 詞 | 白話 | 你的例子 |
|---|---|---|
| **事件（t₀）** | 你在圖上指的那個時間點，一個事件一列 | 某根 12h K 線 |
| **決策時點** | 你**真正按下買／賣**的那一刻。系統只准用這一刻之前**已經收盤**的 K 線算特徵 | t₀ 那根 12h 的 **open** |
| **特徵** | 決策時點之前的指標（Feature Factory 算的） | t₀ 往前 100 根 1h／25 根 4h 的各種指標 |
| **標籤（答案欄）** | 這個事件的**結果**，1＝正例、0＝反例；可多附一個數字（後面實際漲幾 %） | 漲≥5% 且續漲 ⇒ 1 |
| **標籤規則** | 「怎麼判 1 或 0」那條規則。你在外部（/search＋CSV 篩）定好，平台只存規則摘要、不重算 | §2-4 |
| **反例** | 你要模型學會「跟正例分開」的樣本。**反例選錯，學到的 pattern 就是錯的** | §2-4 三種 |
| **去重／簇** | 相鄰幾根都觸發 ⇒ 算幾個事件？它們「後面一段」會重疊，不是獨立樣本 | §3.7 |
| **切分** | 拿一段時間學、另一段時間驗；學的跟驗的不能重疊、不能偷看；每個標的各自切 | 前 70% 時間學、後 30% 驗 |
| **不偷看未來（PIT）** | 任何特徵、任何「挑樣本的條件」，都不能用到決策時點之後才知道的事——**但「挑樣本」可以用未來，前提是驗證時要補回來**（§3.1） | — |

---

## §2 你要做的事（照你 8/19 兩輪回答）

1. **流程與現有初版**：`/search` 用條件搜出正反候選 → 匯出 CSV → 你手工細篩 → `/data-preparation` 上傳（欄位 `symbol, timestamp, Positive_case[, timeframe]`）→ 勾選要抓哪些時間框架（1h／4h／12h…），系統替**每個案例**抓 t₀ 前 lookback 根、後 forward 根、再加指標預熱（warmup）的 K 線存起來。
   我讀過這兩頁的前後端了：概念對（匯入 → 抓 K 線 → 訓練），**但欄位不夠**（沒有決策時點、沒有反例種類、沒有多空方向、沒有標籤規則摘要）、對齊用「時間戳剛好相等」、找不到就默默跳過——這些是要重做的地方（§6），不是概念錯。
2. **目標**：用 **t₀ 之前** 的 K 線指標（1h／4h，Feature Factory 算），**預測 t₀ 會不會漲**，在 t₀ **open** 買入（或賣空）。⇒ 決策時點＝t₀ open；事件本身（這根漲≥5%）是**未來的結果**。
3. **跨時間框架**：t₀ 標在 12h，特徵學 1h／4h。**可以，而且是對的**；鐵律只有一條：特徵只能用到「t₀ open 之前已經收盤」的 1h／4h bar（12h 的 open 在 00:00／12:00 UTC，剛好是 1h、4h 的邊界，對得整齊）。
4. **反例有好幾種，全標 0**：(a) 同樣漲≥5% 但後續不漲或下跌；(b) 只在震盪、close 上下 1%；(c) 跌 x%。⇒ 匯入時多一欄「**反例種類**」（a/b/c），理由見 §3.5。
5. **finlab 那篇**（「23 次暴跌日後 20 天平均 +3.9%、勝率 74%」「跌破 60 日線是負向事件」「崩跌區首根 +2% 出現時買進、中位報酬 4.5%」）＝事件**已經發生、看到了**，統計它之後 5／10／20／45 天的報酬分布。**你要研究這類**（8/19 確認）⇒ 列為第一張要出的表「事件後報酬表」（§6 ⑤-i），**不需要反例**。
6. **規模**：標的約 150–200 個、正反案例合計一兩萬個（§3.8）。
7. **出場點還不知道怎麼辦**（§3.6）；**相鄰幾根都符合／漲了盤整幾根又符合**不知道怎麼辦（§3.7）；**X 根 K 線是隨便喊的**（§3.4）。
8. **先把問題訂清楚再問委員**。✅ 本文檔就是在做這件事。

---

## §3 陷阱與正確做法

### 3.1 你的做法叫「依結果挑樣本」（case-control）——合法，但有一條配套（整票靈魂）

你是用 **t₀ 那根的結果**（漲≥5%／震盪／跌 x%）去挑樣本，再回頭看 t₀ 之前的特徵。統計上叫 **case-control**（先挑結果，再看原因；醫學先挑病人跟健康人、再比生活習慣）。**合法**，而且樣本效率高（正例稀少，全掃會淹在雜訊裡）。

天生限制：實盤時系統是在**每一根** K 線的 open 做判斷，面對的是**全部** bar；你挑的樣本只有「大漲／震盪／大跌」三種，**中間那一大片「小漲 1–3%、小跌 1–3%」不在樣本裡**。在你的樣本裡學出來的 pattern，套到那一大片 bar 上會怎樣——**樣本本身回答不了**。

**配套（硬的）**：
- **用你挑的正反例學，用「全部 K 線」驗。** 驗證期（沒拿來學的那段時間）裡，對**每一根** bar 的 open 都跑一次模型，看它說「會漲」的那些實際幾 % 真的漲了（精確率）、漏了多少真的大漲（召回）、照它下單持有到答案窗結束的報酬。這才是實盤會看到的數字。
- 報告明寫：**學習樣本的「勝率」不是實盤勝率**（它的正反比例是你用 /search 抽出來的）；實盤勝率看「全部 K 線驗證」那欄。
- 好處：反例挑不全沒那麼可怕——挑偏了，全部 K 線驗證會把它抓出來；不會騙到你。

### 3.2 三種「情境」（決策時點 vs 事件），平台契約真正要分的就是這個

| 情境 | 決策時點 | 事件對決策者而言 | 反例是誰 | 你的哪個目標 |
|---|---|---|---|---|
| **A／B 預測型** | 事件那根的 **open**（或更早） | **未來**（還沒發生） | 依結果挑的各類 bar（§2-4 的 a/b/c）＋**全部 K 線驗證** | 主目標：預測 t₀ 漲不漲、open 買 |
| **C 確認型** | 事件那根的 **close**（或下一根 open） | **已發生、看得到** | 同樣觸發但結果不好的那些（a 類）；或**不需反例**（事件後報酬表） | 收盤確認後問續不續漲；finlab 那種 |
| **兩段式** | 第一段 open、第二段 close | 先未來、後已發生 | 兩段各自的 | 你也要；§3.5 說明它跟「反例種類」的關係 |

- A 跟 B 只差答案欄：B＝t₀ 自己 open→close 漲≥5%；A＝t₀ 漲≥5% **而且**後面 24／36／48h 續漲（從 open 進場價起算、含 t₀ 自己的漲幅）。契約一樣。
- 決策時點必須**明寫在每個事件上**；寫錯（說 open 決策、特徵卻用到 t₀ 收盤）⇒ 機器擋。

### 3.3 其他陷阱（平台要擋的）
- 連續觸發 ⇒ 後面一段重疊、不獨立（§3.7）。
- 切分：每個標的各自按時間切、中間留「答案窗長度」的緩衝；禁用「第幾列」切。
- 多標的同一時刻一起大漲是同一件事，不是兩個獨立樣本（跨標的也要算重疊；§3.8）。
- t₀ 在資料末端、答案窗還沒走完的不能當樣本。
- 運氣：把答案欄打亂重跑應該什麼都看不到；找出的規則接 GAP-1 的 DSR/PBO。

### 3.4 「每個案例抓 t₀ 前 100 根 1h（4h 就 25 根）、只用這 100 根訓練」對嗎？（你問的第 2 點）

- **資料層面對、模型層面要改一個觀念**。「每案例一段 100 根」是**抓資料的單位**（`/data-preparation` 的 lookback＋warmup 就是這個），這沒問題；但**訓練用的不是那 100 根原始 K 線**，是 Feature Factory 在那段上算出來、**落在 t₀ open 那一刻的一排指標值**（每個指標一個數字：RSI、量比、均線距離、波動率…）。一個案例＝一列特徵＋一個答案。
- 100 根原始價格直接餵模型（100×欄位數的輸入）會嚴重過擬合——一兩萬個樣本撐不起那麼寬的輸入；那是序列模型的路，現階段不走。
- 「X 根」的理論答案：**X 由指標自己決定**——每個指標有自己的回看長度（均線 20 根、ATR 14 根…），再加 warmup；所以「抓幾根」＝「最長指標的回看＋warmup」，是資料需求，不是建模選擇。至於「用 20 根還是 100 根的指標比較準」——**那是 IC 分析該回答的**（把 20／50／100 根版本都當候選特徵丟進去，讓 IC＋FDR 挑），不用你猜。
- 1h 100 根跟 4h 25 根涵蓋同一段時間但粒度不同，**兩個都當特徵來源**沒問題，讓資料說話。

### 3.5 三種反例全標 0，不會讓 IC／ML 錯亂嗎？（你問的第 3 點）

- **ML（二元分類）**：原則上不會「錯亂」——模型學的是「正例 vs 其他」。但有一個**偷懶陷阱**：分開「大漲」跟「大跌」(c) 太容易了（動能指標一看就知道），模型會先學這個，整體分數很漂亮，**最難也最值錢的 (a)「同樣大漲卻不續漲」根本沒分開**。所以：(i) 報告**按反例種類分開算**分數；(ii) 這三種反例其實天然對應**兩段式**——(b)(c) 是第一段「會不會大漲」的反例，(a) 是第二段「大漲後會不會續漲」的反例；你的「多種反例」＝兩段式的合體，平台可以兩種都報。
- **IC（特徵 vs 後續報酬的相關）**：IC 不看 0/1、看「後面實際漲幾 %」那個數字，所以反例種類不會讓它錯亂；但**你抽了哪些 bar 進樣本會改變 IC 的意思**（它是「在這群被挑出來的 bar 裡」的相關）。處置同上：整體一份、按種類分開一份；加上「全部 K 線」那份才是完整的。
- 反例之間的**比例**是你在 /search 抽的，它會改變學習樣本的基率——又一個「全部 K 線驗證」不可少的理由。

### 3.6 出場點還不知道怎麼辦（你問的第 4 點）

- 現階段**不用決定**：答案窗本身就是一種出場（「持有到 24／36／48h 後的 close」＝時間出場）。研究階段用固定時間出場是誠實的、可比的。
- 「事件後報酬表」（§6 ⑤-i）會把 5／10／20／45 根之後的報酬分布都列出來——那張表本身就是在回答「什麼時候出比較好」；看完再決定。
- 進階版（停利／停損／時間三道牆，取先碰到的）叫 triple-barrier，標籤規則可以容納它，**但出場最佳化會碰到回測層**（成熟度地圖：回測是不完整層），**不在 GAP-3 第一版**；先記在殘留。

### 3.7 相鄰幾根都符合／漲了盤整幾根又符合，怎麼辦？（你問的第 6 點）

- **先分情境**：
  - **C（事件已發生）**：連續觸發就是「同一波」。預設＝**簇首**（相鄰 G 根內的觸發只算第一個；G 預設＝答案窗長度，48h＝4 根 12h，可調）；「漲了盤整幾根又符合」——間隔超過 G 就算新的一簇。finlab 的「崩跌區**首根** +2%」就是這個邏輯。想全留的話要降權（後面那幾根的答案窗跟前面重疊）。
  - **A／B（預測型、open 決策）**：「前幾根剛大漲過」是決策時已知的事實，它**本身就是一個合法特徵**（例如「過去 3 根內有無 ≥5%」），不必刪；但答案窗重疊仍要**降權**，否則顯著性虛高。
- 平台做法：一個參數（簇首／全留降權）＋簇間隔 G；報告一定寫 `原始事件數／去重後事件數／重疊比例`；而且**兩種設定都跑一次**——結論若在簇首和全留之間翻盤，代表結果靠的是重複計數，不可信。

### 3.8 規模：150–200 個標的、一兩萬個案例（你說的第 7 點）

- 對「一列特徵＋一個答案」的表格式 ML 來說，一兩萬個樣本＋幾十到上百個特徵是**夠用**的（前提是時間切分＋打亂答案自檢都做）。
- 但**平均每個標的只有 50–130 個案例**，單一標的撐不起統計 ⇒ 事件型**幾乎一定要把多個標的合在一起算**（統計學叫 pooled／panel）。這正是缺口票 #4「多標的合併估 IC」的內容——GAP-3 會逼著它一起做（至少做「每個標的各自時間切，但統計合併」）。
- 合併的代價：同一時刻 BTC／ETH／SOL 一起大漲是**一件事**，不是三個樣本；要按「時間簇」降權或分塊（§3.3），否則樣本數灌水。
- 資料量：200 標的 × 1h／4h × 全史算 Feature Factory 是重的，但 `/data-preparation` 只抓每案例前後一段，存得下；計算時間要實測（第一批做完會量）。

---

## §4 多空都要

- 事件多一個欄位 **方向（多／空）**。空方：「大陰線」＝跌≥5%、「續跌」＝後面再跌；特徵一樣；統計看方向。
- C 情境還有「反轉」玩法（大跌之後問會不會反彈做多）——只是答案欄定義不同，契約吃得下。
- **建議**多空分開建樣本、分開報。（待你確認 Q-S1）

---

## §5 事件類型盤點（跟 §3.2 的「情境」是兩個獨立維度）

| # | 類型 | 例子 | 哪裡來 | 第一版 |
|---|---|---|---|---|
| ① | 價量觸發 | 大陽／陰線、突破、量能暴增（**你的例子；finlab 的暴跌日也是**） | K 線算得出來（/search 挑、或你標） | ✅ |
| ② | 技術形態／指標事件 | 跌破 60 日線（finlab）、頭肩、均線排列 | K 線算得出來 | ✅（你標好匯入） |
| ③ | 波動／狀態切換 | 盤整轉趨勢、波動率爆發、「崩跌區」 | K 線算得出來 | ✅（你標好匯入） |
| ④ | 衍生品／微結構 | 資金費率極端、OI 激增、清算潮 | **要外部資料** | ❌ 登記待資料源 |
| ⑤ | 日曆／排程 | FOMC、法說會（finlab 的台積電法說後 5 天）、期權到期 | 時間表（事前就知道） | ✅（匯入時間點即可） |
| ⑥ | 新聞／事件 | 上幣、駭客、ETF | **要外部資料** | ❌ 登記待資料源 |
| ⑦ | 人工標定 | 你肉眼在圖上標的任何點 | 你 | ✅ |

- 每一類都可以套 A／B／C 任一情境；類型只是一個標籤欄，方便分群看結果。finlab 那種＝①／②／⑤ × C 情境 × 事件後報酬表。

---

## §6 系統要怎麼建（白話架構；哪些共用、哪些新寫）

```
/search 搜出候選 → CSV 你手篩 ─▶ ① 匯入契約（/data-preparation 升級：欄位＋單位＋正反例都在＋決策時點＋反例種類＋多空＋規則摘要）
                                  ② K 線準備（/data-preparation 的批次抓取：每案例 lookback＋forward＋warmup；多 TF）——概念保留
                                  ③ 對齊（每個事件：決策時點 → 特徵截止 → 進場 → 答案窗；逐事件寫收據，順序錯就擋；
                                     多 TF：12h 的 t₀ 配「最後一根已收盤」的 1h／4h）
                                  ④ 特徵（Feature Factory 在 ② 的資料上算，取決策時點那一列；§3.4）
                                  ⑤ 組樣本（去重／簇／降權；跨標的同時刻簇；§3.7／3.8）
                                  ⑥ 切分（每個標的按時間切＋緩衝；統計跨標的合併）
                                  ⑦ 統計（三張表分開報：
                                       (i) 事件後報酬表（finlab 那種；不需反例；多個 horizon）
                                       (ii) 正反例分不分得開（AUC 類；按反例種類分開看；兩段式各一份）
                                       (iii) 事件樣本裡特徵 vs 後續報酬的相關（條件 IC；接現有 IC 主線）
                                  ⑧ 找 pattern（多特徵：GBDT／規則抽取；只在學習段找）
                                  ⑨ 全部 K 線驗證（§3.1 配套）＋打亂答案自檢＋DSR/PBO
                                  ⑩ 報告／前端（第一版只占位殼；UAT 等整票）
```

**共用現有的**：Feature Factory（特徵＋多 TF）、IC 主線的事件遮罩／切分守衛／IC 函式／FDR、GAP-2 的倖存因子輸出契約（要擴欄）、GAP-1 的 DSR/PBO、`/search`（你的候選產生器，不進契約）、`/data-preparation` 的批次抓 K 線概念。
**新寫的**：匯入契約＋檢查器、對齊收據、去重／簇／降權、三張表裡的 (i)(ii)、全部 K 線驗證、報告節。
**不沿用的**：舊「案例→XGBoost」線的對齊（時間戳剛好相等、對不上默默跳過、特徵取 t₀ 收盤後的值）。ML 訓練那個殼不動（成熟度地圖：ML 是不完整層）。

**委員這輪（只分析了 C）的結論哪些還成立**：匯入契約要新寫、六個時間點逐事件寫收據、毫秒單位、去重預設簇首、每標的時間切、兩種統計分開、分四批——都成立。**要重議的**：反例規則（他們說「隨機 bar 當反例＝廢答案」只對 C 成立；A／B 是 case-control＋全部 K 線驗證）、決策時點要加「open 決策、事件在未來」、A／B 怎麼跟序列型 IC 主線共用、跨標的合併（#4）要不要併進來。**等 §7 待確認清空再問委員一輪。**

---

## §7 清單：已定／待你確認／委員會定

**已定（你裁）**
- 目標＝A／B／C／兩段式全要；任何類型；多空；finlab 那種事件後報酬表也要。正反例你自己在外部（/search＋CSV）標好一起匯入；流程走 /data-preparation 升級版。
- 先討論、不問委員、不寫規格、不動程式；討論改這份文檔。
- 決策時點＝t₀ open（A／B 主目標）；特徵＝t₀ 之前的 1h／4h 指標；反例三種（a 同漲不續／b 震盪／c 下跌）全標 0。
- 規模＝150–200 標的、一兩萬案例。

**我的判斷（寫在這裡讓你否決；不否決就當定案）**
- case-control 合法；**配套＝用全部 K 線驗證**（§3.1）。硬的。
- 跨 TF 可以；鐵律＝只用 t₀ open 前已收盤的 bar。
- 訓練用的是「t₀ 那一刻的一排指標值」，不是 100 根原始 K 線；X 根由指標決定、長短讓 IC 挑（§3.4）。
- 匯入欄位加「反例種類」；分數按種類分開報；三種反例＝兩段式的合體（§3.5）。
- 出場第一版＝時間出場（答案窗）；事件後報酬表回答「何時出」；triple-barrier／出場最佳化列殘留（§3.6）。
- 連續觸發：C 簇首、A 全留降權，兩種都跑看結論翻不翻（§3.7）。
- 跨標的合併（#4）併進 GAP-3 做最小版（§3.8）。

**待你確認**
- Q-S1：多空分開建樣本、分開報？
- Q-T1：第一版只收你標好的事件（/search＋/data-preparation 當你的工具），平台不內建「事件產生器」；「12h 漲≥5%」規則產生器列第二版？
- Q-L1：匯入時除了 1／0，要不要順便給「後面實際漲幾 %」？（建議給；條件 IC 更有力。`/search` 的 CSV 其實已經有 price_change 跟未來指標欄，直接留著就行）
- Q-V1：「全部 K 線驗證」第一版要出到哪：只出精確率／召回／簡單持有報酬（建議；不接回測引擎）。
- Q-H1：A 的答案窗示範用哪組（24／36／48h；「都≥3%」還是「任一」）？這是你的規則，只是確認示範用哪組。
- Q-D1：`/search` 與 `/data-preparation` 兩頁**保留當工具、只升級欄位與對齊**（不重寫 UI）——可以嗎？

**委員會定（技術，不用你裁）**
- 契約欄位細節、對齊收據格式、簇間隔與降權算法、切分緩衝、三張表的計算方式、跨標的合併的統計處理、防運氣檢定、跟 IC 主線共用哪些函式、分批邊界。

---

## §8 討論紀錄（新的寫上面；只記「改了什麼」）

- **2026-08-19 深夜（第 3 版）**：照你第二輪七點——讀 `/data-preparation` 前後端寫進 §2-1／§6 ①②；新增 §3.4（100 根 K 線＝資料單位非訓練單位；X 由指標決定、讓 IC 挑）、§3.5（三種反例＝兩段式合體；按種類分開報；IC 不受 0/1 影響但受抽樣影響）、§3.6（出場：先時間出場；事件後報酬表回答何時出；triple-barrier 列殘留）、§3.7（連續觸發：C 簇首／A 全留降權、兩種都跑）、§3.8（規模：夠用但必須跨標的合併＝#4 併入；同時刻簇）；finlab 列為你要研究的類型（§2-5）；§7 新增 Q-D1。
- **2026-08-19 深夜（第 2 版）**：照你六點回答整份改寫——/search 流程、決策時點＝t₀ open、跨 TF 可行、反例三種、finlab＝C 情境事件後報酬表、case-control 合法但必須全部 K 線驗證。
- **2026-08-19 晚（第 1 版）**：建檔。委員討論輪（Codex／Grok／我；Composer 被 Cursor 額度擋）收斂；你在彈窗揭露主要設想是 A／B 而非委員分析的 C。
