# SPEC／TODO 流程 — 完整問題、討論與改法

> **這份是給你逐條審閱的。** 其中 §四之二（TODO 改為五類落點）已由 TODO 優化票實施中：2026-09-23 起 `CLAUDE.md` 與分工手冊已加入新的 TODO 定義、TODO 範本已改寫；**其餘各條仍未動**。進度見 `白話說明/現在做到哪.md`。
>
> **由來**：你 2026-09-22 的四個問題（逐字收錄於 §一）。
> **依據**：三輪唯讀委員會（codex＋composer 兩家），全部收斂銷帳、零駁回。
> 收斂檔：`handoffs/reconcile/20260922-procopt-x-consult-r{1,2,3}/synth.md`。
> **機器紀錄**：`docs/PROCOPT_DECISION.md`（同內容的技術版，給我和委員用）。
>
> **怎麼 review**：每一條都有編號（例如 `4-2`、`5-1`）。
> 你可以直接回「4-2 不同意」「5-1 要改成 XXX」，我就只動那一條。
>
> 🔴 **標示約定**：
> 「**實查**」＝我實際從 repo／git log／檔案讀出來的，可重跑。
> 「**委員查**」＝委員實查、我抽驗過。
> 「**判斷**」＝推論，不是事實。
> 「**誠實邊界**」＝這個數字或結論的已知限制。

---

## 一、你問的四個問題與答案

### 1-0 你的原話（逐字）

> 1. 現在的問題就是量化主線的 SPEC 和 TODO 為何要這麼多輪，而且還無法收斂
> 2. 量化和軟體業界的模式和流程是什麼，跟我們的有什麼不一樣，我們要如何優化
> 3. 輪數那麼多是因為模式和流程錯了嗎，還是只是你主委問題發散和寫文檔錯誤？
> 4. 花那麼多時間在 SPEC 和 TODO，但實作還是一堆問題和 bug，這是正常的嗎？
>
> 解決問題和品質為最高優先，但也要考量成本和效率和時間，從 First principle 來開始，
> Claude.md 也可以改。可以一步一步來，沒有要你一次到位。

### 1-1 Q1：為何這麼多輪、是不是無法收斂

**「無法收斂」是假的。**

零實質 finding **確實達到過好幾次**（實查）：`gap2` 第 6 輪、`gap3` 第 6 輪、
`splitunify` 第 13 與第 21 輪、`docrot` 三輪內從 22 條降到 0。

達不到的不是「零」，**是「到了零之後把票停住」**——例如 `redispatch` 第 7 輪已經是 0，
還繼續跑。

🔴 所以問題不在「收斂不了」，在**沒有明確的停輪出口**。

**至於為什麼輪數多**，委員把它拆成**三個機制**（我原本只切成兩塊，委員說那個切面太粗、
把兩種不同的東西捆在一起）：

| 編號 | 機制 | 量化 9 票 | 治理 9 票 | 歸因 |
|---|---|---|---|---|
| **M1** | **相位躍升**（換審查文件時 finding 數跳上去） | 跳幅 **+12～+19** | 跳幅 **+3～+5** | **模式**（是程度差，不是有無） |
| **M2** | **同一份文件內小回升**（7→14 那種） | 7／9 票 | 5／9 票 | **我的編輯紀律** |
| **M3** | **到零卻不停住** | 有 | 少 | **模式**（無出口判定） |

> 🔴 **2026-09-22 更正（你提問後我實查改正；未經委員複驗）**
> M1 這一列先前把治理票記成「0 次」，**那是錯的**。
> `redispatch`／`verdictgate`／`docrot2` 都有 TODO 檔、都有兩相位，邊界也都有躍升。
> 正確說法是**跳幅差**。依你裁示（樣本數少、不在數字上糾結），只改結論、不再擴表。

**M1 的來源是 CLAUDE.md 的規定**：SPEC 對抗審完 → 凍結 → **自動開一輪 TODO 全審**；
而**輪次計數器綁在「票」上、不綁在「文件」上**（session 名是 `<epic>-x-review-r<N>`，
epic 是票名不是檔名），所以兩份不同文件的收斂曲線被畫成同一條。

相位邊界實查（同一套計數法，排除佔位符）：

| 票 | 邊界 | 前一輪 | 後一輪 | 跳幅 |
|---|---|---:|---:|---:|
| `gap1` | SPEC→TODO | 1 | 22 | **+21** |
| `gap2` | SPEC→TODO | **0** | 20 | **+20** |
| `gap3` | SPEC→TODO | **0** | 13 | **+13** |
| `verdictgate`（治理） | SPEC→TODO | 4 | 10 | **+6** |
| `redispatch`（治理） | TODO→SPEC | **0** | 3 → 4 → 3 → 4 | **+3** |

> 🔴 **計數法（2026-09-22 訂死，先前算錯過一次）**：一輪的 finding 數＝
> **該輪三份來源檔中 `## <家族>-R<n>-P<0-3>-<NN>` 標題數，扣除 `P3-00` 佔位符**。
> **不可**改數 synth 檔裡出現的 ID——synth 會引用前幾輪的 ID，那樣會灌水。
> 重跑指令：`cat handoffs/reconcile/<session>/sources/*.md | grep -E "^## [A-Z]+-R[0-9]+-P[0-3]-[0-9]+" | grep -vc "P3-00"`

🔴 **一個比跳幅更重要的事實**：`gap2` r6＝**0**、`gap3` r6＝**0**、`redispatch` r7＝**0**、
`verdictgate` r12＝**0**、`gap2` r11＝**0**、`gap3` r8＝**0**。
**歸零是常態。** 唯一沒歸零的是 EVENTSCAN，見 2-7。

🔴 **拆成「每份文件」算之後，量化與治理的差距比 §2-2 那組數字小很多**：
量化每份文件中位數約 **6**、治理約 **4.5**（§2-2 的「8 vs 4」是把兩份文件接成一條算的）。
**但拆開不會讓數字變好看**——見 2-6。

**M2 的來源是我**：只修被點名的地方、改一處又弄壞另一處。
EVENTSCAN 的收斂檔裡「只修被點名處」這句話出現 **12 次**（實查），
「主委被推翻／錯／漏」出現 **9 次**（實查）。

### 1-2 Q2：業界是怎麼做的，差在哪

**一句話：業界驗證行為，我們驗證文字。**

逐項對照（委員實查，具體到可查證的組織與慣例）：

| 尺度 | 本專案（實查） | 業界常態（委員查） |
|---|---|---|
| 單票審查輪數 | 中位數 5／平均 6.8／p75 8／最大 21；**44% 要 7 輪以上、28% 要 10 輪以上** | 設計審查 1–2 輪；RFC last-call 2–3 輪；量化期刊 referee 1–3 輪 |
| 寫 vs 審的時間比 | EVENTSCAN **寫 20 分鐘 : 審 12 小時 ＝ 36:1** | 同數量級（數小時到數日） |
| 何時開始寫碼 | 中／大鐵律：SPEC 凍結前不實作 | 設計「夠用」就開 spike／薄切片 |
| **停輪條件** | **近乎零 finding** | **P0/P1 閉合，小問題可後補** |

⇒ 🔴 **最大的單一差距是停輪條件。**

🔴 **這裡要更正我先前的說法**：我以前寫過「我們的中位數接近業界」——**那是錯的**，
因為我算的母體混進了 115 張批次 code review。用正確母體（18 張有 SPEC／TODO 的票）看，
中位數 5 是設計審查的 **2–5 倍**、RFC last-call 的約 **2 倍**。

**我們做對、業界也這樣做、不該動的**（兩家獨立同判）：
對抗式審查、實作者不自審、預先登記（pre-registration）、PIT／防洩漏／防過擬合的量化紀律。

### 1-3 Q3：是模式錯了，還是我發散、文檔寫錯

**兩個都是。** 而且委員不是憑感覺分的，是用兩個「否證觀測」把它釘死：

**否證 A — 治理票是不是也在震盪？**
如果治理票（同一個主委）完全不震盪，那「小回升歸主委」就不成立。
實查：**同一份文件內小回升，量化 7／9 票、治理 5／9 票，兩組都有。**
而相位躍升**兩組都有**，差在幅度（量化 +12～+19、治理 +3～+5，見 1-1 更正框）。
⇒ **躍升是模式（程度差），小回升是我**（因為兩種票型都會發生）。

**否證 B — 治理票是不是只是比較簡單？**
如果治理票本來就簡單，那「量化輪數高歸模式」就不成立。
委員用「第 1 輪的實質 findings 數」當複雜度代理：
**量化中位 16、治理中位 12，很接近。**
但治理能 3 輪內從 22 降到 0，而 EVENTSCAN 開場 21 條、**跑了 12 輪還剩 11 條**。
⇒ 「治理比較簡單」**解釋不了收斂速度的差距**。

**反事實估計**（委員修正了我的樂觀估計）：

| 情境 | 中位數輪次 |
|---|---|
| 現況 | 8 |
| 換一個編輯紀律完美的主委、模式照舊 | **6–7** |
| 只改模式、主委照舊 | 5–6，但 EVENTSCAN 還是會 12 輪 |

我原本估「完美主委能降到 5–6」，委員說偏樂觀——因為**完美的主委也消不掉相位串接**。

### 1-4 Q4：花那麼多時間寫文件，實作還是一堆 bug，正常嗎

委員逐條抽樣了 **18 個實作階段的缺陷群集**，分類如下：

| 類別 | 條數 | 比例 | 意義 |
|---|---:|---:|---|
| **SPEC 已經寫對、實作沒照做** | **8** | **≈40%** | **再審 10 輪 SPEC 也擋不住** |
| **SPEC 根本沒寫到** | **9** | **≈50%** | 凍結前想像不全，讀散文補不了 |
| 修補時引入的新問題 | 1 | ≈6% | — |

委員的結論（逐字）：

> **這直接否定「花更多 SPEC 輪換實作品質」。實作有洞正常；
> 用 12 小時零產物去買那筆品質，不正常。**

**最刺眼的實例**：`Math.max` 把 per-timeframe 的值塌成一個——
**SPEC 明文禁止、B3 批次修過、B5 批次又犯。**
⇒ 不是規格沒寫清楚，是**規格沒有變成一個會擋人的測試**。

🔴 **誠實邊界（委員自己標的）**：這是**非隨機抽樣**，偏向有長收斂檔的重輪，
所以**會高估嚴重的洞、低估那些一條 finding 都沒有的綠批**。
真實比例可能沒這麼極端，但「SPEC 寫對卻沒照做」這一類確實存在且不是個案。

---

## 二、支撐上面結論的數據（全部可重跑）

### 2-1 輪數分佈（母體＝18 張有 SPEC 或 TODO 的票）

**母體怎麼定的**：對每個 `*-x-review-r1` session 取 epic 名，
檢查 `docs/<EPIC>*SPEC*.md` 或 `docs/<EPIC>*TODO*.md` 是否存在；存在才計入。
133 張 review 票中**只有 18 張**有 SPEC／TODO，其餘 115 張是批次 code review。

| 票 | 輪數 | | 票 | 輪數 |
|---|---:|---|---|---:|
| splitunify | **21** | | evtwarmup | 4 |
| redispatch | **12** | | docrot2 | 4 |
| eventscan | **12** | | evtlabel | 3 |
| verdictgate | **11** | | docrot | 3 |
| gap2 | **11** | | plaindocs | 2 |
| gap3 | 8 | | govb1 | 2 |
| gap1 | 8 | | g7fix | 2 |
| search2event | 7 | | ffdstar | 2 |
| govb25 | 6 | | govb37 | 5 |

| 指標 | 值 |
|---|---|
| 票數 | 18 |
| 中位數 | **5** |
| 平均 | **6.8** |
| p75 | **8** |
| ≥7 輪 | **44%（8 票）** |
| ≥10 輪 | **28%（5 票）** |
| 最大 | **21** |

⇒ **你的印象「至少也都有 7、8 輪」跟實測相符**（平均 6.8、p75 是 8）。

### 2-2 只看量化主線 9 票（這是 Q1 真正問的）

splitunify 21、eventscan 12、gap2 11、gap1 9、gap3 8、search2event 7、
evtwarmup 5、evtlabel 3、ffdstar 2。

**中位數 8／平均 8.7／≥7 輪 6 票（67%）**；
對照治理 9 票：**中位數 4／平均 5.6／≥7 輪 22%**。

🔴 **這組數字是「一票 ＝ 一條曲線」算的，而一票含 SPEC 與 TODO 兩份文件。**
拆成每份文件之後差距會縮小（量化約 6、治理約 4.5），見 2-6。

### 2-3 findings 的實際組成（codex 逐條分類 EVENTSCAN 的 145 條）

| 軸 | 分類 | 條數 | 比例 |
|---|---|---:|---:|
| ① 能不能用實跑證偽 | 可由實跑證偽 | 67 | **46.2%** |
| | 只有讀才找得到 | 78 | 53.8% |
| ② 缺陷性質 | 規格不完備 | 97 | **66.9%** |
| | 副本不同步（同一件事寫在兩處，其中一處過時） | 48 | 33.1% |

**這組數字決定了每個改法的天花板**：
- 薄切片先行**最多**覆蓋 46.2%（是上限，不是預測值）
- 分類式停輪**最多**覆蓋 33.1%
- **兩者都不是主藥，而且處理的不是同一批東西**

### 2-4 SPEC 審完之後，實作階段還要審多少（實查）

| 票 | SPEC 階段 | 實作階段 |
|---|---|---|
| splitunify | 21 輪／122 條 | **91 輪／534 條** |
| gap3ux | 36 輪／472 條 | 41 輪／203 條 |
| gap2 | 11 輪／92 條 | 5 輪／32 條 |
| gap1 | 9 輪／91 條 | 4 輪／56 條 |

⇒ **SPEC 審到零，不代表實作就乾淨。** 這是 Q4 的量化背景。

### 2-5 寫 vs 審（EVENTSCAN，實查 git log）

寫 SPEC **20 分鐘**，審 **12 小時**，比例 **36:1**，期間產出的可執行程式 **0**。

🔴 **誠實邊界**：這是 EVENTSCAN 單票的事實。
**不能**拿它推論成「量化票普遍比治理票缺可執行物」——那個推論我提過兩次，
被委員用同一套啟發式掃過兩組全部輪次推翻：
**強可跑比例 44.8%（量化）vs 42.8%（治理），沒有顯著差異，且方向跟我的假設相反。**

### 2-6 拆成 SPEC／TODO 兩份文件之後（2026-09-22 你提問後補查）

| 票 | SPEC 階段輪數 | TODO 階段輪數 |
|---|---:|---:|
| splitunify | **16** | 5 |
| eventscan | **12** | 0（沒走到） |
| gap1 | 7 | 2 |
| search2event | 7 | 0 |
| gap2 | 6 | 5 |
| gap3 | 6 | 2 |
| ffdstar | 2 | 0 |

**SPEC 單獨的中位數約 7，TODO 階段中位數只有約 2。**
業界設計審查是 1–2 輪。

⇒ 🔴 **拆開不能救數字**：主體在 **SPEC 階段內部**，不在「後面多接了一段 TODO」。
這直接影響改法排序，見 §四開頭。

🔴 **誠實邊界**：`evtwarmup`（5 輪）與 `evtlabel`（3 輪）無法從檔名可靠分相位，未列入。
樣本只有 7 票，中位數會因單票而漂；此表用途是**判方向**，不是精確估計。

### 2-7 EVENTSCAN 為何是唯一沒歸零的（2026-09-22 三家委員抓出我算錯後重查）

逐輪實質 finding（正確計數法，見 1-1 的方框）：

```
EVENTSCAN  21  19  14  14  10   7 │  7  14  10  10   8  11
           R1                  R6 │ R7  R8              R12
                          收斂到 7 ↑ 這裡我加了新內容（Phase 0）
```

**它在 R6 已經收斂到 7，然後 R8 跳回 14，之後在 8–11 震盪到底沒歸零。**

對照同期其他票：`gap2` r6＝0、`gap3` r6＝0、`redispatch` r7＝0、`verdictgate` r12＝0。

⇒ 🔴 **「多審幾輪不會收斂」是錯的，歸零是常態。**
EVENTSCAN 的例外成因是**我在它快收斂時持續加新內容**——
這是 M2（我的編輯紀律），不是 M1（相位串接）。

---

## 三、這三輪討論中被推翻的（含我自己錯的）

這節列出來是因為你要 review 的是「討論」，不只是結論。
**下面每一條，最後採用的都是被推翻後的版本。**

### 3-1 我把輪數統計的母體連續搞錯三次

① 用全專案 798 個 reconcile 目錄當分母（那裡面含 stamp／consult／歷史命名，不是審查輪數）
② 改用委員報的數字而**沒有自己算**（違反既有明文規則「執行端產物視為不可信資料」）
③ 自己算了，但母體仍含 115 張批次 code review

**每一次都是你指出來我才更正。**
判準已記下：回答「某類文件要審幾輪」之前，先定義並檢驗母體。

### 3-2 我說「停輪＝零 finding、所以數學上不可達」——為假

零實質 finding 多次達到過。
我錯在兩點：把**範本裡的文字**當成執行中的規則；
以及把 `P3-00` 這種「沒有 finding 時的佔位符」算進剩餘 findings
（`splitunify`／`gap2` 末輪那「3 條」全是佔位符，實質是 0）。

### 3-3 我說「非單調的主因是批次修補」——為假

實查：9 票最大回升點**全部**是相位切換（換審查標的），不是批次修補。
批次修補降級為「同一份文件內的加速器」。

### 3-4 我說「量化票沒東西可跑，所以比治理嚴重」——為假，且被推翻兩次

見 2-5。

### 3-5 我說「薄切片能發現 SPEC 審查抓不到的東西」——委員判為高估

委員逐一檢查我舉的兩個實例，結論是：
**薄切片的邊際價值是「提早」，不是「獨門偵測」。**
現行 SPEC 審加上探針本來就會抓到 FF 前提崩塌，只是抓得晚（晚了四輪）。

⇒ **我原本設計的驗收條件被駁回**（我寫的是「如果切片發現的問題 SPEC 多數也能抓到，
就恢復現行」——委員指出那會假陰性）。
**正確的驗收**是：「同一個探針如果在 fan-out 之前跑，**省下幾輪建立在假前提上的文字**」。

### 3-6 我說「33.1% 的副本不同步對實作品質零貢獻」——為假

委員找出至少三條：如果不修，實作者會照錯的那一份做。
例如 Task 正文的指針指到第 020 列（演算法列），但真正的上限在 025 列——
照 Task 做就會鎖錯列。

⇒ **改法 4-3 的理由要改寫**：真正的理由是 Q4 的「沒照做 40%」，
**不是**「副本不同步只是衛生問題」。

但委員也同意可以再細分：**「雙權威」**（指針指錯列、計數詞跟表不一致）是規格不完備，
要擋凍結；**「回聲」**（票描述停在舊狀態、某個數字過時）才接近零貢獻，可以不擋。

### 3-7 Boehm 的「缺陷越晚修越貴」曲線——**自本文件移除**

我把它當成既有認知寫進了提案的理由裡。委員查不到可引用的出處與年份來確證或推翻。
⇒ **不以未查證的外部主張支撐內部決策**，該論點整條移除，不作為任何改法的理由。

### 3-8 我兩次犯的流程錯誤（你當場指正）

- **在四個問題都還沒有答案的時候就提三步方案** —— 你的原話「問題都還沒答案，你做什麼那三步？」
- **把你的提問當成裁定** —— 你的原話「你不能因為我說說什麼就說要排除，然後之後又說應該要做」
- **用「窮舉攻擊面」的問法派工** —— 那不是業界慣例，而且
  `templates/BRIEF_REVIEW_TEMPLATE.md` §6 早就明文警告「每輪擴攻擊面」會製造非單調重輪。
  **寫在範本裡我還是照犯——這本身證明「寫進文件」不是有效的防護**，
  也是改法 4-0 與 4-3 的核心動機。

### 3-9 r2 那一輪整輪方向錯誤（你指出來的）

r2 交出的是停輪判定、相位鎖、熔斷——**全部都是「控制審查迴圈」的手段，
預設了迴圈本身有價值，而那個預設從來沒被檢驗過。**
你的原話：「從 First principle 來看，R2 那只是掩蓋問題吧」。
⇒ r3 才去檢驗那個預設本身，也才生出改法 4-0。

---

## 四、改法（四步，逐條審閱）

> 排序依據＝**信號／成本比**，不是重要性。
> 每一條都標了「覆蓋率上限」——那是根據 §2-3 的 findings 組成算的，**是上限不是預測**。

> 🔴 **2026-09-22 排序更正（你問 1-1 之後實查改的）**
> 依 2-6：**輪數的主體在 SPEC 階段內部**（SPEC 中位數約 7），
> 而 TODO 階段中位數只有約 2。
>
> ⇒ **4-0 是唯一主藥**：它針對的正是 SPEC 階段內部那 7 輪。
> ⇒ **4-2 降級**：切斷相位串接省下的是 TODO 那 ~2 輪，不是主體；
> 而且它要動 `CLAUDE.md` 兩句（成本最高的一條）。
> 它仍值得做（治理票邊界也在爬，見 1-1 的 `redispatch`），但**不該排在 4-0 前面**。
> ⇒ 4-1、4-3 排序不變。

### 4-0 不變式的可執行 owner ＋ 只審 diff　【建議優先】

| 項目 | 內容 |
|---|---|
| **誰提的** | composer。**我和 r2 都沒想到這條。** |
| **現在怎樣** | 每一輪都重讀整份 SPEC（EVENTSCAN 是 1,400 行），每輪都可能生出新 finding |
| **改成怎樣** | R1–R2 做完設計對抗之後，**每一條還開著的 P0／P1 必須指向一個測試檔或 probe 檔**。指不出來就標成具名殘留，**不開 R3 全文重讀**。之後如果還有輪次，**只審 diff**。 |
| **為什麼有效** | 它砍的是「審查迴圈本身」，不是像 r2 那樣去控制迴圈。對應業界的 Jane Street Iron／Google CL 審查模型：審變更，不審全文 |
| **覆蓋率** | 🔴 **唯一打到主體的一條**：依 2-6，輪數集中在 SPEC 階段內部（中位數約 7），而 4-0 正是收束那一段；同時也治 M2（同文件回升） |
| **成本** | **零**。不用改 CLAUDE.md，不用新工具，**下一張派工單的「停輪條件」欄就能寫** |
| **風險** | 如果「指向測試檔」被寫得太寬鬆（例如指向一個空的 test stub），會變成形式主義。需要在 brief 寫死「該測試必須能被改壞而轉紅」 |
| **要改 CLAUDE.md？** | **不用** |

### 4-1 薄切片先行

| 項目 | 內容 |
|---|---|
| **現在怎樣** | SPEC 凍結前不寫任何可執行的東西 |
| **改成怎樣** | fan-out 派委員之前，先跑一次真實資料上的探針，產出實跑收據（FACT-RECEIPT） |
| **範圍限定**（委員加的） | **只對** runtime／資料／PIT／狀態可行性、跨域 schema、真實 manifest identity。**純治理文字不強迫造假的 runtime** |
| **驗收條件** | 🔴 **不是**「切片能不能抓到 SPEC 抓不到的東西」（見 3-5），**是**「同一個探針在 fan-out 前跑，省下幾輪建立在假前提上的文字」 |
| **覆蓋率上限** | **46.2%**（可實跑證偽的那一半），且那是上限 |
| **風險** | 探針本身可能寫錯，變成新的假前提來源。需要探針的輸出是原始數值，不是判斷句 |
| **要改 CLAUDE.md？** | **單票試辦不用**；第二張票起要改 |

### 4-2 切斷 SPEC→TODO 相位串接

| 項目 | 內容 |
|---|---|
| **現在怎樣** | CLAUDE.md 中／大鐵律：SPEC 對抗審 → 凍結 → **自動**開 TODO 對抗審 |
| **改成怎樣** | TODO **照寫**、機器 coverage check **照跑**；但對抗審改成「**只審 delta／只審一輪**」。要開全文對抗審，**要你點頭** |
| **為什麼有效** | 切掉 M1（相位躍升）。🔴 **但它不是主體**：依 2-6，TODO 階段中位數只有約 2 輪，切掉省的就是那 ~2 輪 |
| **覆蓋率** | 針對 M1，量化跳幅 +12～+19、治理 +3～+5（治理票也有，只是小） |
| **風險（委員警告）** | 🔴 如果沒有範圍 hash／晉級閘／殘留帳，會退化成「**先寫 code 再補規格**」 |
| **要改 CLAUDE.md？** | **要**（見 §五） |

### 4-3 驗收條件從散文移到測試檔

| 項目 | 內容 |
|---|---|
| **現在怎樣** | 驗收條件寫在 SPEC／TODO 的散文裡 |
| **改成怎樣** | 每一條驗收對應一個 test／probe／receipt。不可執行的要明寫 `NOT_EXECUTABLE` ＋ 原因 ＋ 負責人 ＋ 到期日 |
| **為什麼有效** | 直接針對 Q4 的「SPEC 寫對但實作沒照做 40%」。`Math.max` 那個例子就是規格寫了、沒人擋 |
| **理由已改寫** | 🔴 **不是**「副本不同步只是衛生問題」（那條被推翻，見 3-6），**是** Q4 的 40% |
| **風險（委員警告）** | 🔴 如果不同時要求「**觸發該分支的真實組合**」，只是把假綠從 markdown 搬到 pytest。`splitunify-b11` 已經證明測試可以建在「剔除＝0」上，讓全稱斷言恆真 |
| **要改 CLAUDE.md？** | **要**（可以只對新票生效，不回溯） |

### 4-4 兩家共同的風險警告（我原提案沒寫，補上）

1. **4-3 的假綠搬家風險**（見上）。
2. **4-2 的「先寫 code 再補規格」風險**（見上）。
3. 🔴 **高風險數值契約（命中 CLAUDE.md 的 a–d 四條）仍須完整 SPEC／TODO／adversarial 才能凍結，
   不得一併放寬。** 也就是說：這四步改法**不適用**於 Feature Factory／IC Gatekeeper／回測引擎
   這類會影響數值正確性的東西。

---

## 四之二、TODO 到底還要不要寫（2026-09-22 你問，三家委員已答）

**你的原話**：「TODO 你寫成中文散文我也看不懂也不會去看，這完全是你跟委員間的產物…
甚至你們自己能懂能精確了解執行的語言都可以」。

**三家一致的答案：散文 TODO 作為「規範權威」廢止。** 內容有必要，那個形式沒必要。

### 憑什麼這樣說（實測，不是主張）

GAP2 那個 Task 的 18 行散文，最後長成 `test_marginal_ic.py`（617 行、35 個測試）
＋ `marginal_ic.py`（558 行）。逐條對照：

| 散文寫的 | 最後真的長出來的 | 關係 |
|---|---|---|
| 驗證 O1a／O1b／O2／O3／O5／O6／O7／O9 | `test_o1a_residual_degenerate`、`test_o2_orthogonal_new_info`… | **名稱幾乎逐字一對一** |
| 邊界 ①–⑧ | `test_nan_rows_filtered…`、`test_constant_factor_is_degenerate…`、`test_budget_…` | **一對一** |
| 輸入／輸出 | 函式簽章與 frozen dataclass | 同構 |
| 實作要點 1–7 | 函式本體 | 同構 |

⇒ **那份散文是一份本來就非寫不可的檔案的手寫草稿。** 我們對草稿審了 5 輪，然後才寫真的。

### 內容要落到哪（三家收斂＝五類，不是我提的三類）

| # | 落點 | 放什麼 |
|---|---|---|
| 1 | 生產模組（先 stub 後實作） | 簽章、型別、演算法步驟 |
| 2 | `tests/**`、`scripts/**` | 驗證、邊界、看得出來的「不可做」、mutation |
| 3 | 契約 JSON | 鍵集、枚舉、錯誤碼字面 |
| 4 | **機讀批次卡**（封閉欄位） | 誰依賴誰、動哪些檔、既有 caller、跨批 gate 命令、檔案白名單、生命周期 |
| 5 | 實跑收據 | 真實命令、輸入、rc、誠實邊界 |

我原本提「三類」，**三家各自獨立判為不封閉**——漏掉既有 caller、批次拓撲、gate 命令這些。
grok 舉的例子很具體：GAP2 那行「既有 caller：無（B4 接）」刪掉，**實作會在接線時機上做錯**。

我原本還想另立一份「薄決策檔」，**codex 否掉了**——決策回 SPEC 既有節、執行欄位進機讀檔，
**不要第三份會漂的散文檔**。少一份比我的版本好。

### 🔴 三家把我另外三件事也推翻了

1. **不准宣稱「輪數會降」**——輪數主體在 §A（前提詮釋），這個方向改的是 §P。
   正當性只能是「消除翻譯損耗＋消除副本漂移」。
2. **審查成本會上升**——審程式要建環境、實跑、跑 mutation。**總成本降不降沒量過。**
3. 🔴 **我提的「SPEC 審和 TODO 審合併成一輪」被駁回**——
   §A 是輪數主體，合併後它就**沒有審查槽**了。而且我的提案自己還留著兩份副本。

### 🔴 一個硬前置條件（沒做到就不准採納）

防假綠的 `mutation_probe_check.sh` 對 `tests/momentum/` 覆蓋率是 **0**，而且是 opt-in
（沒寫探針的檔根本不會被選中），`--fast` 和 `--no-probe` 都不跑它。

⇒ **驗收全搬進測試檔，但防假綠的東西不覆蓋那些檔＝假綠也跟著搬家。**
三家一致要求：**先回答「擴覆蓋的成本」才能採納。**

### 採納後仍然不會解決的（三家共同列）

1. §A「把 14 欄的觀測寫成 418,719 欄的結論」——這個方向完全不碰。
2. 假綠覆蓋（見上）。
3. 委員會一起漏同一件事（`splitunify-b11`）。
4. 每輪審查成本上升。

---

## 五、CLAUDE.md 逐字改寫（兩家版本並列，供你挑）

> **本輪沒有改任何一個字。** 下面是兩家各自給的候選文字。
> 4-2 與 4-3 如果你要採納，就需要動其中幾句。

### 5-1 `CLAUDE.md:30`（任務分派表「中／管線」格）

**現行逐字**：

> 完整管線：**SPEC + TODO + adversarial**，**不得跳步/不跳**（D-1）

**composer 版**：

> 完整管線：**SPEC 對抗至本文件規格不完備之 P0/P1 關閉**；TODO 是工作拆解，**不**自動開與 SPEC 同規格的第二段全文對抗（另開須使用者 go）。凍結前允許真實資料薄切片。D-1 改指審查強度（對抗＋實作者不自審＋家數＝ORCH §1），不指相位串接。

**codex 版**：

> 命中共享／數值／跨域 contract 或 PIT/data identity 風險者，必須 SPEC + TODO + adversarial；其餘中型任務先交付最小 contract block + executable feasibility probe，再依 probe 結果決定是否展開完整文件。任何省步須列 scope、exit criterion、residual，並讓使用者可否決。

**風險**：D-1 是 2026-06-05 你定死的「不得跳步」。改這句等於動鎖定條款。
codex 版的風險是「風險分類沒有機器檢查就可能被濫用成省略高風險審查」。
composer 版的風險是「執行端可能把『薄切片』讀成『可以不寫 SPEC』」。

**我的判斷**：composer 版較窄、較安全（它只切斷串接，沒有引入新的風險分類判斷）。

### 5-2 `CLAUDE.md:38`（中／大鐵律①）

**現行逐字**：

> ① 完整管線不得跳步（SPEC/TODO/adversarial 都要）

**composer 版**：

> ① 審查強度不得跳步（對抗＋實作者不自審；家數與家族＝ORCH §1）。SPEC 對抗與 TODO 對抗不串成同一曲線；TODO 全文對抗須使用者 go。copy-drift／P2／P3 不擋本文件凍結。

**codex 版**：

> **中/大鐵律**：不得跳過與風險相稱的 evidence gate；高風險仍必須 SPEC/TODO/adversarial；其他任務可由 decision block + executable acceptance artifact 替代全量展開，但必須留下 scope、exit criterion、residual、quorum 與 audit receipt。

🔴 **委員特別註明**：5-1 和 5-2 **必須一起改**。
只改表不改鐵律的話，執行端還是會按鐵律自動開 TODO 審——`gap2` r6 就是這樣發生的。

### 5-3 `docs/MULTI_AGENT_ORCHESTRATION.md` 中型列（約 L192）

**現行逐字**：

> 中 | 完整管線同大型（SPEC+TODO+至少一家不同模型 adversarial;2026-06-05 使用者定死不得跳步,D-1 維持）

**建議**：與 5-1 同義改寫，保留「D-1 維持」但括注「強度、非串接」。
**理由**：ORCH 不改的話，CLAUDE.md 改了仍有第二個權威來源，會互相矛盾。

### 5-4 新增一句（配合 4-3，不刪舊句）

**composer 建議新增**：

> 驗收條件之權威落點＝測試檔或 `ASSERT` 行；SPEC／TODO 散文只得 pointer。空集合上的全稱斷言不算驗收。

**風險**：與 ORCH 既有的「驗收段裡每次提到受管命令必須在 ASSERT 行內」同向，衝突小。
舊票不回溯就沒有立即成本。

### 5-5 codex 另外提的兩句（我傾向不採，但列給你看）

- `CLAUDE.md:46`「判不出大小：先當中」→ 改成「先只允許可逆的最小 feasibility probe」
- `CLAUDE.md:50`「宏觀斷路器：≤2 輪失敗開委員會」→ 改成「同一假設／同一 scope ≤2 輪失敗，
  停止同形態重跑，回到最近一個可執行 contract block 重新界定」

**我不建議現在動的理由（判斷，非事實）**：這兩句跟本次四問診斷出的三個機制沒有直接關係，
而且「先當中」是 fail-safe 設計，放寬它會引入新的判斷點。

### 5-6 明確不動的條文（兩家一致）

實作者不自審、家數不在 CLAUDE.md 寫死（唯一來源是 ORCH §1）、
產出端覆蓋鐵律、高風險 (a)–(d)、facts-first。

---

## 六、不得動的（兩家獨立同判）

1. **對抗式審查**
2. **實作者不自審**（高風險 contract；純文字尾掃可降級由我自查）
3. **兩家獨立產出**（現行家數已經是下限）
4. **PIT／防洩漏／防過擬合的量化紀律**
5. **不用比例式判準停輪**（你 2026-09-11 定死，全專案適用）

🔴 **負邊際在「每輪擴攻擊面」與「零 finding 才凍結」，不在家數。**
也就是說：**減少委員家數不會改善任何一個機制**，只會降低品質。

---

## 七、等你裁決的事（逐條）

| 編號 | 要你決定的 | 我的建議 | 影響 |
|---|---|---|---|
| **D1** | 採不採 4-0（可執行 owner ＋ 只審 diff） | **採，且優先**。零成本、不改憲法、下一張票就能試；🔴 依 2-6 它是**唯一打到主體**的一條 | 收束 SPEC 階段內部那 ~7 輪 |
| **D2** | 採不採 4-2（切斷 SPEC→TODO 串接） | **採，但排在 D1 之後**。🔴 更正：它省的是 TODO 那 ~2 輪，不是主體，卻要動憲法兩句 | **要改 CLAUDE.md 5-1＋5-2（必須一起）** |
| **D3** | 採不採 4-3（驗收移到測試檔） | **採，但限新票**，且必須配「觸發該分支的真實組合」要求 | **要改 CLAUDE.md（5-4）** |
| **D4** | 採不採 4-1（薄切片先行） | **可延後**。覆蓋率上限 46.2%，且委員判定它的價值是「提早」不是「獨門」 | 單票試辦不用改憲法 |
| **D5** | 5-1／5-2 用 composer 版還是 codex 版 | **composer 版**（較窄、不引入新的風險分類判斷） | — |
| **D6** | 5-5 那兩句（判不出大小、宏觀斷路器）要不要一起改 | **不改**。跟這次診斷的三個機制無關 | — |
| **D7** | 改完之後要不要補機械閘（4-2 的範圍 hash／晉級閘／殘留帳） | 🔴 **這會是新建治理工具**。你 2026-09-15 裁定過治理工具的擴建限制，需要你明示 | 不補的話 4-2 有「先寫 code 再補規格」風險 |
| **D8** | EVENTSCAN 怎麼辦 | **已裁定：維持暫停**，流程改完再回來 | 已記入 HANDOFF |

---

## 八、這份怎麼維護

- **這份是一次性的審閱文件**，不是活文件。你逐條裁決完、改動實施完之後，
  結論搬進 `CLAUDE.md` 與 `docs/PROCOPT_DECISION.md`，這份移到 `Archived/`。
- **技術版在** `docs/PROCOPT_DECISION.md`（含三輪的完整推翻紀錄，我和委員用）。
- **原始收斂檔在** `handoffs/reconcile/20260922-procopt-x-consult-r{1,2,3}/synth.md`。
- **不要在這份往上疊進度**。進度在 `白話說明/現在做到哪.md`。
