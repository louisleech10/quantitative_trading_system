# GAP-3 事件型分析 — 結論

> **這份回答三個問題：這票做了什麼、現在什麼狀態、還剩什麼。**
>
> 🔴 **只寫結論，不寫過程。** 沒有「第 N 輪」「第 N 批」的紀錄——那些在 git log 與
> `handoffs/reconcile/` 的收斂檔裡，你不需要看。
>
> 取代原本的 `GAP-3施工進度.md`（2,407 行，含 29 輪審查流水帳）。

---

## 這票做了什麼

**讓你可以把「外部來的正反例事件」匯進系統，做 PIT 對齊，然後算條件 IC 與事件後報酬。**

白話版：你有一批「某某時點發生了某事」的清單，系統幫你：

1. **收進來**（檢查格式、答案欄、方向一致、不重複）
2. **對齊時間**（確保算特徵時不會偷看到未來）
3. **算報酬與 IC**（事件之後 N 根漲跌多少、特徵能不能排序它）
4. **產隨機對照組**（跟「不挑時機」比）

## 現在什麼狀態

**42 個 Task 全部做完，五個段落（D0–D5）全部交付並各過審碼閉合。**

| 交付面 | 狀態 |
|---|---|
| 匯入契約與驗證 | ✅ 完成 |
| PIT 對齊與 look-ahead 宣告 | ✅ 完成 |
| 條件 IC／事件後報酬表 | ✅ 完成 |
| 隨機對照組 | ✅ 完成 |
| 前端五維度（進場語意／報酬算法／情境／對照類型／決策位移） | ✅ 解灰完成，只剩兩對幾何上零窗的組合依契約拒收 |
| **你的 UAT 實機驗收** | 🔴 **未做**——排在最後 |

## 還剩什麼

**一條真缺陷**、**20 條殘留**，逐條在 [還沒做的事.md](還沒做的事.md)。
你在畫面上看得到的只有 5 條，其中最重要的：

> **IC 頁「報酬量法」送出的 `h` 固定是 1**，不會跟著你宣告的深度變
> （「依深度預設」只在你完全不碰那個面板時成立）。卡在前端要先能預覽後端解析結果。

## 這票期間查出的三個真問題（已修）

1. **選「開盤價」卻拿到上一根收盤價**——連續 K 線下這兩個時間戳差一根，數字合法所以**永遠不會報錯**。
2. **事件模式跑 IC 會整個掛掉**——清舊紅測試時查出來的。
3. **WS 主路徑不回填揭露欄**——三家審查獨立命中同一條。

## 技術文件在哪

規格 `docs/GAP3_EVENT_SPEC.md`、清單 `docs/GAP3_EVENT_TODO.md`（皆已凍結）；
延伸檔 `docs/GAP3_EVENT_UX_SPEC.D-001.md`／`.D-002.md`；
缺口登記 `docs/IC_QUANT_GAP_REGISTRY.md` #3。
