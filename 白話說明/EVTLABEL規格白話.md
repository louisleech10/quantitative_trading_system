# EVTLABEL 規格白話（給你否決用）

> 規格書：`docs/EVTLABEL_SPEC.md`（v4，`24e011dc`）；施工清單：`docs/EVTLABEL_TODO.md`。
> 三家委員三輪找碴（R1 27 條、R2 16 條、R3 11 條）全部採納，戳記輪進行中。
> **你只要看這份。** 看完回我「可以動工」或「哪裡不對」。

---

## 🔴 頭條：你的主目標做到哪、有沒有被延後

你的原話：「我在外面標好正反例（標的＋t₀＋0/1 標籤）匯入，平台找出 t₀ 之前哪些特徵能把正反例分開，再把這些特徵餵 ML。」

| 你要的 | 這票做不做 | 做到哪 |
|---|---|---|
| 匯入標好的 0/1 | ✅ 已經能匯入（之前就存起來了，只是 IC 沒去讀） | 第 3 段接線 |
| 找出 t₀ 前能分開正反例的特徵 | ✅ **第 3 段主體**：IC 直接對你的 0/1 算 | 完整 |
| 把這些特徵餵 ML | ⚠️ **只做到「交出一份 ML 能吃的倖存者檔」**，並用測試證明既有的接收端真的吃得下。**ML 訓練本身不在這票**（8/17 你定的成熟度地圖：ML 層還不完整，禁改殼）。 | 邊界 |

🔴 **要你決定的只有一件**：「餵 ML」這一半，這票交付到「倖存者檔 ＋ 接收端契約測試」為止，ML 訓練另開票。**接受嗎？** 不接受就說，我改規格。
（三家委員對此的判定：不算偷換主目標，但必須頭條告訴你——我照做。）

**沒有任何一段被延後。** 三段順序照你定的：揭露 → purge 換算 → 匯入標籤模式。

---

## 三段各做什麼（一段一句）

### 第 1 段（小）：報告和畫面老實說用了什麼 label 規則
- 報告多一塊「本次 label 怎麼算」：h 幾根、k 幾根、進場價、報酬公式，並明寫 **h 的單位＝事件週期的根數**（你那批：h=1 ＝ 12h 一根 ＝ 1h 特徵的第 12 根）。
- 同一塊也寫「你的 0/1 標籤：有（正 136／反 29）；本次有沒有用到」。
- IC 頁事件 h／k 輸入框旁邊直接標單位。

### 第 2 段（中）：purge 改成真的照 label 視窗算
- 現況：purge 寫 5（是主線的數字，跟事件無關），label 視窗 12 根被塞在 embargo 裡，靠 embargo 144 才沒漏。**不是洩漏**，是標籤貼錯位置。
- 改後：purge ＝ max(主線 5, label 視窗 12) ＝ 12；embargo ＝ max(設定值, 批次宣告的往後看深度 144) ＝ 144；總隔離 149 → 156（更保守）。隔離區那段文字改成講真話。
- 改法用**顯式參數**傳，不走 config 覆蓋（委員實測：config 對不認識的鍵會**默默吞掉**，走那條路等於沒改）。

### 第 3 段（大）：匯入標籤模式
1. **怎麼算「分得開」**：對每個特徵，看正例組的特徵值是否整體高於／低於反例組。用的是 Mann-Whitney 檢定；報 AUC（0.5＝分不開，1＝完美分開）和 rank-biserial（＝2×AUC−1，正負表示方向）。
2. **顯著性**：Mann-Whitney 的 p 值 → BH-FDR 校正（跟現在一樣）。**效應量門檻改看絕對值**（委員 R1 抓到：原本會把「反向但很強」的特徵殺掉）。
3. **防運氣**（兩道）：
   - 每個倖存特徵再用「打亂標籤重算」驗一次（block 置換，會考慮事件時間上的重疊）；打亂後也一樣顯著的，**不交付**。
   - 整批做 50 次打亂，看隨機能篩出幾個；你的真倖存數若沒贏過隨機的 95 分位，整批標「負對照失敗」、**不交倖存者檔**（R2 委員算過：這種誤殺率約 5%，可接受並已預註冊）。
4. **什麼時候自動用 0/1**：切分後，驗證段（test）正反各 ≥ 10 筆就自動用；不夠就退回報酬規則並在畫面上明講原因。你也可以手動指定；手動指定但不夠時，**上傳當下就 422**，不用等 11 分鐘預處理。
5. **報酬版 IC 留第二欄**，報告寫明主統計是哪個。
6. **倖存者檔**帶 `sample_scope=event`、標籤來源＝你的匯入批（批 ID、正反數）。
7. **防「驗了一份、算另一份」**：驗證過的 0/1 向量凍結成不可變物件，算統計前再逐筆對證（這是上一票 EVTALIGN 抓到過的病，這次事先堵）。

---

## 委員三輪抓到、我改掉的最重要五件
1. 效應量門檻看有號值 → **改絕對值＋獨立門檻**（不再共用 IC 的 0.02）。
2. 自動判定看整批正反數，但統計在驗證段算 → **改在切分後看驗證段**。
3. 負對照「打亂後有 1 個就整批殺」→ 委員算出 39k 特徵下這會常態誤殺 → **改 50 次打亂取 95 分位**。
4. 驗證過的向量與實際算的向量沒綁死 → **凍結＋逐筆對證＋列序守衛**。
5. purge 改法走 config 覆蓋會被默默吞掉 → **改顯式參數＋入口守衛**。

---

## 殘留（這票不做，每條有理由）
| 項目 | 為何現在不做 | 何時做 |
|---|---|---|
| R-1 事件序列的 HAC／依賴感知解析 p | needs-research：事件不等距，估計式未定義；先用 block 置換擋 | 委員會定出估計式 |
| R-2 跨 symbol 合併 | blocked-by：Pooled/Panel IC 票 | 該票開 |
| R-3 triple-barrier 出場 | user-ruling 8/19 | 回測層成熟 |
| **R-4 ML 訓練殼吃倖存者檔** | blocked-by：成熟度地圖禁改 ML 殼 | ML 殼契約票 |
| R-5 h/k 掃描 × 0/1 | user-ruling 9/9：0/1 不隨 h/k 變 | 你提需求 |
| R-6 與 GAP-3 B1.4 合流 | blocked-by：兩者切分模型不同 | 切分統一票 |
| R-7 label 視窗太長時 block 不足 10 → 不交倖存者 | needs-research：這是保守，不是 bug；畫面會明講 | 同 R-1 |

---

## 附：報酬版 IC vs 標籤版 IC——用法與差異（你 9/10 要的；同一段文字會放進 IC 頁的說明框）

| | 全域 IC（現有） | 事件型・報酬版（現有） | 事件型・標籤版（第 3 段新增） |
|---|---|---|---|
| 什麼時候用 | 沒有特定事件，想看「哪些特徵一般來說預測未來報酬」 | 有事件批次，想看「在這些事件當下，哪些特徵預測**之後的漲跌幅**」 | 有事件批次**且你已標 0/1**，想看「哪些特徵能把**正例和反例分開**」 |
| 算哪些列 | 每一根 K 線 | 只有事件 t₀ 那些列 | 只有事件 t₀ 那些列 |
| 答案是什麼 | 平台算的往後 N 根報酬（N＝主線 horizon） | 你規則算的報酬（h 根事件週期、進場價、算法） | 你 CSV 的 0／1 |
| 統計量 | Spearman 相關（IC） | Spearman 相關（IC） | AUC／rank-biserial（＝2×AUC−1，正負＝方向） |
| 顯著性 | HAC t 檢定 → FDR | 同左（事件少時用整體 IC） | Mann-Whitney → FDR，再用打亂標籤驗證 |
| 通過門檻 | IC 絕對值、ICIR、p | IC 絕對值、p（ICIR 只診斷） | rank-biserial 絕對值 ≥0.10、q ≤0.05、打亂驗證通過 |
| 報告裡看哪 | summary 表 | summary 表 | summary 表**前面幾欄**（AUC／rank-biserial／p／q／正反數）；報酬版 IC 退到後面當「第二欄」 |
| 怎麼選 | 分析模式＝全域 | 分析模式＝事件，label 模式＝報酬規則 | 分析模式＝事件，label 模式＝匯入標籤（或「自動」且驗證段正反各 ≥10） |
| 一句話 | 「特徵值高，之後通常漲嗎？」 | 「事件當下特徵值高，之後通常漲嗎？」 | 「正例的特徵值和反例的特徵值，分得開嗎？」 |

什麼時候該用標籤版而不是報酬版：你的正反例**不是**單純用「之後漲幾 %」定的（例如你手工挑、或用型態／量能判斷），這時報酬版算的是別的東西，標籤版才是你要的。若你的 0/1 就是「漲 ≥2%」這種閾值規則，兩版會高度一致，標籤版多的是「分得開」這個直觀讀法。

## 附：全域 IC 的 horizon 是怎麼算、怎麼呈現（你 9/10 問的）

- 你在 IC 頁勾的 horizon（例 1,2,3,5,8,13,21）**每一個都會算**，但放在兩個地方：
  1. **summary 表**（排名、門檻、倖存者）只用**一條**「主線 horizon」的答案：清單有 5 就用 5，沒有就用清單第一個。這條決定誰倖存。
  2. **「IC 衰減」圖**（每個特徵一條線）：橫軸＝你勾的每個 horizon，縱軸＝該 horizon 的 IC。這是診斷，不影響倖存者。
- 不同 horizon 的 IC **本來就不一樣**（同一特徵可能對 1 根很準、對 21 根沒用），衰減圖就是給你看這件事。
- 現況的坑：主線 horizon 是「5 或第一個」這條規則畫面沒講——這就是你裁定另開的 GLOBALH 小票：讓你**指定**主線 horizon，並在報告寫明「倖存者是用 horizon＝X 選的」。

## 附：殘留逐條白話＋舉例（你 9/10 要的）

**R-1 事件序列的「依賴」問題（needs-research）**
- 白話：顯著性檢定（p 值）假設每個事件是獨立的一票。但如果事件 A 在 1/1 00:00、事件 B 在 1/1 12:00，而你的答案窗是 3 根 12h（36 小時），A 和 B 的答案窗重疊 24 小時——它們看的是同一段未來，等於一票投了兩次。獨立假設高估了樣本數，p 值會偏小（更容易誤判「顯著」）。
- 舉例：165 個事件裡有 31 對是相鄰兩根 12h。h=1 時窗只有 12 小時、剛好不重疊，沒事；h=3 時每對重疊 24 小時。
- 這票怎麼擋：用「區塊置換」——打亂標籤時把重疊的事件綁成一塊一起動，不拆開。但**用公式直接算修正後 p 值**（像主線 IC 用的 HAC）在「事件不等距」下沒有現成公式，所以標 needs-research。
- 何時做：委員會定出事件序列的顯著性估計式。

**R-2 跨 symbol 合併（blocked-by Panel IC 票）**
- 白話：你的 CSV 可以同時有 ETHUSDT 和 BTCUSDT 的事件。這票一次只算**一個 symbol**（跟現在的條件 IC 一樣：不是本次 feature run 的 symbol 會被排除並列出來）。
- 舉例：CSV 165 筆 ETH＋40 筆 BTC，選 ETH 1h feature run ⇒ 只算 165 筆 ETH，40 筆 BTC 在報告「排除清單」。要把兩邊放一起算「哪個特徵在所有幣都分得開」，那是 Pooled／Panel IC 票（ROADMAP GAP-4）的事——它要先解決「不同幣的特徵尺度不同、事件密度不同」怎麼合併。
- 何時做：GAP-4 開票。

**R-3 triple-barrier 出場（user-ruling 8/19）**
- 白話：現在的答案是「時間出場」——t₀ 之後第 h 根收盤，不管中間漲跌。triple-barrier 是「停利／停損／時間三道牆，先碰哪道算哪道」。
- 舉例：h=2、停利 +5%、停損 −3%。事件後第 1 根就漲 6% ⇒ 時間出場算第 2 根收盤（可能已回落到 +1%），triple-barrier 算 +5% 出場。兩種答案不同，特徵排名也會不同。
- 為何不做：你 8/19 裁定第一版用時間出場；而且出場最佳化會碰回測層（成熟度地圖：不完整層）。
- 何時做：你提出，且回測層成熟。

**R-5 h／k 掃描 × 0/1 標籤（user-ruling 9/9）**
- 白話：報酬模式有「掃描立方體」——把 h 從 1 掃到 12、k 從 0 掃到 3，看每格的 IC。但你的 0/1 標籤是你事先標好的，**不隨 h、k 變**：h=1 時 ETH 1/2 那筆是 1，h=12 時它還是 1。掃描出來每一格都一樣，沒有意義。
- 舉例：你選「匯入標籤模式」又勾掃描 ⇒ 平台直接回 400「掃描不適用於匯入標籤模式」，不會浪費 11 分鐘跑一格一格相同的結果。
- 何時做：你提出「按 k 掃描 0/1」這種需求（例如「決策點往前挪 k 根，特徵還分得開嗎」——那是另一種問題）。

**R-6 與 GAP-3 B1.4 合流（blocked-by 切分模型不同）**
- 白話：8 月 GAP-3 已經寫過一支「每個特徵算 AUC＋打亂標籤驗證」的函式（B1.4），跟這票第 3 段做的事很像。但它用的是「事件切分」（把事件本身分訓練／驗證），這票用的是 IC 主線的「時間切分」（把 K 線列分訓練／驗證，purge／embargo 都在這裡）。兩套切分不能混，所以這票只借它的「打亂驗證」核心，不合併整支。
- 舉例：B1.4 會說「這 165 個事件，前 132 個訓練、後 33 個驗證」；IC 主線說「2025-12-22 05:00 之後的 K 線是驗證段，落在裡面的事件有 31 個」。兩邊驗證段的事件集合不同，數字不能互比。
- 何時做：事件切分與 IC 切分統一的票。

**R-7 label 視窗太長時「區塊不足 10 個」（needs-research）**
- 白話：R-1 的區塊置換要至少 10 個區塊才打得亂。區塊長度＝視窗÷事件最小間距。165 個事件、最小間距 12 根 1h：h=1 ⇒ 區塊長 1 ⇒ 165 塊 ✅；h=16 ⇒ 區塊長 16 ⇒ 11 塊 ✅；h=20 ⇒ 區塊長 20 ⇒ 9 塊 ❌。
- 不足時會怎樣：統計照算、表照出，但**倖存者標「不可餵 ML」**，畫面琥珀提示「label 視窗太長，無法做依賴感知放行」。這是保守（不確定的東西不交），不是壞掉。
- 為何不放寬 10：10 是置換檢定的慣例下限，放寬就是拿假的「驗證過」去交付；要改須委員會簽核。
- 何時做：同 R-1（有了解析式就不必靠置換）。

## 附：為什麼全域 run 和事件 run 的隔離區不一樣（你 9/10 問的）

先講隔離區是幹嘛的：切分成「訓練段」和「驗證段」時，中間要空一段，讓訓練段最後幾根的**答案**（往後看的報酬）不會偷看到驗證段的價格。空多少，取決於「答案往後看了多遠」。

| | 全域 run（1h 特徵） | 事件 run（12h 事件 × 1h 特徵） |
|---|---|---|
| 答案是誰算的 | 平台：每根 1h 看**往後 5 根**（`return_5`，設定檔 `default_horizon=5`） | 你的 label 規則：每個事件看**往後 h 根 12h**（這次 h=1 ＝ 12 根 1h） |
| 答案往後看多遠 | 5 小時 | 12 小時（h=1）；h=3 就是 36 小時 |
| **purge**（答案窗）現況 | 5 根 ✅ | 5 根 ❌（抄了全域的數字，跟你的 h 無關） |
| **purge** 改後 | 5 根（不變） | **max(5, 你的 label 視窗)** ＝ 12 根 |
| 批次有沒有「額外往後看」 | 沒有 | 有：你在 /search 篩正例時用了**往後 12 根 12h** 的資訊（批次宣告 `lookahead_bars_declared=12`）＝ 144 小時 |
| **embargo** | 0（設定檔預設，沒有東西抬它） | 144 根（被那個 144 小時的宣告抬上去） |
| 總隔離 | 5 根 | 現況 149 → 改後 156 |

一句話：**全域 run 的答案是平台算的、窗只有 5 小時、沒有事先「偷看」，所以隔離只要 5 根。事件 run 的答案是你定的（窗隨 h 變），而且你挑事件時已經用了未來 6 天的資訊，所以要多空 144 根。** 兩邊不一樣不是 bug，是答案的來源和「事先看了多遠」本來就不同；EVTLABEL 第 2 段修的是「事件 run 的 purge 應該跟你的 h 走，而不是抄全域的 5」。

另外你問「全域 purge 是不是要按選的每個 horizon 各做一次」：不用。跨越訓練／驗證邊界的答案只有 `return_5` 那條；你選的其他 horizon（1,2,3,8,13,21）是「IC 衰減」圖，只在驗證段裡面算、不跨邊界，所以 purge 不必跟著它們變。

## 附：你 9/10 追問的四題（逐字整理）

**Q1 全域 horizon 選 1,3,8,21（沒選 5）purge 怎麼算？**
purge ＝ 主線那條 label 的 horizon。清單有 5 就用 5；沒有 5 就默默取清單第一個（這例＝1，主線 label 變 `return_1`、purge 1）。一致所以不洩漏，但畫面沒揭露——**你已裁定另開小票修**（見 ROADMAP「GLOBALH」）。其他 horizon 只在驗證段算 IC 衰減，不影響 purge。

**Q2 事件 h 超過 12（14、16）怎麼辦？**
12 不是上限。h=14 ⇒ purge 168、總隔離 312；h=16 ⇒ purge 192、總隔離 336。副作用：驗證段被吃更多列（仍夠）；匯入標籤模式的區塊置換要 ≥10 個區塊，165 事件時 **h≥17 會不足 ⇒ 該 run 標「不可餵 ML」（R-7，琥珀提示，不是 bug）**；h 太大時尾端事件算不出答案，UI 的 `h_max_feasible` 會擋。

**Q3 為什麼 purge 不是「最大答案窗」＋embargo？「答案由平台算、無未來洩漏」是什麼意思？**
就是那條公式，只是「最大」是對**跨越訓練／驗證邊界**的答案取，不是對畫面上所有 horizon 取。全域 run 訓練段只用 `return_5`，其他 horizon 只在驗證段內算 ⇒ purge 5。embargo 管的是**挑樣本時偷看了多遠**：全域樣本＝全部 K 線、沒有挑 ⇒ 0；事件樣本是你用「之後 12 根 12h 漲幅」挑的 ⇒ 挑的動作已含 6 天未來 ⇒ 144。**purge＝跨邊界最大答案窗，embargo＝挑樣本時偷看多遠**；全域 5＋0，事件 max(5, h 視窗)＋144。

**Q4 CSV 帶 1–12 根報酬、我用 1／3／6 篩、UI h=2，事件型 purge／embargo 是什麼？**
purge ＝ max(5, 2×12) ＝ **24**（只看 UI 的 h；你篩選用的 1/3/6 不影響）。embargo ＝ 匯入時宣告的深度×12：CSV 匯出預設宣告 12（附的最大 horizon）⇒ **144**；你可在匯入表單改宣告 6 ⇒ 72，但平台驗不了你用哪幾欄篩，要勾「無法驗證」，宣告比實際小＝自己製造洩漏；懶得想就留 12。總隔離 24＋144＝168。

**你的裁定（9/10）**：purge 部分同意；全域靜默用第一個 horizon 另開票；標籤（P3）那部分你還沒看。⇒ 我先凍結 P1／P2 動工（三家 R2/R3 皆判可獨立凍結），P3 等你看完再放行。

## 我要你回答的
1. 「餵 ML」交付到倖存者檔＋接收端契約測試為止——**接受／不接受**。
2. 其餘規格——**可以動工／哪裡不對**。
